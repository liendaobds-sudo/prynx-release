[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
# Chỉ thu thập test trong thư mục tests/ — tránh các script thử nghiệm ở gốc
# backend/ (test_bbox.py, test_api.py…) làm hỏng collection khi
# chạy `pytest` trần ở thư mục gốc.
testpaths = tests
