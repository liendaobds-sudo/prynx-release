# SECURITY: bump 0.115.14 -> 0.138.1 để kéo starlette 1.3.1 (transitive, nằm dưới
# MỌI request). Vá chuỗi CVE starlette 0.46.2: CVE-2025-54121, CVE-2025-62727,
# PYSEC-2026-161, CVE-2026-48818, CVE-2026-48817, CVE-2026-54282, CVE-2026-54283.
# fastapi 0.115.x ghim starlette<0.47 nên KHÔNG thể pin starlette vá trực tiếp ->
# buộc bump fastapi (vẫn 0.x, không phải major). pydantic đã 2.12.5 nên không bị
# ép nâng. LƯU Ý: extra [standard] phiên bản mới kéo thêm fastapi-cloud-cli +
# sentry-sdk (xem ghi chú rủi ro chuỗi cung ứng trong báo cáo audit).
fastapi[standard]==0.138.1
# SECURITY: pin starlette TƯỜNG MINH lên 1.3.1. fastapi 0.138.1 chỉ yêu cầu
# starlette>=0.46.0 (KHÔNG có cận trên) nên pip giữ nguyên 0.46.2 đã cài (vẫn dính
# CVE) — phải ép pin để thực sự vá. 1.3.1 thoả ràng buộc fastapi, không xung đột.
starlette==1.3.1
uvicorn[standard]==0.30.0
pydantic-settings==2.5.0
sqlalchemy==2.0.35
alembic==1.13.0
psycopg2-binary==2.9.9
celery[redis]==5.4.0
redis==5.1.0
websockets==13.0
# SEC (pentest 2026-08-28 §ATK.04): nâng 4.30.0 → 5.13.0, kéo PDFium bundle từ build
# 6462 (~T5/2024) lên 7999 — khoảng hai năm bản vá bộ nhớ của PDFium/Chromium. PDFium
# parse PDF KHÔNG tin cậy và chạy IN-PROCESS trong sidecar nên đây là giảm rủi ro trực
# tiếp cho §ATK.04 (crash/RCE hạ sidecar).
#
# Bump major 4→5 nhưng diện va đụng API bằng 0 sau khi kiểm từng breaking change của
# v5.0.0: repo KHÔNG dùng `PdfDocument.render()` (đã xoá), `PdfBitmap.get_info()`,
# `fill_rect`, `from_pil`, `get_pos`, `fb_render`, `get_toc`, `PdfMatrix.mirror`,
# `get_text_range`, và các cờ version cũ (V_LIBPDFIUM…). Mọi `PdfImage` trong repo là
# của **pikepdf**, không phải pypdfium2 (nên rename get_size→get_px_size không ảnh
# hưởng); `PdfPage.get_size()` vẫn còn ở v5; `to_numpy()` chỉ dùng trên render màu nên
# đổi shape ảnh grayscale không chạm tới.
#
# Verify: FULL backend pytest 4288 passed / 2 skipped / 0 failed, gồm golden render và
# parity màu — PDFium mới không làm lệch artifact. License không đổi: Apache-2.0 OR
# BSD-3-Clause (permissive, dùng được trong sản phẩm đóng).
pypdfium2==5.13.0
# SECURITY: bump 0.11.0 -> 0.11.9 để kéo pdfminer.six==20251230 (transitive, parse
# PDF live). Vá CVE-2025-64512 (fix 20251107) + CVE-2025-70559 (fix 20251230).
#
# SEC (pentest 2026-08-28 §ATK.04): nay lên 0.11.10 CÙNG LÚC với pypdfium2 5.x — lý do
# hoãn trước đây (0.11.10 ép pypdfium2>=5.9.0) đã được xử lý bằng cách nâng cả cặp.
# Kéo theo pdfminer.six 20260107.
pdfplumber==0.11.10
# SECURITY (audit 2026-07-25, pip-audit): 6.13.3 -> 6.14.2 vá 4 CVE
# (CVE-2026-59935/59936/59937/59938). pypdf parse PDF do người dùng nạp nên nằm
# thẳng trên đường chạy — không phải dep ngủ.
pypdf==6.14.2
opencv-python-headless==4.10.0.84
scikit-image==0.24.0
# SECURITY (audit 2026-07-25, pip-audit): 12.2.0 -> 12.3.0 vá 20 advisory
# (PYSEC-2026-2253..2257, 3451..3454, 3493..3496, ...). Pillow decode ảnh nhúng
# trong PDF / ảnh người dùng nạp (tách nền, upscale, OCR) → đường chạy thật.
Pillow==12.3.0
python-multipart==0.0.31
# reportlab: GIỮ. (kiến trúc audit 2026-07-29 §C.2 — đính chính) Ban đầu tưởng chỉ
# `api/routes/report.py` dùng nên định bỏ, nhưng người dùng thật gồm
# `workers/vdp_engine.py`, `workers/nup_report.py`, `workers/cut_export/emitters/pdf_spot.py`,
# `core/layer_engine.py`, `core/stream_editor.py` + ~18 file test. Bỏ là vỡ VDP/báo cáo.
reportlab==4.2.0
fonttools==4.60.2
uharfbuzz==0.55.0
aiofiles==24.1.0
python-dotenv==1.2.2
httpx==0.27.0
# SECURITY: bump pytest 8.3.0 -> 9.0.3 vá CVE-2025-71176 (chỉ dev/test, không nằm
# trên đường runtime live). pytest-asyncio 0.24.0 -> 1.4.0 để tương thích pytest 9
# (0.24.0 ghim pytest<9). Config asyncio_mode=auto vẫn được 1.x hỗ trợ.
pytest==9.0.3
hypothesis==6.155.3
pytest-asyncio==1.4.0
# Reproducible Windows production-build toolchain.
maturin==1.13.3
Nuitka==4.1.2
ordered-set==4.1.0
zstandard==0.25.0
pytesseract==0.3.13
# SEC (pentest 2026-08-28 §ATK.04): nâng 9.5.0 → 10.12.0 để bundle libqpdf 12.3.2
# (từ 11.9.1). qpdf parse PDF KHÔNG tin cậy ngay ở /upload và chạy IN-PROCESS trong
# sidecar, nên các bản vá bộ nhớ của qpdf 12.x (nhiều fix đến từ fuzz) giảm trực tiếp
# rủi ro crash/RCE hạ sidecar.
#
# Bump này ban đầu làm ĐỎ test_action_engine_native::test_downscale_keeps_smask_aligned_
# with_image. Nguyên nhân KHÔNG phải qpdf: pikepdf 10 đổi `PdfImage.as_pil_image()` sang
# TỰ ÁP `/SMask` nên trả RGBA thay vì RGB, và guard số-kênh trong `_resample_image_stream`
# (đúng đắn) từ chối ghi 4 kênh vào /DeviceRGB. Đã vá engine bằng cách bỏ kênh alpha
# tường minh — xem `pdf_actions_native._resample_image_stream`.
pikepdf==10.12.0

segno==1.6.6
python-barcode==0.16.1
# VDP upgrade (spec vdp-upgrade 1.2/1.3/1.12): đọc nguồn dữ liệu Excel .xlsx
# (gồm tên sheet và merged cell) trong Data_Source_Reader (vdp_datasource.read_xlsx).
openpyxl==3.1.5
# Office → PDF (Windows): Word/Excel COM. Optional — engine degrades to LibreOffice if absent.
# Install on Windows build/runtime: pip install pywin32
pywin32==308; sys_platform == "win32"
shapely==2.0.6
scipy==1.12.0

# Background removal (isnet/birefnet) + Upscale (realesrgan) — cả 3 engine chạy ONNX.
# HARD import trong birefnet_engine.py/isnet_engine.py/realesrgan_engine.py — BẮT BUỘC
# có để feature chạy trong bản đóng gói (trước đây chỉ cài ad-hoc trong venv → Nuitka sót).
# LƯU Ý upscale: repo Real-ESRGAN chỉ phát hành .pth → phải convert sang .onnx ở BƯỚC
# BUILD bằng scripts/convert_realesrgan_onnx.py (cần torch+onnx, CHỈ ở máy build; runtime
# KHÔNG cần torch). build_production.ps1 tự chạy convert nếu build venv có torch.
onnxruntime==1.24.4

# Serial transport cho Cut Export (spec gui-may-be 5.3/5.5 — gửi lệnh cắt qua cổng COM).
# serial_port.py import lazy; thiếu -> kênh serial trả lỗi "missing pyserial".
pyserial==3.5


