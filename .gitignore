# Environment variables (SECURITY: contains Supabase keys)
.env
desktop/.env

# Dependencies
node_modules/
dist/
.pnpm-store/
venv/
venv-broken-*/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Legacy frontend (Next.js web — replaced by Tauri desktop)
frontend/

# PDF Processing Tools
backend/bin/pdfcpu.exe
backend/bin/pdfcpu

# Tauri / Rust Build Artifacts
src-tauri/target/
native/target/
imposition_core/target/
print_engine/target/
*.rs.bk

# Sidecar binaries (237 MB each — built by build_production.ps1, too large for GitHub)
desktop/src-tauri/binaries/

# Ban copy THIRD_PARTY_NOTICES.md dung de dong goi vao installer.
# Ban duoc theo doi la ban o goc repo; build_production.ps1 copy no vao day.
# Theo doi ca hai ban se sinh ra hai ban le nhau, va ban le se la ban ship.
desktop/src-tauri/THIRD_PARTY_NOTICES.md

# OS Files
.DS_Store
Thumbs.db

# Test Outputs
test_modified.pdf
test_original.pdf
backend/uploads/
backend/results/

# SQLite databases
*.db
backend/data/

# Next.js
frontend/.next/
*.pdf
backend/ocr_*.txt
desktop/test_*.pdf
desktop/*.py
desktop/*.js
!desktop/eslint.config.js
desktop/*.txt
backend/temp/

# Illustrator scripts
*.jsx

# Debug log files
debug_main_*.txt
debug_frontend_*.txt
detect_shape_error.log
*.log

# Temp/scratch files at root
diff.txt
search_*.txt
temp_*.txt
temp_*.tsx
recovered_*.json
recovered_*.ts
extract.py
find_res_bottom.py
old_sticker_imposer.py
scratch_rewrite.py
# Chỉ ignore scratch test_*.py ở gốc repo và gốc backend/ — KHÔNG ignore backend/tests/.
/test_*.py
/backend/test_*.py
test_*.png
test_*.js
test_*.txt

# Debug folder
.debug_logs/
/results/

# Sandbox / Trash folders
/test/
sandbox/
backend/sandbox/
backend/debug/
backend/debug_output/
/_to_delete/
backend/test_*.jpg
proposal.html
backend/download_sam.py
backend/search_models.py
desktop/src-tauri/icons/android/
desktop/src-tauri/icons/ios/

# Release output (installers built locally — too large / not source)
Ban_Phat_Hanh/

# Cargo/diagnostic scratch logs
_diag*.txt
desktop/src-tauri/_cargo_out.txt
desktop/src-tauri/_cargo_err.txt
desktop/src-tauri/_chk/

# Claude Code local data (memory/plans/session — per-machine, not source)
.claude/

# Local terminal scratch
terminals/

# Build scratch / diagnostics (not source)
tmp/
backend/scratch/
backend/nuitka-crash-report.xml

# Bản sao môi trường Python cục bộ giữ để rollback, không phải source/release input.
backend/venv-*-backup-*/

# Icon backups (kept locally before white/transparent conversion)
desktop/src-tauri/icons/_backup_before_white/
desktop/src-tauri/icons/_backup_transparent/
desktop/src-tauri/icons/_backup_opaque_white_*/

# Secret/scratch cuc bo - tuyet doi khong commit.
/.sr_tmp

# Intro animation render artifacts (video + verify frames — regenerated by render-intro.mjs)
desktop/scripts/intro/out/

# Research clones (img2threejs etc.)
tmp/research/

# Private production PDFs used for local PPE validation.
# Never commit customer artwork or generated corpus reports.
/private_test_corpus/

# Cargo output của Rust probe trong spike logo (tái sinh được, không phải mã nguồn).
/tools/logo_rebuild_spike/rust_probe/target/

# Scratch patch files from experimental Codex sessions (never source-of-truth).
.codex_*.patch

# Supabase CLI temp/link state (machine-local, not source).
supabase/.temp/

# Release env scripts (chua secret — KHONG commit ban da dien)
scripts/set_release_env.ps1

# ─── KIENTRUC (audit 2026-07-29 §F): thư mục rác cục bộ còn sót ───────────────
# Đã kiểm `git ls-files`: các mục dưới đây KHÔNG được theo dõi, nhưng cũng chưa
# được ignore nên `git status` vẫn kêu và mọi lệnh grep/quét của người + của AI
# đều phải lội qua chúng. Ignore để cây làm việc sạch; xoá đĩa thì tự quyết.
backend/_atm2/
backend/_atm4/
backend/_audit_tmp2/
backend/logs/
logs/
uploads/
