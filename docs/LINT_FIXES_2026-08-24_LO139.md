# LO139 — Process handlers

## Phạm vi

- `desktop/src/lib/processHandlers.ts`

## Thay đổi

- Type hóa boundary của context, batch output, bytes/blob, file có path và các nhóm settings; loại bỏ `any` ở pipeline bình bản, resize, shuffle, trim/shift, split và merge.
- Dùng `unknown` + cast có tên ở biên vào cho các fixture/recipe cũ, giữ union `GuillotineSettings`/`DieCutSettings`/`OffsetSettings` để không mất narrowing.
- Giữ nguyên payload backend, route chọn theo settings và các nhánh native-path/frontend.
- Trong verify phát hiện và sửa hồi quy self-reference do thay đổi type (`targetW = targetW ?? 0`), khôi phục đúng nguồn `settings.targetW/targetH/scaleMode/applyToStr`.

## Verify

- ESLint file mục tiêu: đạt, 0 lỗi, 0 cảnh báo.
- `npx vitest run src/lib/processHandlers.test.ts src/lib/processHandlers.mixedGuillotine.test.ts`: 52/52 đạt.
- Typecheck: không còn lỗi processHandlers; đang chờ lô AcrobatViewer xử lý các lỗi còn lại.
- Chưa commit/push/build.
