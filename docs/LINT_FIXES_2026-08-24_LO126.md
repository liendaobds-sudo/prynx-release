# LINT FIXES 2026-08-24 LO126

## Phạm vi

- desktop/src/hooks/viewer/useTileRenderer.ts

## Việc đã làm

- Thay kiểu `file: any` bằng `ViewerFileLike | null | undefined`, giữ đủ metadata path/name/type và identity mà renderer đang đọc.
- Thay `pdfRef: any` bằng `PDFDocumentProxy | null | undefined` từ PDF.js.
- Bổ sung kiểu block chữ và bộ kiểm tra `PdfJsTextItem`; thay `Record<number, any[]>` và callback item `any` bằng kiểu cụ thể.
- Lọc an toàn các marked-content item không có tọa độ trước khi dựng block chữ; text item hợp lệ vẫn giữ nguyên công thức bbox/origin và payload trả về.
- Không thay đổi tuyến render native/PDF.js, cache, cancellation, màu hay request payload.

## Verify

- ESLint mục tiêu: đạt, 0 errors / 0 warnings.
- npx vitest run src/hooks/viewer/useTileRenderer.test.ts: 1 file, 34 passed.
- npm run typecheck: đạt.
- Không commit, push hoặc build release.
