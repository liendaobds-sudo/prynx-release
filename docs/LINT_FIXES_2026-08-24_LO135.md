# LO135 — App shell

## Phạm vi

- `desktop/src/App.tsx`

## Thay đổi

- Dùng `ToolLaunchPayload` cho tab payload và callback spawn tab.
- Thay các runtime check/catch `any` bằng kiểu mở rộng `globalThis`, `unknown` và helper kiểm tra Tauri.
- Type hóa sự kiện hover tab, kích thước file recovery và guard `lockedMode` từ snapshot cũ.
- Bổ sung dependency `t` cho effect dùng thông báo; không đổi routing, entitlement hay lifecycle tab.

## Verify

- ESLint file mục tiêu: đạt, 0 lỗi, 0 cảnh báo; suppression `react-hooks/set-state-in-effect` là chủ đích đã có.
- Typecheck: App sạch; còn 4 lỗi ở `LivePageFrame.tsx:5601`, thuộc lô LO136.
- Chưa commit/push/build.
