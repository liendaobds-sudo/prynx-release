# LINT FIXES 2026-08-24 LO116

## Phạm vi

- `desktop/src/components/workspace/SaveModal.tsx`
- `desktop/src/components/workspace/SavePrintFilesModal.tsx`

## Việc đã làm

- Gỡ `// @ts-nocheck` khỏi cả hai modal.
- Thay các `any` còn lại bằng kiểm tra kiểu rõ ràng:
  - `SaveModal`: so sánh blob trực tiếp, dùng `HTMLAnchorElement`, và nối `reportMsg` bằng giá trị hiện có thay vì updater function.
  - `SavePrintFilesModal`: chuẩn hóa `Error` handling bằng `instanceof Error`, giữ danh sách mode với tuple `as const`, và bỏ `as any` khỏi `setSavePrint`.

## Verify

- `npx eslint src/components/workspace/SaveModal.tsx src/components/workspace/SavePrintFilesModal.tsx`
- `npx tsc --noEmit --pretty false --project tsconfig.app.json`
  - Output lọc theo `SaveModal`/`SavePrintFilesModal` không còn báo lỗi từ hai file này.
  - Toàn project vẫn còn lỗi ở các file khác, nên `tsc` tổng thể chưa xanh.
- `npx vitest run src/components/workspace`

## Ghi chú

- Không đụng tới các thay đổi đang tồn tại ở những file khác trong working tree.
