# Lint fixes 2026-08-24 — Lô 105

## Phạm vi

Đợt này xử lý các finding `react-refresh/only-export-components` và `@typescript-eslint/ban-ts-comment` ở các module nhỏ, theo các tiểu-lô độc lập (mỗi tiểu-lô không quá 5 file). Không chạm backend, PDFium, build release hoặc thuật toán preview.

## LO105-A — BookReportSettings

- File: `desktop/src/components/imposition-tools/sections/BookReportSettings.tsx`.
- Bằng chứng trước sửa: chỉ còn `@ts-nocheck` ở dòng 1; ESLint ghi nhận 1 lỗi.
- Thay đổi: bỏ `@ts-nocheck`; TypeScript strict đã suy luận được toàn bộ state/config hiện tại, không cần assertion hoặc disable mới.
- Kết quả: ESLint file = 0 lỗi/0 cảnh báo; không đổi payload, nội dung report hoặc UI.

## LO105-B1 — FileContext

- Files: `desktop/src/lib/fileContext.tsx`, `desktop/src/lib/fileContextCore.ts`, `desktop/src/App.tsx`.
- Bằng chứng trước sửa: `fileContext.tsx` export cả Provider React và hook `useFileContext`, gây 1 finding `react-refresh/only-export-components`.
- Thay đổi: chuyển `FileContext`, các type API và hook sang module thuần `fileContextCore.ts`; `fileContext.tsx` chỉ còn export `FileProvider`; App import hook từ module core.
- Bất biến: giữ nguyên vòng đời Blob URL, fallback ngoài Provider, API method và cleanup theo tab.

## LO105-B2 — AcrobatViewer

- Files: `desktop/src/components/AcrobatViewer.tsx`, `desktop/src/components/AcrobatViewer.helpers.ts`, `desktop/src/components/AcrobatViewer.pageOverlay.test.ts`.
- Bằng chứng trước sửa: `AcrobatViewer.tsx` export component cùng các helper thuần overlay/scroll; finding Fast Refresh còn ở `shouldCenterVirtuosoList` (các helper cũ đã bị disable cục bộ).
- Thay đổi: chuyển toàn bộ contract/helper thuần (`PageOverlayRenderContext`, renderer, virtualization context, overlay target, selection scroll, center policy) sang module `.ts`; component và test cập nhật import.
- Bất biến: giữ nguyên điều kiện nhắm overlay theo instance/trang, Set selection, context revision và quy tắc chỉ căn giữa một row.

## Xác nhận

- Targeted ESLint sau sửa:
  - `fileContext.tsx`, `fileContextCore.ts`: 0 lỗi/0 cảnh báo, Fast Refresh = 0.
  - `AcrobatViewer.tsx`, helper và test: Fast Refresh = 0; các lỗi `any`/hook vốn có của AcrobatViewer vẫn giữ nguyên để xử lý lô riêng.
  - `BookReportSettings.tsx`: 0 lỗi/0 cảnh báo.
- `npm run typecheck`: đạt.
- `npx vitest run src/components/AcrobatViewer.pageOverlay.test.ts src/lib/bookReport.test.ts`: 2 file, 15 test đạt.
- `git diff --check`: không có lỗi nội dung; chỉ còn cảnh báo chuẩn hóa CRLF trên các file vốn đang dùng line ending khác.
- Chưa commit, push hoặc build release.

## Ghi chú an toàn

- Một thử nghiệm bỏ `@ts-nocheck` ở `AutoCatalogSection.tsx` được hoàn tác riêng vì còn finding `any` và exhaustive-deps; file đó không thuộc kết quả LO105.
- Các thay đổi có sẵn của phiên khác trong `AcrobatViewer.tsx`, `fileContext.tsx`, `App.tsx` được giữ nguyên; lô này chỉ tách export/helper và cập nhật import liên quan.
