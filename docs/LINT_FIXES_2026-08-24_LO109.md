# LO109 — Reset state modal chèn liên file (2026-08-24)

## Phạm vi

- `desktop/src/components/acrobat/CrossFileInsertModal.tsx`

## Bằng chứng

ESLint báo `react-hooks/set-state-in-effect` tại effect reset draft `pos/pageNum` khi `pending` đổi.

## Thay đổi

Tách shell modal và content thành component keyed theo pending request. Draft được khởi tạo từ request mới; khi đích, tên, số trang, chế độ hoặc danh sách nguồn đổi, key remount content và giữ hành vi reset cũ. Khi không có pending, shell trả `null` như trước.

## Verify

- ESLint mục tiêu: 0 lỗi, 0 cảnh báo.
- `npm run typecheck`: đạt.
- Chưa commit/push/build release.
