# PrynX Master Audit Matrix

> Phản hồi chất lượng 2026-09-07 · `W2-U09/W7-U11`: người dùng xác nhận tốc độ tốt hơn nhưng nesting càng lên càng lệch hàng. **§NESTROW.1 SOURCE + CORE/AUTO + ARTIFACT + RUNTIME BACKEND đã sửa; GUI/TAURI/INSTALLER HOLD**: periodic baseline không còn fallback greedy cho S&R và không bị cắt giữa motif bởi deadline; core/runtime fixture giữ sàn 69/71/58/31 (mục tiêu 66/66/57/29), PDF thật 4 mẫu giữ đúng sàn, CUT không có `cm`/`Do` ẩn. Native `baselineVersion=13` đã cài vào `backend/venv`, smoke 145/145 đạt; chưa nghiệm thu GUI/Tauri/installer. Không quy hiện tượng cũ thành PDF tích lũy CTM hoặc whole-solve lặp nhiều lần. Xem `NESTING_ROW_DRIFT_FIXES_2026-09-07.md` và `audit/NESTING_ROW_DRIFT_R2_R3_EVIDENCE_2026-09-07.json`.

> Triển khai tiếp theo 2026-09-07 · Theo yêu cầu sửa lệch rồi làm toàn bộ B–F không hỏi lại: **S1 và B–F đã có SOURCE + AUTO, artifact/benchmark theo phạm vi; GUI chưa nghiệm thu**. S1 nối simple_auto ở full-layout và preview homogeneous, 24 ca PDF/raster đạt. B diagnostics CUT dev opt-in, PDF byte-identical; C chặn tab nền và stale/owned abort, 125 frontend test + typecheck đạt. D context per-part/side: 659 ô projection 346,567→185,507 ms; E index giữ visits3P thay P×S và nguyên pixel/report. F vẫn full native/hash/schema/source/lease, CPU→RAM admission + size/SHA reread; fixed13manifest/659placement N20: load 2776,660→1095,589 ms; export 5245,434→3563,229 ms, solve0, raster26trang khớp, peakRSS export117,512→118,289MiB, grant13/capacity15. Finalbackend770pass/1skip cũ; khônggolden/commit/release. Computer Use hiện đã đọc được cửa sổ PrynX debug; ứng dụng chặn ở “Không thể xác minh bản quyền” vì chưa có checkpoint thời gian tin cậy. Chỉ kích hoạt/quan sát, chưa thao tác tính năng hoặc hộp xác thực; cần người dùng khôi phục xác minh bản quyền rồi tiếp tục nghiệm thu GUI. Kiểm độc lập 17/17 hash nguồn/fixture/native khớp, HEAD không đổi; không nâng RUNTIME. Xem `BINH_TEM_BE_SIMPLE_AUTO_B_F_FIXES_2026-09-07.md` và `audit/BINH_TEM_BE_B_F_EVIDENCE_2026-09-07.json`.

> Triển khai đã duyệt 2026-09-07 · `W2-U01/W2-U09/W7-U11`: A1–A2 của audit hiệu năng Bình tem bế đã sửa **SOURCE + AUTO**, chưa Tauri/installer. §TEMPERF.C1: batch canonical cùng preview đơn, giữ original-path/revision key và lifetime worker; matrix trước sửa 12/16 đỏ, sau sửa full file 34 pass. §TEMPERF.1: singleflight, ownership deepcopy, lỗi/retry/reentrant, exact-float key và SourceFingerprint trước canonical/sau compute; source đổi trả 409; warm không mở worker PDF nếu không cần né ốc. Test singleflight 30 pass, regression backend 142 pass, typecheck/py_compile đạt; không golden update.
>
> A/B xen kẽ 2 warm-up + N=20 trên PDF 17 mẫu, chỉ đo cặp request legacy cho DUMBBELL: cold trùng **841,210→400,287 ms**, compute 2→1, cùng response hash; tuần tự 371,801→369,369 ms; warm 59,537→59,992 ms. Không phải speedup toàn feature/True-shape. Artifact optimal_auto Rotate=90 đạt 156/156 điểm CUT, 4 trang; probe simple_auto riêng còn lệch 48/144 và cold baseline HEAD cũ giống hiện tại — giữ proof gap ngoài A1–A2, chưa sửa engine. Xem `BINH_TEM_BE_PREVIEW_A1_A2_FIXES_2026-09-07.md` (raw số đo/script); các finding khác vẫn mở, chờ kiểm thao tác thật trước lô mới.

> Bổ sung 2026-09-07 · `W2-U01/W2-U09/W7-U11`, audit hiệu năng preview/thực thi Bình tem bế: **TRACED + PROBE, CHỜ DUYỆT; chưa sửa production**. Xác nhận 5 P2: compute trùng single/batch, bảng sức chứa chạy tab nền, parse polygon lặp, writer quét placement theo từng tờ và WARNING CUT trong vòng nóng. Thêm §TEMPERF.C1 P1 ở hợp đồng cache: PDF hai trang độc lập, trang 0 Rotate=90, batch→single làm 12/12 polyline đổi bbox 80×180→180×80 pt trong khi cell giữ nguyên; chưa chứng minh PDF xuất sai/Tauri runtime. Cần chuẩn hóa batch trước singleflight.
>
> Baseline riêng: legacy PDF 17 mẫu, 1 warm-up + N=3, engine median 2,312 s/34 trang; explicit nesting PDF 13 mẫu, 2 warm-up + N=3, cold/warm/export 15,171/3,000/10,191 s, warm/export solve=0. Lượt stage N=1 khác đạt export 6,234 s, trong đó load/verify manifest 3,314 s (53,2%); không có patch, không gọi khác biệt là speedup. Placement N=3 là 583/589/582, stage 659; SLO/quality KPI vẫn HOLD dù same-run manifest/pose và source hash khớp. Native thực đo `6fc91ac5…0ce89`, dirty provenance; HEAD `79f05a39…c7e3aebc`.
>
> Verify: frontend 106 pass/3 file; backend 144 pass + 1 named-pipe WinError 5 trong sandbox, đúng ca chạy lại ngoài sandbox 1 pass. Có probe root độc lập, parse/raster PDF nesting và soi Front/CUT đầu; chưa first-paint/Tauri/installer, CNC duplex nặng hoặc hai tier RAM thấp. Báo cáo: `BAO_CAO_AUDIT_BINH_TEM_BE_PREVIEW_THUC_THI_HIEU_NANG_2026-09-07.md`; script/số đo/fingerprint: `audit/BINH_TEM_BE_HIEU_NANG_EVIDENCE_2026-09-07.json`. Các delta tách tem và audit cũ bên dưới giữ nguyên.

> Bổ sung 2026-09-06 · `W2-U03-SHADOW/W2-U04-SHADOW`: người dùng xác nhận đã test Lô A; đã triển khai Lô B. `AUTO + ARTIFACT-PARTIAL`: 197 test đạt; trên PDF gốc, hai mode nhận cùng Alpha/đường preview với cùng tham số, không gọi AI; điểm bóng Alpha 0, ROI thân đủ 650/650. Bốn artifact classic/multi × bleed 0/2 mm đều 1 trang/1 CutContour, đã parse/render; classic 0 vẫn giữ RGB gốc ngoài đường dao, không tuyên bố xóa nền toàn artwork. Có chốt bảo vệ viền phẳng/viền sáng/tai nhạt JPEG, fallback khi mơ hồ. Chưa Tauri/installer cho Lô B, chưa Lô C. Xem `BU_XEN_SHADOW_FIXES_2026-09-05.md`; baseline audit `BAO_CAO_AUDIT_BU_XEN_SHADOW_2026-09-05.md`.

> Bổ sung hiện hành 2026-09-05 · `W2-U09/W7-U11`: audit độ thông minh/hiệu năng
> nesting đạt **`TRACED + PROBE · Lô A đã sửa source, artifact còn HOLD`**. Lô A đã nối
> `cncDuplexMarks` qua cache key → payload → schema → mapper → job identity; test contract
> true/false đều pass. Mở §NEST26.1 P1:
> CNC duplex marks rơi ở preview (0 obstacle) nhưng export có 4; identity/reference miss
> đã tái hiện, chưa chứng minh collision hoặc pose PDF sai. §NEST26.2 P2 ở core quantity:
> Balanced 12 trial đều bị ngắt trên CONSTRAINTS/S20, rơi về baseline; S20 vùng bao tờ cuối
> lớn hơn Fast 7,95% (không phải giảm số tem/hao giấy production). Grant 1 và 15 cho cùng
> score/hash ở hai ca này; Tem/CNC hiện dùng Fast. §NEST26.3 P2: corpus N=3 thực chạy grant 1,
> 15 chỉ là planner metadata. S&R 13 trang N=1 solve-only: production batch 10,088 s/600
> placement, serial 25,799 s/661; không chạy auto UI quality gate/render, không gọi là
> speedup giữ chất lượng. Baseline-lock S&R là chủ đích; NFP gap sheet-axis đúng, 2 test pass.
> Backend focused 257 pass/1 WinError 5 môi trường, ca đó rerun ngoài sandbox 1 pass;
> Rust lib 47, scanner self-test 17 pass. Installed native `fbe222dd…572bf`,
> source revision `baeec051…`, dirty; HEAD lập báo cáo `669b92db…` không chứng minh binary
> parity toàn source. Chưa CNC duplex artifact nặng/Tauri/installed/N20 hiện hành. Đo bổ sung đúng file khách
> `test/test nesting.pdf` (backend explicit, 2 warm-up + 3 đo): cold preview P50 `15,417 ms`, warm `2,736 ms`,
> handoff `405 ms`, export `11,069 ms`, export solve lại `0`; trong từng lượt preview/export manifest/pose và 26
> trang khớp. Ba cold placement `612/592/589` khác nhau, nên đây là baseline khảo sát, chưa phải speedup hay
> quality KPI.
> Báo cáo: `BAO_CAO_AUDIT_NESTING_THONG_MINH_HIEU_NANG_2026-09-05.md`;
> raw/probe: `audit/NESTING_THONG_MINH_HIEU_NANG_EVIDENCE_2026-09-05.json`.

> Bổ sung hiện hành 2026-09-04 · `W8` vẫn **`STALE · HOLD`**: source/test đã có native
> Save As grant/lease §SEC.15; sidecar-generation key đóng khe replay §SEC.19/§ATK.09;
> signature v2 bind request body §SEC.21; Tesseract lock + hardlink/link-count/ADS/reparse/
> identity lease §SEC.23; trusted tool/source/publisher authority §SEC.24-R1–R7. R4 khóa Rust
> toolchain theo content-addressed exact-set/provenance; R5 pin canonical GitHub updater
> endpoint/destination; R6 khóa Git/GitHub metadata/config/credential/grammar, SemVer ASCII-only,
> exact raw/escaped tag route, Known Folder signing key và Notepad; R7 neo BAT vào `%__APPDIR__%`.
> Snapshot nền: backend token/body/route/probe **144/144**, frontend auth/license **89/89** +
> typecheck, Edge **36/36**, Rust **227 passed/5 ignored** + release check/fmt, Save As registry
> **23/23**. Delta release: secret-store từng **19 pass + 3 `[BLOCKED-ENV]`** và sau bản vá
> availability Git 2.53 đạt **22/22 ngoài sandbox**; artifact-runtime
> **55 pass + 1 `[BLOCKED-ENV]`** trong sandbox; bốn ca môi trường đạt **4/4 ngoài sandbox**;
> no-Ghostscript **49/49**, native QA **1/1**, grammar/wrapper/AST **3/3**; parser PS5.1 + pwsh 7
> đạt bốn script, `py_compile` đạt hai test file và independent YAML/line-ending/BAT verifier sạch.
> Ratchet **191/191** là route coverage, không phải test count. Không nâng `RUNTIME`/`Closed`:
> production DB/Edge, key/secret thật, Authenticode artifact, TPM/CNG/no-TPM, clean build/install/
> uninstall + VM fault injection, Save As UI/NAS/non-NTFS, quyền GitHub push/release,
> `gh` tương lai và packaged double-click/hostile-`PATH` vẫn `[EXTERNAL]`; OAuth/Credential Manager/
> GitHub read-only đã đạt với `gh` 2.93.0. BAT interpreter/file
> association cùng Ring-3 admin/debugger/memory patch là residual accepted risk. Checkout hiện tại
> còn bị chặn đúng thiết kế bởi `extensions.worktreeConfig=true`, `.git/info/exclude` non-empty
> 335 byte và `.git/info/attributes` missing; đây là prerequisite cục bộ, không phải `[EXTERNAL]`.
>
> Snapshot 2026-09-02 (xem delta 2026-09-05 ở trên) · `W2-U09/W7-U11` đã triển khai Lô A–F. Snapshot
> source/native `04f40087…b4d0a0`, N=20 sau hai warm-up trên máy `≥16 GB`: preview
> 13 mẫu cold P50/P95 `9,313/10,758 giây`, warm `2,265/3,458 giây`, RSS P95
> `138,2/136,7 MB`, warm `solve=0/20`; execution từ reference `6,416/7,066 giây`,
> RSS P95 `135,5 MB`, không solve lại và raster parity.
> CNC **writer synthetic** N=20, `quantity=2`/hai pose: simplex `12,980/13,714 ms`,
> duplex report/registration `22,498/28,976 ms`, RSS P95 `97,309/98,824 MB`,
> raster/sides/artifact parity; không đại diện full pipeline hoặc ca nặng. Lượt breadth
> `lap=1` đã chạy đủ 23/23 ca Lô 0 synthetic: 14/22 smart không kém, 8/22 kém,
> một fail-closed đúng chủ đích; không phải P50/P95 hay artifact parity. Auto-route có
> quality gate legacy nên raw `152→85` không phải output production tự động.
> PERF-NEST-12 đã loại oversubscription trong probe hai preview SR13: max grant
> `30→15`, deficit layout `50→16`; absolute concurrent wall gần như phẳng và strict
> parity còn một outlier. Production nesting 253 pass; cut-export core 177 pass/2
> warnings (178 cộng SVG), frontend 17 pass + typecheck. Verdict vẫn
> **`SOURCE + AUTO + PROBE · HOLD`**: chưa packaged Tauri/Nuitka, N=20 đủ 23 ca/hai
> tier RAM thấp, contention 3+ preview hoặc fixture+harness CNC duplex nặng thật.
> Báo cáo hiện hành:
> `BAO_CAO_AUDIT_TOC_DO_PREVIEW_VA_THUC_THI_NESTING_TEM_CNC_2026-09-01.md`.

> Bổ sung 2026-08-30 (tái audit Esko/i-cut, read-only) · Audit surface hiện hành gồm
> 127 file, manifest `docs/audit/NESTING_REAUDIT_SOURCE_MANIFEST_2026-08-30.tsv`,
> digest `857c2a3b6913ca90ccc301307523f922b37cee27ce3fd08ba75e927d6c47f91e`;
> HEAD không đủ định danh vì working tree dirty/untracked. Verdict **HOLD**: 3 P1 còn mở
> (quantity global/kế thừa/explicit-zero, CNC duplex→simplex, hard-cap 200 tờ) và 8 P2.
> Đính chính: fixed-point `1e6` chỉ mô tả Boolean kernel; B9 incremental chỉ live ở
> autofill baseline; fixed-work tất định nhưng deadline phụ thuộc số wave hoàn tất. Verify
> snapshot: Rust **347 pass/2 ignored**, backend nesting **384 pass/1 skip**, cut-export
> **41 pass**; frontend rerun cuối trên audit surface đạt **121/121**, file GridPreview
> cô lập **34/34**. Lượt pre-digest từng quan sát 116 pass/3 fail và 31 pass/1 fail;
> B10-6 phù hợp race tiền điều kiện test, hai ca còn lại không tái hiện — không nâng thành
> production finding. Raw test log chưa content-addressed; chưa benchmark binary vì native
> identity chưa khớp source; không sửa production code. Báo cáo hiện hành:
> `BAO_CAO_AUDIT_LAI_NESTING_PRYNX_2026-08-30.md`.

> **[LỊCH SỬ · SUPERSEDED BỞI NOTE TRÊN]** Bổ sung 2026-08-30 (đối chiếu Esko i-cut, read-only) · Audit lại toàn luồng
> `W2-U09/W7-U11` trên working tree hiện tại xác nhận lõi NFP/IFP/BLF, final validator,
> fixed-point, parallel cold-miss NFP và portfolio song song đều đã có. Release vẫn **HOLD**
> vì 3 P1 xuyên tầng: CNC duplex rơi về simplex; quantity global/kế thừa bị rơi; hard-cap
> 200 tờ. Có thêm 6 P2; tác động vật lý của PDF→cutter bỏ registration marks còn `PROOF GAP`.
> Verify: Rust **347 pass/2 ignored**, backend nesting **192 pass**, frontend **104 pass**,
> cut-export **22 pass**.
> Báo cáo: `BAO_CAO_AUDIT_DOI_CHIEU_NESTING_ESKO_ICUT_2026-08-30.md`. Chưa sửa code.

> Bổ sung 2026-08-30 (NF-BUDGET landed) · Preview Tem/CNC `W2-U09/W7-U11`: benchmark máy rảnh
> (dev loop off) cho cold preview autofill file khách **~8,0s** (không phải ~17,8s — số 17,8s cũ
> đo DƯỚI TẢI dev loop nên méo ~2×). Tách pha sạch: search 40% / baseline 34% / pyOverhead 17%.
> **PV-CACHE (cache resolve geometry) đã thử rồi REVERT** vì A/B máy rảnh cho lợi ích ≈0 (geometry
> resolve chỉ rẻ, "4,6s" cũ là ảo do contention). **NF-BUDGET đã triển khai** (backend-only,
> `nup_true_shape_nesting.py`): autofill dùng `AUTOFILL_TIME_BUDGET_MS=1` vì đo 5 ca (simple/L/
> poly32/mixed5 + file khách) chứng minh search KHÔNG thắng baseline ở autofill cardinal (kể cả
> khi 4 trial hoàn tất). Kết quả đo sau sửa: cold **8,0s → 5,0s** (−38%), search `3.199→214ms`,
> **placedCount 46 không đổi** cả 8 run; quantity giữ 3000ms (search thiết yếu — corpus ANGLE_ONLY);
> env `PRYNX_NEST_AUTOFILL_SEARCH_MS` bật lại được. Verify nesting suite **397 passed/1 skipped**
> (đã sửa `test_mixed_nesting_lifecycle` deadline: nhận `work_budget_exhausted` — terminal hợp lệ
> sau khi portfolio song song làm engine nhanh hơn, KHÔNG do NF-BUDGET). Tier/HOLD & cờ release
> `false` chưa đổi. Xem `BAO_CAO_BENCHMARK_PREVIEW_PHASES_2026-08-30.md`,
> `BAO_CAO_NF_BUDGET_AUTOFILL_SEARCH_2026-08-30.md`.

> Bổ sung 2026-08-30 (NF-3 landed — đính chính note re-audit ngay dưới) · True-shape nesting
> Tem/CNC `W2-U09/W7-U11`: portfolio trial **nay chạy SONG SONG theo wave**
> (`multi_start.rs::allocate_trials` + scoped threads; mỗi trial một NFP cache + evaluation atomic
> riêng, reduce theo `trial_id` giữ tất định). Cả hai đường runtime đã truyền `worker_grant` xuống
> native (`mixed_nesting_jobs._solve_with_hardware_plan`,
> `nesting_production_orchestrator._solve_production_with_hardware_plan`). Native `capabilities()`
> và progress `runtimeControl` công bố `portfolioParallelEnabled / nfpColdMissParallelEnabled /
> nfpCacheByteBudgetEnforced = true`; sidecar Python nay **parse ba cờ này vào `EngineCapabilities`**
> và đã bỏ mọi docstring/comment cũ còn nói "portfolio tuần tự / chưa bật / tương lai". RAM
> reservation giữ nguyên cận bảo thủ `worker_grant × per_worker` (đã gồm trọn một NFP cache mỗi
> worker) — là cận TRÊN của đỉnh song song vì số trial đồng thời luôn `≤ worker_grant`. Giá trị
> `portfolioParallelEnabled: false` còn thấy trong `backend/.audit-tmp/*/metrics-*.json` là số
> **lịch sử** trước khi bật cờ, không phản ánh binary hiện tại. Câu "Portfolio chưa được bật" ở note
> ngay dưới **không còn đúng**. Tier `AUTO + ARTIFACT-PARTIAL · HOLD` và cờ release `false` chưa
> đổi: KPI tốc độ (cold P95 ≤5 s, speedup ≥3×/≥6×) vẫn phải đo lại có kiểm soát. Verify: 347
> backend test đạt, 2 benchmark thủ công skip theo thiết kế.

> Bổ sung 2026-08-30 (re-audit song song/preview) · Dòng `3g` được kiểm lại bằng log ứng dụng,
> regression cô lập và artifact đúng `test/test nesting.pdf`. Ca lịch sử preview 45 → export 44
> là lỗi parity thật. Các phiên app về sau và fresh-store regression cùng 44 tem/
> `solvedAgain=false`, nhưng persisted-store current-source gặp `ManifestConflictError` rồi export
> solve lại; parity vì vậy vẫn OPEN. Current-source lề 3 mm cho 44×0°, residual tâm Y
> `0,075150 mm`; PDF CUT raster có free top/bottom cùng
> `5,115 mm`. Hình cũ 43×0° + 1×90° dùng lề 5 mm và native `97b3f0e4…fec4`, không thể so pose
> trực tiếp với native re-audit `42266070…56b`; log app chưa cho biết runtime user dùng binary nào.
> Cold preview current-source quan sát `15,519–16,640 s`; mỗi solve
> chỉ dùng một compute thread, `plan.workers` không tới native, `multiStartRestarts` là no-op.
> Portfolio chưa được bật vì RAM estimator coi cache per-trial là shared và work budget chưa được
> chia tất định. Xem `BAO_CAO_RE_AUDIT_SONG_SONG_VA_PREVIEW_NESTING_2026-08-30.md`; tier
> `AUTO + ARTIFACT-PARTIAL · HOLD` và cờ release `false` không đổi.

> Bổ sung 2026-08-30 (hiệu năng nesting, lô B6/B7/B8) · **B6 — KHÔNG khả thi, đã đo & revert:** spatial-index để bỏ O(N²) của pha `feasible_region` difference là bất khả thi byte-exact — đo trần bỏ-blocker chính xác chỉ **0,04%** ở 457 tem (`blockersConsidered=115.117`, bỏ được `47`); trần giảm khi N tăng ⇒ O(N²) difference (`31,99s`/`differenceMs/N²` phẳng) là **nội tại**. Golden 4/4 giữ xanh, instrumentation đã gỡ sạch. **B7 — ĐÃ GIAO (chính xác, byte-identical):** tiền lọc bbox trước `judge_pair` trong `run_autofill_baseline` + `run_baseline` (`baseline.rs`), bỏ cặp cách nhau quá gap (chắc chắn `Ok`) ⇒ quyết định clash `any` không đổi. Đo trên `test nesting.pdf`: baseline 457 tem **47,60s → 35,79s (−24,8%)**, phần clash O(N²) `~13,6s → ~1,85s (−86%)`; `differenceMs`/`placedCount` không đổi; golden 4/4 **byte-identical (không bless)**; `cargo test` ~347 xanh. **B8 — SPEC chốt 1 chờ duyệt:** đường nhanh lưới/kệ (đổi layout ⇒ golden mới + cổng utilization + duyệt). Grounding đo: autofill file này là **13 mẫu distinct cân bằng, 100% ở 0°, utilization 62,3%** (ngưỡng THẤP dễ vượt); B8 hứa hẹn vừa nhanh (O(N log N)) vừa **fix luôn xoay**. Spec: `SPEC_NEST_B6_SPATIAL_INDEX_2026-08-30.md` §10 + `SPEC_NEST_B8_GRID_FASTPATH_2026-08-30.md`. **B8 đo NO-GO** cho file khách (skyline-bbox 316 vs NFP 351 con, −10% do hình lõm R=0,71). **B9 — ĐÃ GIAO (miền hợp lệ tăng dần):** cache miền theo (mẫu,góc) + chỉ trừ delta trong `run_autofill_baseline`; `feasible_region_cached` giữ nguyên. difference 457 con **31,8s→1,67s (−94,8%)**, baseline **35,8s→5,06s**, `blockers/N` 252→12,9 (O(N²)→O(N·D)). Đổi layout mức phần-triệu (±1 con ở N lớn do Clipper nhạy gom-batch; net 0, không hồi quy mật độ) — người dùng duyệt giữ, golden thêm ca `autofill_scale_mixed_cardinal`=111 con khoá layout B9 (4 ca cũ 25/13/43/18 không đổi), 5/5 xanh; backend nesting 1013 passed/0 failed. Spec `SPEC_NEST_B9_INCREMENTAL_FEASIBLE_2026-08-30.md`.

> Bổ sung 2026-08-29 (source cuối) · Dòng `3g` bên dưới được tái chốt bằng native identity
> `97b3f0e4…fec4`: artifact đúng `test/test nesting.pdf` có 44 tem, 43×0° + 1×90°,
> `dx/dy=0,032611/0,073127 mm`; preview cold/warm cùng cell, render cùng manifest/fingerprint/
> bundle hash, native revalidation đạt và 4 ốc sạch. `NEST-AUD-07` pin lặp và `NEST-AUD-18`
> cache alias pivot đã đóng; direct-difference/deadline batch đóng một phần `NEST-AUD-10`.
> Cold/warm/render cuối là `23,778/0,101/2,707 s` (giảm 68,2% so với 74,736 s), nhưng cold
> vẫn trượt KPI nên tier `AUTO + ARTIFACT-PARTIAL · HOLD` và cờ release `false` không đổi.
> Verify source cuối: Rust 334/334, backend session/pipeline/ốc 295/295, diff-check đạt.

> Bổ sung 2026-08-29 (sau audit) · Đã đối chiếu audit `W2-U09/W7-U11` với cây mã và **đóng 6 finding**: `NEST-AUD-01` preview đi đúng solver (`totalItems=46` khớp engine, lưới cũ 41), `NEST-AUD-06` khoá phiên phủ đủ năm spec render, `NEST-AUD-03` ốc bế thành `fixed_obstacles` (46→44 con để giữ dấu canh, 0 tem giao vùng ốc), `NEST-AUD-09` một nửa. Ngoài danh sách audit: writer nay vẽ ốc + stamp report, 5 lỗi ánh xạ khoá đã sửa, dấu xén xác định ngoài phạm vi. **Hai điều đáng nhớ**: (1) lô preview làm `NEST-AUD-06` chuyển từ tiềm ẩn thành lỗi thật — preview tạo phiên mà khoá thiếu `pont/report` ⇒ export tái dùng bundle cũ, im lặng; (2) kết luận từ **bbox** trên contour lõm là không dùng được — probe báo "1 tem giao vùng ốc" bằng bbox nhưng đa giác thật cho 0. Rust 325 pass, full backend 4606+19=4625 khớp collect-only, EXITCODE=0. Còn mở: CNC duplex, trần 200 tờ, gate strategy, `NEST-PREVIEW-FINISHING`, pin lặp, parallel, miền tăng dần, portfolio solver. Báo cáo: `BAO_CAO_DOI_CHIEU_AUDIT_NESTING_VA_LO_A_2026-08-29.md`.

> Bổ sung 2026-08-29 · Audit `W2-U09/W7-U11` cho True-shape nesting Tem bế/CNC trên working tree hiện tại, **chưa sửa production code**. Core Rust 315/315 xanh và kiến trúc validator/provenance tốt, nhưng rollout giữ **HOLD**: preview vẫn dùng solver cũ; CNC duplex, boong-as-obstacle và fulfillment >200 tờ chưa kín; production gần một lõi; NFP quét/hợp toàn blocker; generic solver thua heuristic tam giác 152→86 con. Đúng `test/test nesting.pdf` (SHA-256 `efcde4f…57ab`, 13 mẫu), 3 lượt Fast/3.000 ms mất 23,054–25,239 s, p50 23,854 s, 46 placement và `trialsRun=0`; lượt CPU riêng có CPU/Wall 0,923 trên máy 16 logical CPU/32 GB. Báo cáo và roadmap lô ≤5 file: `BAO_CAO_AUDIT_HIEU_NANG_VA_SUC_MANH_NESTING_TEM_CNC_2026-08-29.md`.

> Bổ sung 2026-08-26 · Audit `W6-U04` vòng đời file và `W3-U06` hợp đồng ảnh đã trace startup/single-instance/drop/picker/Recent/remount cùng JPEG/PNG/WebP/BMP/TIFF qua mọi consumer. Xác nhận 6 P1, 3 P2 và 1 P3; regression 55/55 xanh và harness TIFF 300 DPI nay xanh (0,48 × 0,72 pt). Cả 10 finding đã có bản sửa theo Lô A–F, chưa build; xem `BAO_CAO_AUDIT_VONG_DOI_FILE_VA_HOP_DONG_ANH_2026-08-26.md`.

> Bổ sung 2026-08-26 · Hạng mục F2b của Lô F provenance (`W6-U04` §FILE.A4 — chốt chặn Save As lên artifact tạm) đạt `ARTIFACT` trên baseline `b0bfa6aa6e3b2fe6aabb31871aa0a831ec951604` nhánh `codex/pre-release-audit-2026-08-04`. Đã sửa: quyết định bước ghi tách khỏi component thành hàm thuần `planWorkspaceSaveWrite` + executor `executeWorkspaceSaveWrite` không React trong `desktop/src/lib/workspaceFileSave.ts`, nhánh từ chối là **giá trị plan** chứ không phải exception mang chuỗi nên không đi qua `catch` lỗi phạm vi ghi; so path chuẩn hoá (`workspacePathCompareKey` / `isSameWorkspacePath`) phủ hoa/thường, `/` và `\`, phân cách trùng, `.`, `..` và giữ tiền tố UNC; tầng native `desktop/src-tauri/src/lib.rs` giữ `validate_disk_copy_request` fail-closed khi nguồn trùng đích, oracle nâng từ so chuỗi canonical lên định danh file thật của Windows (`windows_file_identity` → volume serial + file index) với fallback `disk_compare_key` khi không kết luận được, không thêm dependency Cargo; call site `handleSaveFile` của `ImpositionTab.tsx` đã nối plan/executor và giữ nguyên nhánh mở lại hộp thoại cho lỗi phạm vi ghi thật; khoá i18n `tabs.imposition:khong_the_luu_de_len_file_lam_viec_tam` có ở cả vi và en. Bằng chứng: TS phạm vi spec 6 file/64 test xanh (`workspaceFileSave.test.ts` 33, bốn file property test 24 phủ Property 1/2/3/4/5/6/8, integration mount `ImpositionTab` thật 7); full suite 287 file/2.849 pass/2 skip/0 fail; Rust `disk_copy_request_tests` 7/7 gồm hardlink, junction, fail-closed và artifact nguyên vẹn, toàn crate 165 test với 160 pass/5 ignored, `cargo check --lib` không warning mới; typecheck, lint 0 error và lint:budget đều xanh; đỏ-trước đã chứng minh bằng mutation tạm rồi hoàn nguyên ở cả bốn tầng plan, executor, oracle native và call site. **Chưa `RUNTIME`**: chuỗi 7 bước trên `run_dev.bat` chưa chạy — mở PDF khách, chạy một công cụ sinh kết quả, Save As trỏ đúng `uploads\<uuid>.pdf`, kỳ vọng từ chối và không mở lại hộp thoại, kiểm file nguyên vẹn không `.tmp`, rồi lưu ra thư mục khách. Giới hạn còn lại: ổ mạng map so với đường UNC (Requirement 3.4) chưa dựng được fixture cục bộ nên chưa có bằng chứng; thời điểm và điều kiện xoá thật của vòng dọn artifact backend (Requirement 2.4) chưa đo, chuỗi "mất vé thuê → owner release → backend xoá" là đọc code (`artifactLease.ts`, `ImpositionTab.tsx:1350/1394`) nên bất biến chỉ được kiểm ở tầng frontend; symlink chưa có test riêng vì cần đặc quyền, junction đã phủ họ reparse point; băng lỗi của tab không tự tắt sau lượt lưu thành công là finding UIUX sẵn có, không do lô này sinh ra và chưa xử lý. Tra lại: spec `.kiro/specs/save-as-artifact-guard/`, báo cáo gốc `BAO_CAO_AUDIT_VONG_DOI_FILE_VA_HOP_DONG_ANH_2026-08-26.md` §FILE.A4.

> Bổ sung 2026-08-26 · Kết luận mục "Nghi vấn cần fixture/oracle" của `W6-U04`/`W3-U06`, 5 nghi vấn. **§FILE.S1 và §FILE.S2 đã đóng**: hook `useTauriCloseRequested` (deps `[enabled]` là boolean ổn định nên đổi ngôn ngữ không còn tạo khe hở giữa unregister và register lại) và `useAuthDeepLinkListener` (DOM listener gắn đồng bộ trước khi gọi plugin, `disposed` guard cho Promise đến muộn, dedupe theo URL); `LoginScreen` thêm `mountedRef` chặn setState sau unmount. Khoá bởi `useTauriLifecycleListeners.test.tsx` 7/7, gồm ca listener đến muộn sau unmount và ca callback đổi identity không đăng ký lại. Mức `AUTO` ở tầng hook, wiring vào `App`/`LoginScreen` vẫn `TRACED`; Alt+F4 và deep-link runtime chưa chạy. · **§IMG.S1 còn mở, đã xác nhận**: `readEmbeddedIccProfile` chỉ có parser PNG `iCCP` và JPEG APP2; TIFF tag 34675, WebP chunk `ICCP` và BMP profile không có parser. Tầng Rust `normalize_image_bytes` dùng `load_from_memory` rồi ghi PNG nên không mang ICC, khiến nhánh đọc ICC từ output đã normalize luôn trả `null` — `attachIccProfile` ở nhánh fallback là code chết cho mọi ảnh đi qua decoder native. Ảnh CMYK TIFF của khách vào PDF thành RGB không quản lý màu, im lặng. Cần fixture TIFF CMYK và TIFF RGB có ICC thật, WebP có `ICCP`, cộng color oracle đo ΔE sau reopen. · **§IMG.S2 còn mở, đã xác nhận**: `readImageDpi` chỉ ba nhánh JFIF APP0, PNG `pHYs`, TIFF IFD; comment trong code ghi thẳng bỏ qua EXIF. JPEG chỉ mang DPI ở APP1 EXIF (phổ biến với ảnh scan và Photoshop) nên trả `null` rồi mặc định 72 DPI, sai MediaBox đúng tỉ lệ `dpi/72` — cùng lớp lỗi §IMG.B1 nhưng khác container. Orientation không đọc ở TS và không áp ở Rust nên ảnh `Orientation=6` vào PDF bị quay 90° và khổ trang lấy chiều chưa xoay. Cần fixture JPEG chỉ có APP1 EXIF DPI và JPEG Orientation 1/3/6/8. · **§IMG.S3 vẫn `[SUSPECTED]`**: bốn ca `chuẩn hóa webp|bmp|tif|tiff qua decoder native` truyền source giả ba byte và mock `invoke` trả PNG đục, chỉ assert tên file, số trang và việc `invoke` được gọi; ca duy nhất assert `/SMask` dùng nguồn PNG nên đi `embedPng` trực tiếp, không qua decoder native. Cần fixture WebP lossless có alpha và TIFF RGBA thật chạy qua command Rust, parse artifact kiểm `/SMask` và pixel biên alpha. · **Bullet "browser fallback chưa chứng minh WebP/BMP/TIFF" đã đóng**: `normalizeImageToPngBytes` ném lỗi khi không có `__TAURI_INTERNALS__`, khoá bằng hai ca có assert `embedPng` không được gọi. Mức `AUTO`.


<!-- W7-U10 Lô 0A: core 30/30 + WebView2 60/60 đã đạt; Viewer chính PPE-only sạch. Thumbnail smoke đạt nhưng vẫn dùng PDFium, Phase 7 cutover còn mở. -->

> Cập nhật: 2026-08-15 · `W6-U02` Ghi/Lưu/Phát Recipe đã hoàn tất Lô R1 trên baseline `f1e18ffd4f50`: `§PLAY.1/.2/.6/.7` đạt `AUTO`, sửa chuỗi working path/bytes, Hủy và commit tuần tự; còn 2 P0 (`§PLAY.3/.4`) cùng integration/artifact/runtime. Không build/installer. `W7-U10` PPE Native Viewer Lô 0A đạt **BENCH + RUNTIME-PARTIAL** cho Viewer chính: core 30/30, WebView2 30 cặp cold/warm, pixel gate và PPE-only trace sạch; peak toàn cây đã đo riêng. Thumbnail smoke dựng đúng nhưng vẫn gọi PDFium nên Phase 7 cutover còn mở. `W3-U03` Logo re-audit rồi sửa cùng ngày: production path đã là PrynX core (không còn VTracer); §LR3.01–§LR3.13 vẫn đóng ở M2; §LR4.01–§LR4.07 đã sửa theo lô A–E (`LOGO_REBUILD_FIXES_2026-08-13.md`), HOLD/NO-GO giữ tới khi có runtime Tauri + JPEG holdout. `W7-U04` hiện chỉ đạt `RUNTIME-PARTIAL` trên Tauri dev: cold/warm đã có pixel compositor thật, còn lăn/zoom/pan/xoay phải chạy lại bằng harness mới. `W7-U05` giữ `AUTO + ARTIFACT`; smoke `42/42` cũ đã bị hạ vì gate Background và semantic Show không hợp lệ. Lô 7 provenance/corpus gate đã đạt `AUTO`, nhưng chưa có log shadow thật nên default vẫn là `current`. Build/installed smoke, `§RENDER.11` và Lô 8 vẫn mở theo chỉ đạo hoãn build. `W8-U02` đã dọn xong source/runtime Ghostscript và còn chờ installer chứa source hiện tại để nâng bằng chứng artifact. `W2-U06 §MSHEET.1` đã sửa: CNC Dàn nhiều mẫu bảo toàn mọi tờ preview/artifact và fail-closed nếu thiếu mẫu. `W2-U07 §OPENAPP.1–2` đã sửa và đạt `AUTO`: path Corel/Illustrator thủ công thắng fallback tự dò; còn chờ runtime app thật. `W7-U07 §PRINTRANGE.1–4` đã sửa và đạt `AUTO`: nhận `27-28,30-33`, giữ current/selection đúng tab và truyền explicit list tới Rust/GDI; còn runtime artifact sau native rebuild.

> Bổ sung 2026-08-17 · Delta re-audit `W6-U02` sau S1: các P0 cũ đã được chặn, nhưng toàn luồng vẫn `TRACED`; còn P1 ở callback báo thành công giả, AI-sheet không vé, lifecycle object-edit, Merge nhiều file và recipe Sticker legacy. Bằng chứng tự động hiện tại: 161 test Recipe/i18n + 14 test Sticker và typecheck; chưa có PDF artifact hoặc Tauri runtime.

> Bổ sung 2026-08-20 · `W3-U05` audit engine màu RGB→CMYK đạt `ARTIFACT + RUNTIME-PARTIAL`, nhưng production **HOLD**: xác nhận writer ảnh CMYK/Gray ghi stream Flate hỏng, endpoint chuyên dụng trả HTTP 500 và Native Combine gắn Adobe RGB cho PNG sRGB; thêm các P1 về residual RGB/CalRGB/Lab, ICC nguồn, OutputIntent, black policy và độ tin cậy PPE. Báo cáo dừng ở chốt duyệt, chưa sửa source.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô A+B của `W3-U05`: sửa response `log`, Flate writer CMYK/Gray và hậu kiểm fail-closed cho residual RGB/JPX/inline/CalRGB/Lab; route/ActionEngine dọn artifact partial. Targeted backend + artifact tests xanh; production vẫn **HOLD** vì §COLOR.03 và các P1 profile/intent/OutputIntent/black-policy chưa xử lý. Nhật ký: `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô C–E của `W3-U05`: Native Combine dùng sRGB thật; Convert Colors dùng ICC nguồn nhúng, truyền intent/preserve-black, giữ black K-only và gắn/hậu kiểm OutputIntent đúng profile đích. HTTP smoke cô lập đạt, 146 test backend liên quan + 16 test Rust Combine đạt. Luồng Convert Colors trực tiếp được nâng lên `ARTIFACT + AUTO + RUNTIME`; toàn họ màu vẫn **HOLD** vì PPE CalRGB/SMask Matte và profile `auto` chưa theo điều kiện in.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô F1/F2 của `W3-U05`: PPE không còn báo clean cho CalRGB/CalGray chưa áp calibration; `/SMask /Matte` được khử preblend đúng trước ICC; writer fail-closed cho color-key/Matte RGB chưa thể rewrite. Full print_engine `371 pass/2 ignored`, backend màu `147 pass`; còn CalRGB full matrix và profile auto theo điều kiện in.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô G của `W3-U05`: Convert Colors và Output Preview dùng chung profile/intent theo tab; UI gửi profile thật, công bố Relative+BPC, còn API chặn conversion/intent/profile sai và profile tường minh chưa cài. Smoke HTTP `18323` + download/reopen/postflight/OutputIntent/render đạt; `104` backend + `13` frontend test và typecheck xanh. `auto` chỉ còn alias tương thích cũ; toàn họ vẫn HOLD ở CalRGB full calibration, corpus acceptance và bản in/màn hình chuẩn.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô H của `W3-U05`: nhúng corpus 17³ sRGB thành Image XObject qua writer thật và thêm PDF vector tám patch; khóa byte BPC, mean/P95/signed ΔL*, Relative tốt hơn Perceptual, TAC mean/P95/max, K-only, postflight và OutputIntent hash/pin profile. Backend màu mở rộng `106 pass`; corpus còn cần AdobeRGB/P3, ảnh ICC/alpha, gradient/transparency, profile giấy khác và file khách reference.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô I của `W3-U05`: AdobeRGB/Display P3 ICCBased qua writer thật được khóa byte CMM + ΔE00/ΔL*/TAC; graphics state sống xuyên `/Contents` và `q/Q`; ICC `/N` phải khớp chữ ký profile; RGB transparency/SMask fail-closed thay vì blend sai. Backend màu `188 pass`; smoke HTTP P3 tải/reopen/OI đạt và alpha trả business failure không artifact. Còn flatten-before-ICC, CalRGB full matrix, profile giấy/file khách và acceptance vật lý.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô J của `W3-U05`: ảnh DeviceRGB+SMask cô lập trên trang trắng được composite trong RGB (kể cả khử `/Matte`) rồi mới ICC; reference qua Form/annotation/pattern và backdrop phức tạp không được flatten nhầm. HTTP smoke `18325` chứng minh isolated alpha success có cảnh báo, nền vector business failure không artifact; ma trận màu trước Lô K `191 pass`.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô K của `W3-U05`: ICCBased RGB hợp lệ (Adobe RGB/Display P3, `/N=3`) trong lane alpha cô lập được đổi sang blend DeviceRGB trước composite, rồi mới CMYK. Oracle wide-gamut khóa byte CMM/OutputIntent/TAC; smoke HTTP `18326` success Adobe alpha + warning, backdrop vector fail-closed không artifact; ma trận màu `194 pass`. Transparency group/ExtGState/backdrop phức tạp vẫn HOLD.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô L của `W3-U05`: `/Decode` tùy biến trên RGB image/SMask không còn bị Pillow bỏ qua rồi writer xóa âm thầm; mapping ngoài `[0 1]` mỗi kênh trả `[UNSUPPORTED_IMAGE_DECODE]` và no-artifact, mapping mặc định tường minh vẫn được phép. HTTP smoke `18327` đạt business failure/no-output; ma trận màu `197 pass`.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô M của `W3-U05`: ICCBased RGB `/Range` chỉ được phép ở domain canonical `[0 1]×3`; range tùy biến/hỏng bị chặn trước writer và trước alpha flatten bằng `[UNSUPPORTED_ICC_RANGE]`/`[INVALID_ICC_RANGE]`, không artifact. Ma trận image/vector/Indexed/alpha và HTTP contract đạt; smoke `18328` xác nhận canonical download sạch/OI đúng, custom business failure/no-output; backend màu `209 pass`.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô N của `W3-U05`: CalRGB phẳng có `WhitePoint/Gamma/Matrix` tự nhất quán được dựng thành ICC matrix-shaper D50 và chuyển qua cùng CMM; image Flate/vector/Indexed khớp oracle Adobe RGB tối đa 1 byte. BlackPoint khác 0, alpha/shading và codec ngoài lane vẫn fail-closed; smoke `18329` download `/DeviceCMYK` sạch với discriminator `(222,0,217,0)`; ma trận màu `249 pass`.
> Bổ sung 2026-08-20 · Đã hoàn tất Lô O của `W3-U05`: mở vector-alpha isolate lane có oracle — đúng một fill DeviceRGB với ExtGState alpha thường được composite trên nền trắng trước ICC; nhiều paint, `q/Q` khôi phục alpha, blend mode, soft-mask, transparency group và backdrop không chứng minh được vẫn fail-closed. Native/HTTP regression + socket smoke `18331` đạt; ma trận liên quan `256 pass, 2 warnings`, positive download `/DeviceCMYK` không còn `/ExtGState`, negative trả business failure/no-artifact. Toàn họ vẫn **HOLD** cho transparency phức tạp, CalRGB BlackPoint/alpha/shading, profile giấy/file khách và proof vật lý.
> Bổ sung 2026-08-20 · Đã hoàn tất Lô P của `W3-U05`: harden compatibility lane Flatten Transparency — phát hiện inline mask và Type3 alpha, từ chối plate PPE sai lưới, mở lại artifact để chặn annotation AP/transparency còn sống và surface blocker qua ActionEngine. Ma trận màu `261 pass, 2 warnings`; socket `18332` đạt success có cảnh báo `MẤT VECTOR` và hai failure no-artifact. Baseline complex transparency cho thấy raster CMYK còn thiếu OutputIntent/drift màu nên chưa nối làm fallback tự động của Convert Colors.
> Bổ sung 2026-08-20 · Đã hoàn tất Lô Q của `W3-U05`: chuẩn PDF/X chỉ nhận `x1a|x4` trước handler/path; compliance X-1a chuyển từ heuristic page-string/10 trang sang parser hậu điều kiện + detector transparency; X-4 surface blocker gốc và sửa nhãn double-X. Ma trận `271 pass, 2 warnings`; socket `18333` đạt CMYK X-4/OI/postflight và complex-alpha/invalid-standard 422 no-artifact.
> Bổ sung 2026-08-20 · Đã hoàn tất Lô R của `W3-U05`: Save PDF/X surface warning backend và chặn compliance/download lỗi; Convert Colors chỉ báo xanh sau download + commit Working File. UI regression `12 pass`, typecheck đạt; copy X-4 khớp engine CMYK hiện hành. Recipe runner vẫn chưa có warning channel riêng.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô S của `W3-U05`/`W6-U02`: prepress Recipe runner giữ `warnings[]`, `log[].message` và `engine`, PlaybackRunner truyền theo từng step + giữ cả nhánh thoát sớm, ImpositionTab hiện toast sau commit. Recipe `116 pass`, typecheck đạt; Tauri production click-smoke vẫn mở.

> Bổ sung 2026-08-20 · Đã hoàn tất Lô T của `W3-U05`: compatibility Flatten Transparency giữ bằng chứng `color_managed` từ PPE, yêu cầu đúng bộ plate C/M/Y/K và byte grid; FOGRA39 OutputIntent chỉ gắn khi mọi trang raster hoặc OI nguồn hợp lệ `/N=4` trùng bytes, rồi mở lại artifact hậu kiểm. Mixed-page không OI được đánh dấu `profile_mixed_unmanaged`, Spot/Separation + transparency trả `[SPOT_FLATTEN_UNSUPPORTED]` và không artifact. Backend màu liên quan `326 passed, 2 warnings`; smoke artifact all/mixed/Spot đạt. Complex transparency vẫn HOLD vì OI không giải quyết drift appearance; Tauri production click-smoke còn mở.

> Bổ sung 2026-08-21 · `W3-U05` đã có click-smoke Tauri dev trên source hiện tại: harness WebView2/CDP đạt `23/23`, ca RGB + SWOP cũ commit đúng một Working File CMYK FOGRA39 (`/N=4`, hash `da2b9b…ce77`, K-only black, không residual), ca alpha + Multiply trả `[LIVE_TRANSPARENCY_RGB]`, `output_filename=null` và không đổi Working File. Bằng chứng bền vững: `audit/evidence/W3_U05_TAURI_CLICK_SMOKE_2026-08-20.json`; raw report: `.tmp/runtime-smoke/color-click-20260820/color-click-report.json`. Đã dọn toàn bộ process/cổng thử nghiệm; packaged-installer production smoke vẫn là audit unit riêng.

> Bổ sung 2026-08-21 · Sau khi người dùng duyệt, Lô A–D3 của W3-U05-R2 đã triển khai: Lab/vibrance, reachable DeviceCMYK/OI, DefaultRGB, preview PPE, UI freshness/ownership, Spot profile parity và trusted-recommendation gate. Artifact rgb.pdf + HTTP execute đạt; claim “giữ sáng/nổi bật nhất” vẫn HOLD vì preview một trang, chưa Tauri exact-file/installer/press.

Tài liệu này là bản đồ độ phủ audit sống của PrynX. Nó trả lời ba câu hỏi: luồng nào đã được truy vết, bằng chứng cao nhất hiện có là gì, và khoảng trống nào cần audit tiếp. Nó **không** chứng nhận “toàn dự án không còn bug”.

## 1. Quy tắc đọc ma trận

- Chấm theo **luồng dọc của người dùng**, không chấm theo số file đã đọc hoặc số test toàn repo.
- Trạng thái của một wave phản ánh mức bằng chứng đủ cho phạm vi wave trên cây mã hiện tại. Một nhánh nhỏ đạt `RUNTIME` không tự nâng cả wave.
- Test/scan cũ không đại diện cho code đã đổi. Khi entry, handler, engine, writer, dependency hoặc pipeline liên quan thay đổi, chuyển luồng sang `STALE` cho tới khi chạy lại.
- Preview, response API hoặc object trung gian không thay thế việc parse/render/đo artifact thật.
- Mọi hit của `scripts/audit_contracts.ps1` bắt đầu ở `[SUSPECTED]`; không tự xếp P0–P3 và không tự sửa.

### Thang bằng chứng

| Trạng thái | Điều kiện tối thiểu |
|---|---|
| `UNKNOWN` | Chưa truy vết đủ đường chạy sống. |
| `TRACED` | Đã chứng minh reachable và nối entry → handler → engine → artifact/consumer bằng `file:dòng`. |
| `AUTO` | `TRACED` + test tự động kiểm đúng bất biến nghiệp vụ/biên dữ liệu. |
| `ARTIFACT` | `AUTO` + đã parse/render/đo file thật do luồng tạo ra. |
| `RUNTIME` | `ARTIFACT` + đã thao tác lại trên app thật ở đúng chế độ dev/cài đặt/release cần kiểm. |
| `STALE` | Bằng chứng trước đó bị mất hiệu lực do code/dependency/pipeline/hợp đồng thay đổi. |

`STALE` là trạng thái phủ định, không phải mức cao hơn `RUNTIME`.

### Vòng đời finding

| Trạng thái | Ý nghĩa |
|---|---|
| `[SUSPECTED]` | Scanner/code smell/giả thuyết; chưa phải bug. |
| `[CONFIRMED]` | Đã chứng minh reachable, consumer, bất biến bị vi phạm và có tái hiện/test/artifact. |
| `[DISPROVED]` | Đã bác bỏ; lưu lý do để không điều tra lặp. |
| `[EXPECTED]` | Hành vi có chủ đích; có nguồn quyết định hoặc test bảo vệ. |

Chỉ `[CONFIRMED]` mới được xếp severity P0–P3 trong báo cáo audit.

## 2. Baseline độ phủ theo 8 wave

| Wave | Trạng thái | Bằng chứng đang có | Khoảng trống còn mở | Audit unit kế tiếp |
|---|---|---|---|---|
| **W1 — Kích thước, đơn vị, PDF page boxes** | `AUTO` | PB1–PB6 đã sửa; Crop 0/90/180/270 + mixed rotation đạt artifact; N-Up `/UserUnit=1/2` cùng 4 placement/raster; backend khóa `/UserUnit=1/100`; Viewer Rust khóa identity cache, parser mismatch, RAM gate và trang 2.001. | Chưa smoke app thật; chưa có một corpus duy nhất chạy qua mọi consumer/page box; Viewer native PB6 mới ở mức tự động. | Runtime smoke Crop + Viewer `/UserUnit=2`; sau đó mở rộng corpus sang W3 utilities. |
| **W2 — Preview ↔ PDF xuất của bình bản** | `AUTO` | PA1 truyền đủ `marginBottom`; PA2 working PDF và Mixed Guillotine fail-closed; preview/working-PDF regression đạt. Nhánh bù xén Alpha đạt `ARTIFACT`. `W2-U02-OC` đã sửa §OC.1–§OC.3. `W2-U06 §MSHEET.1` đã sửa và có artifact CNC một/hai mặt bảo toàn mọi tờ mẫu. `W2-U07 §OPENAPP.1–2` đã sửa, modal + OCG 29/29 test. | Chưa chạy app thật và chưa phủ toàn bộ marks/bleed/report/duplex bằng artifact chung. CNC multi-sheet còn thiếu thao tác runtime UI với file khách có đường bế/boong thật. `W2-U07` còn smoke đúng process/version CorelDRAW/Illustrator và quyết định riêng `§OPENAPP.S1`. | Runtime/downstream smoke CNC nhiều tờ + CorelDRAW/Illustrator trên app; sau đó ma trận W2 marks/bleed/report/duplex còn lại. |
| **W3 — Resize/Crop/Combine/Split/Convert** | `TRACED` | Resize transparency, resize giữ tỷ lệ, Export Image, Combine và Office từng có test/artifact/runtime riêng. `W3-U05` đã trace RGB→CMYK xuyên UI/API/Python/Rust; Lô A–T đã chặn writer hỏng, HTTP/residual/input/PDF-X/UI-download/recipe-warning false-success, ICC nguồn/đích drift, black/state policy, asset sRGB sai, CalRGB/SMask false-clean, blend-after-ICC, `/Decode`, ICC `/Range`, transparency carrier ẩn và provenance PPE/OutputIntent; Convert Colors phẳng + CalRGB matrix lane + lane ảnh alpha cô lập (kể cả ICCBased Adobe/P3 đổi qua blend DeviceRGB) và lane vector alpha đơn giản đạt hậu điều kiện, profile/intent công khai, corpus sRGB/AdobeRGB/Display P3/CalRGB đạt. Generic flatten có artifact postflight, FOGRA39 provenance có guard mixed-page/Spot, nhưng vẫn là compatibility raster mất vector; X-1a compliance dùng parser thật; direct UI/Recipe surface warning và download lỗi. Click-smoke Tauri dev source hiện tại đã đạt positive commit + negative no-commit. | Chưa có bản đồ dọc cho toàn họ công cụ; Crop/Split thiếu ma trận hiện hành. Convert Colors vẫn **HOLD toàn họ** vì CalRGB BlackPoint khác 0, alpha/shading/codec ngoài lane và transparency group/ExtGState/backdrop phức tạp cần oracle flatten; generic raster còn drift appearance dù OI đúng, `auto` legacy chưa suy điều kiện giấy/máy in, ICCBased `/Range` tùy biến hiện fail-closed, corpus chưa phủ profile giấy/file khách và chưa acceptance màn hình hiệu chuẩn/bản in vật lý. | Mở rộng flatten có source-RGB oracle + profile đích + gate ΔL*/ΔE00 cho transparency group/backdrop/gradient (hoặc giữ compatibility lane có UI rõ); hoàn thiện CalRGB BlackPoint/alpha lane; chỉ mở ICCBased `/Range` tùy biến khi có oracle vector/image/Matte; mở rộng profile giấy/file khách; giữ regression Recipe warning; chạy packaged-installer production smoke khi worktree/provenance cho phép; song song giữ `W3-U01` cho Resize/Crop. |
| **W4 — VDP và dữ liệu biến đổi** | `STALE` | Audit 2026-06 từng xác minh page-count/toạ độ/nội dung Numbering; repo có suite VDP và test gate Free/Pro mới. | `vdp.py`, DataMerge/Numbering/CoverNumbering và entitlement đã đổi sau bằng chứng artifact cũ. Chưa chạy lại CSV/XLSX/Google, rotate, multipage, barcode, preview↔output và key thật. | `W4-U01`: Data Merge CSV UTF-8/Unicode + multipage + reopen artifact; sau đó Numbering/Cover Numbering. |
| **W5 — Dieline, nesting, export, 3D** | `AUTO` | 582 test dieline, 30/30 nesting và sidecar/WebView đã đạt trong đợt gần nhất; Flip Top Tuck có golden/property/parity; Double Tray có test pose/wiring. | Chưa nghiệm thu trực quan 2D/3D toàn catalog trên cây mã hiện tại; thiếu pixel golden WebGL, installed-sidecar và in/cắt vật lý. | `W5-U01`: catalog sweep 2D CUT/CREASE/BLEED → export PDF → fold 0–100% → installed sidecar. |
| **W6 — Multi-tab, routing, event, state ownership** | `STALE` | Audit NAV.1–NAV.12 từng đạt full Vitest; audit mở file từng có smoke Tauri thật. `W6-U02` Recipe: Lô R1 giữ working artifact/cancel/commit tuần tự ở `AUTO`; S1 thêm ownership cửa chặn theo tab, chặn Split/Merge không tuyến tính và cảnh báo page state ở `AUTO-PARTIAL`; Lô S giữ warning/engine metadata từ prepress tới toast sau commit. Delta audit 2026-08-17 đạt 161 test Recipe/i18n + 14 test Sticker và thêm 116 test recipe hiện hành. | Recipe vẫn chỉ `TRACED` toàn luồng: cửa chặn resolve như thành công, AI-sheet không có vé, object-edit lệch lifecycle, Merge nhiều file mất cardinality/order, legacy sticker mơ hồ; CRUD Tauri/Undo/playRecipe wrapper còn mở. Chưa có record→save→play PDF artifact hoặc runtime Tauri; recipe warning channel đã đóng ở contract/unit nhưng chưa click-smoke Tauri. Chưa lặp đầy đủ multi-tab/current-tool, background listeners, native drop, recovery, USB/UNC/NAS và đủ định dạng. | Duyệt `W6-U02` Lô A (commit outcome) rồi B (object-edit lifecycle); Lô C cần chốt Merge cardinality và migration recipe sticker cũ; chạy Tauri click-smoke cho warning sau commit. Song song giữ `W6-U01` cho hai tab cùng loại + tab nền + native drop + đóng/reopen/recovery. |
| **W7 — PDFium, worker/process, RAM gate, hiệu năng** | `AUTO` | §PERF.1–9 đã sửa theo Lô 1–11. `W7-U04` có first-page fast path, Render Coordinator, process-isolated display worker, lane theo RAM, cancellation vật lý, cache ownership/atomic, viewport xoay và PPE latest-only. Hotfix `§RENDER.F1` chặn frame PDFium sai màu; `§ZOOM.F2` giữ tile nét theo DPI bucket. Pixel gate mới trên Tauri dev đo cold `4175/4670 ms` và warm `3971/4402 ms` cho first-visible/sharp + hai frame ổn định; đây là một lượt trên phiên dev nhiều process, không phải P50/P95. `W7-U05` giữ `AUTO + ARTIFACT`; smoke `42/42` cũ không còn là acceptance. `W7-U06` đã có cancel cooperative + output atomic cho file-action, benchmark high-tier và low-tier budget động `256–640 MiB`; máy mạnh không bị hard-cap mới. Native capability/pipeline nay khóa revision/dirty/build identity; corpus schema 2 giữ `current` và fail nếu thiếu cặp, MAE/P95/status/số mẫu không đạt. | Lăn/zoom/pan/xoay và Output Preview phải chạy lại bằng pixel gate mới sau khi rebuild native; chưa có log shadow thật đủ 5 cặp/trang; low/medium mới mô phỏng policy trên máy 32 GB, chưa benchmark vật lý `<8 / 8–15 GB` hoặc swap/page fault thật. W7-U01 PDFium cross-file chưa hoàn tất. Chưa build/smoke installer source hiện tại. | Chạy lại runtime current-source và thu corpus shadow sau native rebuild; nghiệm thu policy low-tier trên máy thật; khi người dùng duyệt build thì chạy installed smoke để đóng `§RENDER.11`. |
| **W8 — Security, license, build, release artifact** | `STALE` | Black-box artifact 1.0.0.900 xác nhận loopback/docs-off/header/origin/env gate và sidecar tamper. Current-source đã áp dụng: Save As §SEC.15; A0 §SEC.16; release gate §SEC.17/.20; isolation §SEC.18; clock + generation key §SEC.19/§ATK.09; signature v2 §SEC.21; lifecycle §SEC.22; payload provenance §SEC.23; và §SEC.24-R1–R7. R4 khóa Rust byte-set/provenance; R5 pin GitHub endpoint/destination; R6 khóa Git/GitHub metadata/config/credential/exact grammar, ASCII SemVer/tag route, Known Folder key/Notepad; R7 dùng BAT `%__APPDIR__%`. Snapshot nền backend 144/144, frontend 89/89 + typecheck, Edge 36/36, Rust 227 pass/5 ignored; release-secret đạt 22/22 ngoài sandbox sau availability fix Git 2.53, artifact-runtime 55+1 trong sandbox và ca môi trường đạt ngoài sandbox; no-GS 49/49, native QA 1/1, grammar/AST 3/3; 191/191 là route coverage. OAuth/Credential Manager/GitHub read-only đạt với `gh` 2.93.0. | Chưa có server-verified TPM attestation (§SEC.16), rotate/revoke/audit production (§SEC.17/.20), Authenticode artifact, clean artifact/VM/fault/deploy proof (§SEC.15/.18/.19/.21/.22/.23/.24), Save As UI/NAS/non-NTFS, quyền GitHub push/release, `gh` tương lai hoặc packaged BAT/hostile-`PATH`. Policy `hosts.yml` chỉ lexical cho canonical gh 2.93.0, không phải YAML-general scanner. Checkout hiện tại fail-closed do `extensions.worktreeConfig=true`, `.git/info/exclude` 335 byte và `.git/info/attributes` missing; không normalize trực tiếp vì WIP/quarantine lớn. Artifact lịch sử `NotSigned`; cert code-signing không có; updater key ACL kế thừa cần bridge/rotate. BAT interpreter/file-association và Ring-3 admin/debugger/memory patch là residual accepted risk. | Giữ release `HOLD`; chốt A1 hoặc accepted residual, hoàn tất rotation/audit và certificate/policy vận hành; commit/push source đúng phạm vi, tạo standalone clean clone/normalize Git metadata, rồi clean-build + exact inventory + packaged double-click/installed/fault-injection/uninstall/runtime smoke. Không nâng `RUNTIME`/`Closed` từ source tests hoặc coverage 191/191. |

## 3. Nguồn bằng chứng baseline

| Wave | Tài liệu chính |
|---|---|
| W1 | `docs/BAO_CAO_AUDIT_PAGE_BOX_W1_2026-08-04.md`; `docs/PAGE_BOX_FIXES_2026-08-04.md`; `docs/BAO_CAO_AUDIT_DO_CHINH_XAC_KICH_THUOC_UI_2026-08-04.md`; `docs/DO_CHINH_XAC_KICH_THUOC_FIXES_2026-08-04.md` |
| W2 | `docs/BAO_CAO_AUDIT_NESTING_THONG_MINH_HIEU_NANG_2026-09-05.md`; `docs/BAO_CAO_AUDIT_PREVIEW_ARTIFACT_W2_2026-08-04.md`; `docs/PREVIEW_ARTIFACT_FIXES_2026-08-04.md`; `docs/BAO_CAO_AUDIT_DUONG_CAT_ALPHA_2026-08-04.md`; `docs/BAO_CAO_AUDIT_OC_VA_DAT_TEN_OC_2026-08-05.md`; `docs/OC_VA_DAT_TEN_OC_FIXES_2026-08-05.md`; `docs/BAO_CAO_AUDIT_BAO_TOAN_NHIEU_TO_BINH_TRANG_2026-08-10.md`; `docs/BAO_CAO_AUDIT_DUONG_DAN_COREL_ILLUSTRATOR_2026-08-11.md`; `docs/DUONG_DAN_COREL_ILLUSTRATOR_FIXES_2026-08-11.md`; `audit-rules.md` §6; `docs/NUP_CUT_BORDER_FIXES_2026-08-04.md`; `docs/BINH_CAT_XEN_NHIEU_KICH_THUOC_FIXES_2026-07-30.md`; `docs/DAU_XEN_MIXED_GUILLOTINE_FIXES_2026-08-01.md`; `docs/BAO_CAO_AUDIT_TOC_DO_PREVIEW_VA_THUC_THI_NESTING_TEM_CNC_2026-09-01.md` |
| W3 | `docs/BAO_CAO_AUDIT_ENGINE_MAU_RGB_CMYK_2026-08-20.md`; `docs/ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; `docs/RESIZE_TRANSPARENCY_FIXES_2026-08-03.md`; `docs/RESIZE_GIU_TY_LE_TUNG_TRANG_FIXES_2026-08-01.md`; `docs/XUAT_ANH_FIXES_2026-07-31.md`; `docs/COMBINE_PNG_LOADING_FIXES_2026-08-01.md`; `docs/LUONG_MO_FILE_FIXES_2026-08-02.md` |
| W4 | `docs/audit/AUDIT_REPORT.md`; `docs/audit/PERFORMANCE_REAUDIT_2026-07-23.md`; `docs/BAO_CAO_AUDIT_UIUX_2026-07-27.md`; `docs/FREE_PRO_FIXES_2026-08-04.md` |
| W5 | `audit-rules.md`; `docs/audit/PACKAGING_DIELINE_AUDIT_2026-07-19.md`; `docs/FLIP_TOP_TUCK_FIXES_2026-08-02.md`; `docs/DOUBLE_TRAY_FIXES_2026-07-27.md`; `docs/DO_CHINH_XAC_KICH_THUOC_FIXES_2026-08-04.md` |
| W6 | `docs/BAO_CAO_AUDIT_GHI_VA_PHAT_QUY_TRINH_2026-08-15.md`; `docs/RECIPE_FIXES_2026-08-15.md`; `docs/BAO_CAO_AUDIT_RECIPE_2026-07-27.md`; `docs/DIEU_HUONG_TAB_CONG_CU_FIXES_2026-07-28.md`; `docs/WORKSPACE_CONTEXT_FIXES_2026-07-27.md`; `docs/LUONG_MO_FILE_FIXES_2026-08-02.md` |
| W7 | `docs/BAO_CAO_AUDIT_NESTING_THONG_MINH_HIEU_NANG_2026-09-05.md`; `docs/BAO_CAO_AUDIT_HIEU_NANG_VA_THAN_THIEN_PHAN_CUNG_2026-08-05.md`; `docs/PERF_FIXES_2026-08-05.md`; `docs/PERF_FIXES_2026-07-26.md`; `docs/KIEN_TRUC_FIXES_2026-07-29.md`; `docs/BAO_CAO_AUDIT_TOC_DO_BINH_TRANG_2026-07-29.md`; `docs/BINH_TRANG_PERF_FIXES_2026-07-29.md`; `docs/BAO_CAO_AUDIT_XEM_TRUOC_BAN_IN_PREFLIGHT_2026-08-10.md`; `docs/BAO_CAO_AUDIT_PHAM_VI_ANH_HUONG_PPE_2026-08-10.md`; `docs/BAO_CAO_AUDIT_IN_THEO_PHAM_VI_TRANG_2026-08-11.md`; `docs/IN_THEO_PHAM_VI_TRANG_FIXES_2026-08-11.md`; `docs/BAO_CAO_AUDIT_TOC_DO_PREVIEW_VA_THUC_THI_NESTING_TEM_CNC_2026-09-01.md` |
| W8 | `audit-rules.md` §12/§14.7/§15; `docs/BAO_CAO_AUDIT_CHONG_CRACK_BLACKBOX_WHITEBOX_2026-09-02.md`; `docs/BAO_CAO_AUDIT_SEC19_REAUDIT_2026-09-03.md`; `docs/CHONG_CRACK_FIXES_2026-08-28.md`; `docs/BAO_CAO_AUDIT_CHONG_CRACK_2026-08-28.md`; `docs/BAO_CAO_AUDIT_AUTH_SIGNATURE_CROSS_FEATURE_2026-08-15.md`; `docs/AUTH_SIGNATURE_FIXES_2026-08-15.md`; `docs/BUILD_RELEASE_FIXES_2026-08-03.md`; `docs/BAO_CAO_AUDIT_SAN_SANG_BUILD_RELEASE_2026-08-03.md`; `docs/BAO_CAO_AUDIT_DON_SACH_GHOSTSCRIPT_2026-08-08.md`; `docs/FREE_PRO_FIXES_2026-08-04.md`; `docs/BAO_MAT_FIXES_2026-07-30.md` |

`docs/audit/AUDIT_REPORT.md` là bằng chứng lịch sử từ 2026-06-19. Chỉ dùng để tìm corpus/luồng cũ; không dùng riêng nó để nâng trạng thái hiện tại.

## 4. Hàng đợi audit unit

| Thứ tự | Mã | Luồng cần chốt | Bất biến/artifact phải kiểm | Hiện trạng |
|---:|---|---|---|---|
| 1 | `W1-U01` | Mọi reader/writer đọc PDF page boxes | mm ↔ pt không mất số lẻ; rotate; Media/Crop/Trim/Bleed; reopen cùng kích thước | `AUTO`; các nhánh Crop/N-Up đạt `ARTIFACT`, PB1–PB6 đã sửa, chờ runtime Viewer/app |
| 2 | `W2-U01` | N-Up từ form đến PDF mở lại | preview plan = placement thật; marks/bleed/cut-border đúng page box | `ARTIFACT + AUTO · §CLIPOWN.1 ĐÃ SỬA / §CANONFAIL.1 P1 MỞ`: Lô 1 thay ownership theo bbox block bằng `compute_output_clips()` trên toàn tờ, sweep/index `O(N log N)`; gate `not is_die_cut and not page_sheet_mode` giữ Tem bế/CNC/Page Sheet trên đường legacy. Dung sai `0.01 pt` bắt tiny-overlap làm tròn ở seam — cơ chế của các ca hiếm — nhưng khe dương `0.005 pt` vẫn chia đúng. Regression 2 A5 → A4 khóa content+raster/outer bleed; unit khóa gap, corner và tiny-overlap. Verify hẹp `13/32 passed`; compatibility rộng `89 passed` và CNC/sheet-plan `44 passed` (các lượt có chồng lấp, không cộng); parity brute-force `15.800` cấu hình, synthetic 10k≈`0,34 s`, 40k≈`1,44 s`. Đúng nguồn khách `Adjusted page size 1.pdf` đã chạy public engine ở mixed diagnostic và sequential đúng route: 4 tờ A4, 2 `Do`/tờ; mọi clip sequential gặp seam `420.9455 pt`, raster tờ 2/3 không cross-paint, source hash không đổi. Canonicalization file này thành công nhưng không đóng nhánh fail-open `§CANONFAIL.1`; còn thiếu UI/app và installer runtime. |
| 3 | `W2-U02` | Booklet/Step Repeat/Sticker/CNC | page order, duplex, creep, rotation, marks và report khớp artifact | `TRACED` một phần; nhánh `W2-U02-OC` đạt `ARTIFACT`, §OC.1–§OC.3 đã sửa/verify, chờ runtime; Booklet/Step Repeat chưa phủ |
| 3a | `W2-U03` | Bù xén → đường cắt theo biên trong suốt/ảnh nền trắng | bám Alpha; lùi 0,15 mm; topology/lỗ; node/mm; Hausdorff; path CutContour | `ARTIFACT` cho topology/noodle; §NOODLE.12–15 đã sửa: 111/111 artifact, 154 test liên quan, backend full 2.420 pass/21 skip. Chất lượng chuyển động máy bế được tách sang `W2-U05`, không suy từ tổng node. **Audit 2026-08-16 (`W2-U03-HOPDONG`)** mở nhánh mới về **biên hợp đồng + đường phát lại**, không phải hình học: §BX.F01–F13 và §BX.P01/P05–P12 đã sửa theo Lô A–E (`docs/BU_XEN_TAO_DUONG_CAT_FIXES_2026-08-16.md`), verify `AUTO` (backend full 2.876 pass, typecheck, 429 test frontend phạm vi, lint/budget đúng baseline). Lô F–H đóng thêm §BX.P03 (đo được: 3→1 suất heavy toàn cục bị giữ khi 1 job tem chạy), §BX.P04 (tracemalloc: peak RAM cha 5×chunk→<2,5×chunk), §BX.F16 và §BX.F18; backend full 2.880 pass, 434 test frontend phạm vi. Còn `TRACED`/hoãn có lý do: §BX.P02/P09/P10 (raster lặp + I/O + watermark, phải đo trước), §BX.F14/F17. **Delta audit 2026-08-19 (`W2-U03-EDGE`)** xác nhận §STK.EDGE01–02 và §STK.MEM01–02/OBS01: approved-AI làm rơi nền trắng nên lấy halo hồng (`RGB 255,233,233` thay viền mực `99,42,41`), đồng thời ảnh 2000 px không DPI sinh contour 5294²/float64 213,825 MiB rồi OOM. Lô A/B và log stage trực tiếp của D đã sửa/verify: deterministic chỉ lấy scalar nền, AI giữ hình học mượt; background đi xuyên approved payload, candidate màu bắt buộc coverage ≥90% và density ≥20%, raster approved giữ lưới nguồn 2000². Hồi quy trung gian 11 path/905 cubic đã bị loại; PDF cuối đúng 1 path/91 cubic, median bleed `RGB(85,10,6)`, cache-hit 3,636 s và cold dự kiến 14–15 s. Test cuối 260/260. Lô C admission liên-kind và provider/cache timing rộng hơn còn backlog; xem `docs/BU_XEN_BE_TEM_MAU_VIEN_HIEU_NANG_FIXES_2026-08-19.md`. |
| 3b | `W2-U04` | Ảnh AI nhiều tem → sửa mask → PDF từng tem | preview không đổi màu; pan/edit tách biệt; scale mm; góc lõm; CutContour; tái dùng ảnh đang mở | `ARTIFACT` cho corpus trước; Lô A 2026-08-10 đã đóng §CUTSMOOTH.1, giảm rủi ro §CUTSMOOTH.2 và trả quality metadata thật; semantics slider/UI §CUTSMOOTH.3–7 vẫn chờ Lô B/C. Ca `W2-U04-XEPTEM` ngày 2026-08-15 xác nhận §XEPTEM.1–3 còn mở: UI ép `ai` làm PDF Corel 5 artwork chỉ còn 1 tem/confidence 0,046; cùng engine `auto` nhận 5 và xuất PDF 5 trang/5 CutContour. Chưa có regression lâu dài hoặc runtime Tauri cho ca này. |
| 3c | `W2-U05` | Bù xén → đường chạy CutContour freeform an toàn cho máy bế | reference bất biến; tangent/curvature continuity; minimum command length; normal error; không polyline fallback; fail-safe khi nguồn không đủ | `TRACED`; metric/oracle đã có nhưng follow-up artifact 2026-08-10 chứng minh live `safe-fallback` chưa đi qua final oracle: sao 300 DPI có 3 đoạn <0,25 mm/15 join >1°, khe lõm có 6/17; chưa được nâng `AUTO`. |
| 3c.1 | `W2-U03-SHADOW / W2-U04-SHADOW` | PDF tem AI có bóng màu → nhận diện classic/multi → preview → xác nhận → PDF mở lại | Loại bóng khỏi biên dao đồng thời giữ thân/viền/artwork; guard mất vùng cục bộ; cùng biên ở hai mode | `AUTO + ARTIFACT-PARTIAL · LÔ B CHỜ RUNTIME` (2026-09-06): Lô A người dùng đã xác nhận thử; B thêm loang nền theo gradient, kiểm ổn định/topology/ID, đảo chiều viền sáng, bước nhảy mép gốc và guard artwork. 197 test đạt. PDF gốc được nhận deterministic không AI: mask 273.962 px, ROI 650/650, lá Alpha 255, điểm bóng 0; Alpha và path bằng nhau với cùng settings. Classic/multi × bleed 0/2 mm đều 1 trang/1 CutContour, 45 segment, parse/render đạt; source hash giữ nguyên. Classic 0 giữ ảnh gốc ngoài CutContour, multi xóa Alpha bóng; không claim pixel-identical artwork. Nền phẳng/soft-shadow được phủ, forced-AI/nền mơ hồ giữ fallback; chưa UI/installed/máy vật lý/Lô C. Nhật ký `BU_XEN_SHADOW_FIXES_2026-09-05.md`; baseline audit và evidence cũ giữ nguyên. |
| 3d | `W2-U06` | Mọi mode bình trang khi số mẫu/trang vượt sức chứa một tờ | mọi tờ mẫu khác nhau có placements trong preview và artifact; `runCount` chỉ đại diện bản lặp; không mẫu nào rơi im lặng | `ARTIFACT + AUTO`: `§MSHEET.1` đã sửa; CNC helper/API/artifact một-hai mặt phủ đủ tờ, ca thiếu mẫu fail-closed. N-Up/Sticker/Mixed/cluster/Booklet không có lỗi cùng pattern trong ca đại diện; 116 backend + 8 frontend test và typecheck đạt. |
| 3e | `W2-U07` | Bế → chọn CorelDRAW/Illustrator → mở PDF khuôn | lựa chọn thủ công thắng fallback tự dò; UI = localStorage = payload native; app nhận đúng PDF; hai app độc lập | `AUTO`: `§OPENAPP.1–2` đã sửa; regression phủ Illustrator/CorelDRAW, persistence, fallback và cancel; modal 9/9, modal + OCG 29/29, typecheck pass. Còn runtime Tauri/app thiết kế thật và quyết định `§OPENAPP.S1`. |
| 3f | `W2-U08` | Bình trang S&R: preview số bài/tờ → job backend → PDF mở lại | working PDF/PageBox, khổ tờ, lề, bleed, gap, `splitGap`, solver version và capacity phải cùng fingerprint; artifact đếm placement khớp preview | `ARTIFACT + AUTO-DIAG`: `splitGap=0 → 17/17`, `2 mm → 16/16`; đã thêm `traceId/requestId/jobId` xuyên preview→job→solver→placement→render, log số placement thật và test chống chèn log. `§SRPARITY.1` còn mở phần fingerprint/fail-closed; cần tái hiện app thật để khóa nguyên nhân ảnh user. |
| 3g | `W2-U09 / W7-U11` | Tem bế/CNC `true_shape_nesting`: preview → session → Rust solver → manifest → Front/Back/Cut | preview=export từng pose; duplex; obstacle; fulfillment; floor theo best legacy; p50/p95/RAM/CPU/cancel; hardware grant theo tier | **Delta 2026-09-05: `TRACED + PROBE · HOLD`** — §NEST26.1 P1 CNC marks preview/export làm khác obstacles/identity, handoff miss (chưa artifact collision); §NEST26.2 P2 Balanced core bị ngắt cả 12 trial trên CONSTRAINTS/S20, S20 vùng bao lớn hơn Fast 7,95%; §NEST26.3 P2 corpus ghi planner 15 nhưng runtime grant 1. S&R solve-only N=1: batch 10,088 s/600 placement vs serial 25,799 s/661, chưa quality gate UI, không speedup giữ chất lượng. Native `fbe222dd…572bf`, source dirty `baeec051…`, HEAD báo cáo `669b92db…`; chưa binary parity/full CNC artifact/runtime hiện hành. **Snapshot lịch sử 09-02 — `SOURCE + AUTO + PROBE · HOLD`:** Lô A–F + follow-up semantic/report đã triển khai. PERF-NEST-01/03/04/05/07/10/11/12/13/14 đóng; PERF-NEST-02 `FIXED-PARTIAL`; PERF-NEST-06 có telemetry. Bình trang không reload preview khi chỉ đổi SL dương/metadata; đổi tập trang hoặc hình học vẫn invalidate. Execute dùng report mới qua `reportHash`/artifact fingerprint mà không đổi manifest/layout; batch spill giữ marker fail-closed. Snapshot source/native 09-02 `04f40087…b4d0a0`, N=20 sau hai warm-up ở tier `≥16 GB`: preview cold/warm P50/P95 `9,313/10,758` và `2,265/3,458 giây`; execution `6,416/7,066 giây`, không solve lại/raster parity. CNC writer synthetic authoritative (`quantity=2`, hai pose): simplex `12,980/13,714 ms`, duplex `22,498/28,976 ms`, parity; không phải full pipeline/ca nặng. Breadth Lô 0 đã chạy 23/23 synthetic ở `lap=1`: 14/22 không kém, 8/22 kém, một fail-closed đúng chủ đích; auto-route quality gate bảo vệ floor legacy nên raw `152→85` không phải output production tự động. Percentile N=20 vẫn chỉ phủ 4/23 case. PERF-NEST-12 giữ max grant hai preview SR13 `30→15`, deficit layout `50→16`; absolute wall phẳng và strict parity còn một outlier. Follow-up: backend 258 + 671 pass (suite tách), post-fix 111; frontend 90 pass + typecheck/lint, post-fix GridPreview 40. Còn shared snapshot/cross-job hash, packaged runtime, N=20 đủ 23 ca/hai tier RAM thấp, contention 3+ preview, alias/env + harness CNC duplex nặng thật và tác động vật lý registration. Báo cáo snapshot: `BAO_CAO_AUDIT_TOC_DO_PREVIEW_VA_THUC_THI_NESTING_TEM_CNC_2026-09-01.md`; delta hiện hành: `BAO_CAO_AUDIT_NESTING_THONG_MINH_HIEU_NANG_2026-09-05.md`. |
| 4 | `W3-U01` | Resize + Crop | transparency, content transform, box policy, số lẻ mm, mixed pages | `STALE` |
| 5 | `W3-U02` | Combine + Split + Convert | order, page boxes, metadata, encrypted/error/cancel và reopen | `TRACED` một phần |
| 5a | `W3-U03` | Ảnh logo raster → preview SVG → artifact lưu lại | mode/palette/alpha; compound counter; path/node QC; RAM/cancel; DPI→mm; active-tab/drop; dirty-session; dev/release; VI/EN; SVG thật render lại; Engine v2 structured; khử hạt sau upscale; palette high-res | `AUTO` (rerun 2026-08-13 sau lô sửa: backend logo 66/66, vitest logo+i18n+routing 74/74, typecheck pass) + `ARTIFACT-PARTIAL` (regression engine thật §LR4.01: dấu 3×3 qua `logo_vectorize_structured_rgba`, đo nội dung SVG; smoke core 2026-08-11/12). §LR4.01–§LR4.07 đã sửa theo lô A–E (`LOGO_REBUILD_FIXES_2026-08-13.md`): default despeckle theo upscale, accent grid theo RAM + sàn tuyệt đối, ValueError→422, cancel trong prepare, reservation khi RAM unreadable, mono despeckle 0, copy "Vector hóa Logo". Bằng chứng VTracer-as-production là `STALE`. §LR3.01–§LR3.13 vẫn đóng ở M2. Production HOLD/NO-GO. Còn `RUNTIME` Tauri, JPEG holdout có vector gốc, mở SVG 1:1. Báo cáo `BAO_CAO_AUDIT_LOGO_REBUILD_2026-08-13.md`. |
| 5b | `W3-U04` | Ảnh → Upscale → companion PDF → Viewer/PPE → Bù xén/Tạo đường cắt | pixel ×2/×4; DPI và kích thước vật lý; ICC/alpha; owner theo tab/Undo; capability path; cancel; lease; native/fallback/release parity | `ARTIFACT + AUTO + RUNTIME-PARTIAL`: §UP.X.01/02/06/07/09 và §UP.R.01 đã sửa, test frontend/backend/Rust/build-contract đạt. File khách Balanced ×4 qua route thật đạt `8000×8000 @ 288,0106 DPI`, MediaBox `1999,9264 pt`; PPE 96 DPI `2667×2667`, không degraded/unsound/recovery; claim/release xóa artifact. Còn Tauri UI nhiều tab/picker/native drop/Undo, Poppler, corpus đầy đủ và installed release. Báo cáo `BAO_CAO_RE_AUDIT_UPSCALE_CROSS_TOOL_2026-08-11.md`, log `UPSCALE_CROSS_TOOL_FIXES_2026-08-11.md`. |
| 5c | `W3-U05` | Chuyển RGB→CMYK → tải/mở lại PDF; Preflight/PDF-X/TIFF/Combine dùng cùng hợp đồng màu | ICC nguồn/đích; intent+BPC; black policy; Spot/Gray; mọi stream decode; graphics state; không residual RGB/CalRGB/Lab; OutputIntent khớp numerics; ΔL*/ΔE00/TAC; artifact/runtime; preview freshness/ownership | `ARTIFACT + AUTO + RUNTIME-PARTIAL · HOLD toàn họ`: Lô A–D3 đã sửa Lab/vibrance, reachable DeviceCMYK/OI, DefaultRGB, preview PPE, UI revision/race, Spot profile parity và trusted gate. `rgb.pdf` artifact thật `/DeviceCMYK`/OI FOGRA39/no residual; HTTP execute/download/reopen đạt. Còn aggregate gate đa trang, complex transparency/CalRGB BlackPoint, profile giấy/file khách, packaged installer và proof vật lý. |
| 5e | `W3-U06` | Ảnh native/path-backed → normalize → PDF → mọi consumer | ICC/DPI/alpha; magic bytes; lỗi decoder; capability định dạng; MediaBox và reopen | `AUTO + ARTIFACT-PARTIAL`: sáu finding đã sửa và có test khoá. Harness TIFF **nay xanh** — `đọc TIFF little-endian 300 DPI để giữ đúng khổ vật lý` cho 0,48 × 0,72 pt qua `readTiffDpi` (IFD tag 282/283/296); JPEG quét chuỗi marker nên JFIF sau APP2 vẫn ra 300 DPI; Combine dùng một oracle `isSupportedImageFileName` ở picker và cả ba nhánh phẳng/interleave/nhóm; decoder failure giữ `cause` và không còn trả bytes nguồn cho `embedPng`; Imposition giữ nguyên nhân qua `formatFileOpeningError`; codec chọn theo magic bytes. `ARTIFACT` chỉ đạt ở §IMG.B6: JPEG CMYK đổi đuôi `.png` được save và reparse với `/DCTDecode`, `/ICCBased`, `/N 4`, `/Alternate /DeviceCMYK`, `/Decode`. `imageNormalizer` 29, `combineTransport.integration` 22, `ImpositionTab.fileOpening` 8, `upscaleWorkingPage` 4. Còn mở: ca TIFF đo MediaBox trong RAM với `normalize_image_bytes` bị mock nên chưa có artifact TIFF thật; Rust normalizer vẫn `load_from_memory`→PNG nên không chuyển pHYs/ICC (§IMG.S1/S3 còn mở); EXIF DPI và Orientation vẫn bỏ có chủ đích (§IMG.S2 còn mở); TIFF big-endian, ResolutionUnit=cm, multi-IFD và mật độ TIFF cho Upscale chưa phủ; call site lỗi ảnh trong `ImpositionTab` chỉ `TRACED`; ICC thật và Tauri/installed runtime chưa chạy. |
| 5d | `W3-U01-TRIM` | Trim & Shift: một trang đã bình → tách dọc/ngang 2–3 mảnh → lề trắng riêng → trang mới để bình lại | số mảnh/trục; thứ tự; lề từng mảnh; nền trắng đục; Media/Crop/Trim/Bleed/Art đồng nhất; apply-to; rotate; reopen | `AUTO`: engine + artifact + 33 backend test; typecheck + 2.644 frontend test; runtime Tauri còn chờ thao tác thật |
| 6 | `W4-U01` | Data Merge | schema CSV/XLSX/Google, Unicode, multipage, barcode, preview/output | `STALE` |
| 7 | `W4-U02` | Numbering + Cover Numbering | coordinate/rotate/page selection/font và output text/vector | `STALE` |
| 8 | `W6-U01` | Multi-tab native event | event có tab/session owner; tab nền/đã đóng không nhận file | `STALE` |
| 8a | `W6-U02` | Ghi → lưu → phát Recipe trên working file và nhiều tab | Step đúng thao tác/tab; output bước N là input N+1; path/bytes có ownership; Hủy/Undo không chạy tiếp; CRUD sống qua restart | `TRACED`; Lô R1 đạt `AUTO` cho working artifact/cancel, S1 đạt `AUTO-PARTIAL` cho cửa chặn theo tab và guard mode. Delta 2026-08-17 xác nhận không còn P0 cũ nhưng còn P1 ở success giả, AI-sheet, object-edit lifecycle, Merge cardinality và recipe sticker legacy; store/playRecipe/artifact/runtime vẫn mở. |
| 8b | `W6-U03` | Viewer/Working revision → công cụ kế tiếp → artifact giữ lâu | Tool nhận đúng file + order + page instance + rotation + edit revision + image owner; job chỉ commit khi expected revision còn hiện hành; Undo/Recovery và lease artifact giữ cùng revision | `TRACED + AUTO-PARTIAL`: cả 13 nhóm §REV.01–§REV.13 đã có bản sửa theo lô: Edit barrier, revision-scoped cache/CAS, AI-sheet/ảnh owner/Extract, generic Undo/Recovery, fail-closed Resize/Recipe, lease N-up/VDP/Edit xuyên tab/history/Recipe và fingerprint direct-path trước/sau queue. Verify cuối: frontend 107/107 + 123/123, backend 84/84, typecheck/lint/py_compile đạt. Còn Tauri exact-flow, crash/restart/sleep nhiều tab, artifact chain qua TTL và giới hạn fingerprint metadata. Báo cáo `BAO_CAO_AUDIT_HOP_DONG_REVISION_CONG_CU_2026-08-25.md`, log `HOP_DONG_REVISION_CONG_CU_FIXES_2026-08-25.md`. |
| 8c | `W6-U04` | Startup/single-instance/drop/picker/Recent/remount → dispatcher → tab/dirty/save | at-most-once; batch/intent identity; prefetch best-effort; provenance; listener lifecycle | `AUTO`: cả bốn finding đã sửa và có test khoá bất biến. Startup one-shot ở `SystemFilesInbox.take_startup` + command `take_startup_system_file_batch`; second-instance giữ `batchId` `instance-N`; dispatcher tách lane mặc định theo `batchId` và lane explicit theo action; provenance chuyển sang metadata producer (`isGeneratedWorkspaceFile`, xoá hẳn `isOutputFile`); prefetch đổi số thành §FILE.E1 tạo tab trước và §FILE.E2 grace 250 ms không hủy request. Nghi vấn listener §FILE.S1/S2 cũng đã đóng bằng hook `useTauriCloseRequested` và `useAuthDeepLinkListener` (deps ổn định + guard Promise đến muộn). Rust `system_files_inbox_tests` 2/2; TS `SystemIntegrations` 14, `useIncomingFileDispatcher` 19, `tabNavigation` 21, `useRecentFiles.office` 3, `ImpositionTab.fileOpening` 8, `useTauriLifecycleListeners` 7. Còn mở: đầu producer `SystemIntegrations` chưa có test assert `batchId` phát ra hay ranh giới hai batch khác action; `App.tsx` `isSpawnedDirty`, call site grace ở `ImpositionTab` và wiring hai hook vào `App`/`LoginScreen` chỉ `TRACED`; `read_frame` của render worker vẫn không deadline; nhánh fallback `get_startup_args`, second-instance/root-reload/Alt+F4 runtime và installed release chưa chạy. |
| 8d | `W6-U04-F2b` | Save As trên working file là artifact tạm → chọn đích trùng chính file đó → plan/executor → lệnh copy native | từ chối trước mọi lệnh ghi; không mở lại hộp thoại chọn vị trí; artifact nguyên bytes và không `.tmp` rơi lại; provenance + vé thuê artifact còn nguyên sau khi từ chối; revision chỉ sạch khi ghi thành công; cùng-một-file qua hoa/thường, dấu phân cách, `..`, tên 8.3, junction, hardlink | `ARTIFACT` (không tự nâng `RUNTIME`): plan/executor thuần trong `workspaceFileSave.ts`, oracle native lên định danh file Windows (`windows_file_identity`) có fallback `disk_compare_key`, call site `handleSaveFile` và i18n vi/en đã nối. TS phạm vi spec 6 file/64 test, full suite 287 file/2.849 pass/2 skip/0 fail, Rust `disk_copy_request_tests` 7/7 + toàn crate 160 pass/5 ignored, `cargo check --lib`/typecheck/lint/lint:budget xanh; đỏ-trước bằng mutation tạm rồi hoàn nguyên ở bốn tầng. Còn chuỗi 7 bước `run_dev.bat`, ổ mạng map so với UNC (3.4), thời điểm xoá của vòng dọn artifact backend (2.4) và symlink chưa có test riêng. Baseline `b0bfa6aa6e3b2fe6aabb31871aa0a831ec951604`; spec `.kiro/specs/save-as-artifact-guard/`, báo cáo `BAO_CAO_AUDIT_VONG_DOI_FILE_VA_HOP_DONG_ANH_2026-08-26.md` §FILE.A4. |
| 9 | `W7-U01` | PDFium trong thread | mọi callable reachable có guard đúng chỗ hoặc đi ProcessPool | `TRACED` một phần; scanner 0 hit cùng-file nhưng inventory wrapper/cross-file chưa khép |
| 10 | `W7-U02` | RAM/worker benchmark | máy mạnh không bị hard-cap; máy yếu giảm an toàn; RSS/P95/cancel | `AUTO + BENCH (32 GB)`: N-Up/Sticker/VDP/scheduler/warm-up đạt policy; PPE Lô B đo 5 mẫu cho ba policy, high-tier production peak RSS `851,961 MiB`, P95 slowdown tối đa `10,4%`, session cancel drain P95 `56,027 ms`. Lô B2 áp low budget động `256–640 MiB`; hai lượt production giữ artifact/cleanup, slowdown bảo thủ tối đa `17,1%`. Low/medium chưa có máy vật lý. |
| 10a | `W7-U03` | PDF Viewer CMYK/gradient fidelity | raw renderer = transport; DeviceCMYK/DeviceN/transparency/OutputIntent; parity full-page/tile | `ARTIFACT + RUNTIME-PARTIAL`; detector + runtime identity + PNG pixel-equal + accurate route/định tuyến đã có test; đúng PDF khách đạt PPE→Acrobat MAE 4,6632. Pixel compositor current-source đã hiện ổn định khi mở cold/warm, nhưng chưa lặp Acrobat parity theo timeline/runtime. |
| 10b | `W7-U04` | PrynX Render Engine: first-page, zoom/pan, cancel, process, cache và accurate viewport | first pixel không chờ metadata thừa; trang rủi ro không phát frame display sai màu; zoom-out không bỏ bitmap đã nét; interactive không bị stale/prefetch chặn; rotate/tile parity; soundness; RAM tier; installed artifact | `RUNTIME-PARTIAL (Tauri dev)`: pixel gate hai frame đo cold first-visible/sharp `4175/4670 ms`, warm `3971/4402 ms`, cùng signature `5aee9a8d`, `0` HTTP/console error. Các số lăn/zoom/pan/xoay cũ bị hạ `STALE` vì harness trước chưa kiểm compositor đúng; cần chạy lại sau native rebuild. Provenance + shadow gate đạt `AUTO`, nhưng không có log corpus thật và không promote `hybrid`. Frontend tập trung `148`, backend `182`; `§RENDER.1–10`, `§RENDER.F1`, `§ZOOM.F2` có bản sửa. Chưa P50/P95 sạch, installed smoke hoặc ba tier RAM; `§RENDER.11` còn mở. |
| 10c | `W7-U05` | Preflight “Xem trước bản in” → separations → composite/metadata/parity UI Acrobat | không reload/flash; spot lấy tint alternate; một state Simulation; overprint đúng nghĩa; inventory document/page; transparency/blend; workflow UI có thứ tự rõ | `AUTO + ARTIFACT`: contract profile/intent, 9 Show, 2 Preview, PageBox, ICC subset/solo, Overprint, sampling/TAC, inventory, Paper/Black/Background có regression tương ứng; Show filter hiện đã dùng cùng filtered planes cho sampling/TAC/subset. Smoke `42/42` cũ bị hạ `STALE` vì Background gate sai và chạy trước semantic fix. Full runtime current-source chưa chạy do binding native chưa rebuild; installed/clean-user và Acrobat parity rộng còn mở. |
| 10d | `W7-U06` | PPE core → mọi consumer ngoài Viewer/Output Preview | Export CMYK; flatten; PDF/X-1a có transparency; Outline Fonts + hậu kiểm kẽm; fallback nhận diện khuôn; default isolation; shared DLL; CPU/RAM đồng thời | `ARTIFACT + AUTO + BENCH (32 GB)`: detect-shape, Export, Outline, default isolation và release ABI/provenance gate đã khóa; manifest tương lai sẽ map source revision/build identity/SHA-256 `.pyd`. Flatten/PDF-X mixed-page chỉ raster trang có transparency. File-action nay cancel cooperative, worker dừng trước `CancelledError`, ghi `.pending` rồi hậu kiểm/atomic replace và dọn output/intermediate khi cancel/fail. Lô B/B2 giữ high-tier không hard-cap, low-tier tự co `256–640 MiB`; còn máy low/medium vật lý, runtime UI và installed smoke. |
| 10e | `W7-U07` | Workspace/ô danh sách trang → preview → native print → PDF/GDI | `27-28,30-33` chỉ in 6 trang đúng nội dung; current/selected theo đúng tab và PDF sau bake; preview = payload = artifact; fallback không nới range | `AUTO`: `§PRINTRANGE.1–4` đã sửa; targeted frontend 23/23, typecheck, Rust helper 7/7 và cargo check đạt. Full frontend chỉ có một timeout contention ngoài phạm vi, chạy riêng 1/1 đạt. Còn Tauri runtime, Microsoft Print to PDF artifact/fingerprint và máy in vật lý. |
| 10f | `W7-U08` | Chữ & Font → quét font/chữ → OUTLINE_FONTS → Viewer | identity/cancel; event-loop; rule planner; unique font vs occurrence; progress; output handoff; outline/verify session | `AUTO + BENCH (synthetic)`: Lô A đã offload route, bỏ ProcessPool font-only và chặn stale result. Lô B tách unique font/occurrence, cache report theo identity, tự quét, gộp cảnh báo, ba trạng thái quyết định và progress/Hủy; client disconnect được nối tới cooperative cancel backend. Backend liên quan 74 pass/17 skip + frontend 10 pass, typecheck/lint hẹp đạt. Benchmark font-only 15/50/100 trang `27,73/104,83/188,82 ms` (baseline full `357,19/503,73/770,32 ms`; chỉ so xu hướng). Còn Lô C–D: output handoff, batch/session native và runtime Tauri/PDF khách. Báo cáo `BAO_CAO_AUDIT_CHU_FONT_HIEU_NANG_UIUX_2026-08-13.md`; nhật ký `CHU_FONT_FIXES_2026-08-13.md`. |
| 10g | `W7-U09` | Ctrl+P → hộp In PrynX còn sống → Properties/PrintDlg/GDI → job xong hoặc lỗi có nguyên nhân | hộp không tự mất khi overlay/Properties; preview không nhồi cả PDF vào WebView; PrintDlg/COM cùng process worker; list máy in fail không kẹt dialog; hủy không xóa temp khi worker còn chạy | `TRACED + RUNTIME-PARTIAL`: mở hộp In trên máy dev có log; 6 P1 (`§PRINTWIN.01–06`) chưa sửa. Log `print_debug.log` không có `print_pdf_direct: enter`. `§PRINT.01–08` / `§PRINTRANGE.1–4` vẫn đóng. Báo cáo `BAO_CAO_AUDIT_CUA_SO_PRINT_2026-08-13.md`. Chờ duyệt, chưa sửa. |
| 10h | `W7-U10` | Standee → Viewer PPE-only trang chính → cold-open/warm-zoom → pixel compositor | đúng path/tab; thumbnail đóng; không display/shadow/PDFium; FSP/FCVF/blank-gap; content bounds; partial report; RSS | `BENCH + RUNTIME-PARTIAL (Viewer chính)`: core 30/30; WebView2 60/60, cold FSP/FCVF P50–P95 `1511–1798/1801–2092 ms`, warm `803–844/1171–1204 ms`, warm blank-gap `0`; `900/900` PPE fulfilled, 0 rejected/pending/stale/display/shadow. Peak toàn cây `2889,555 MiB` working set / `2241,938 MiB` private. Thumbnail smoke `302×661`, PDFium `587 ms` đạt nhưng xác nhận Phase 7 cutover vẫn `OPEN`. Không build. Báo cáo `BAO_CAO_AUDIT_PPE_NATIVE_VIEWER_2026-08-13.md`, log `PPE_NATIVE_VIEWER_FIXES_2026-08-13.md`. |
| 11 | `W5-U01` | Dieline catalog end-to-end | CUT kín, CREASE đúng, BLEED, export, fold 0–100%, parity sidecar | `AUTO` |
| 12 | `W8-U01` | Entitlement + installed artifact | catalog/gate/backend cùng nguồn; Free/Pro key thật; clean-user fail-closed | `STALE` |
| 12a | `W8-U02` | Dọn Ghostscript khỏi runtime contract, setup và payload | 0 production caller; không còn `use_gs`; build không tạo thư mục/binary GS; verifier luôn fail nếu GS tái xuất hiện | `AUTO`: source/runtime/policy đã dọn; backend 2.533 ca đạt và 2 test stale được sửa rồi đạt; frontend typecheck + 37 test; Rust 74 integration test. `ARTIFACT` vẫn chờ build từ source hiện tại. |
| 12b | `W8-U03` | Token refresh → ký HMAC → mọi HTTP API; sidecar chết → supervisor → startup proof | key/token/header/binding cùng phiên; signature v2 bind method + raw path/query + credential snapshot + content type + body mode/commitment; JSON/string/binary raw bytes, FormData order/duplicate/file metadata và streaming EOF; session key đổi theo generation; cờ thoát riêng từng thế hệ; không kill listener lạ; retry có giới hạn | `AUTO`: source/test đã đóng body-binding §SEC.21 và replay-after-respawn §ATK.09; backend 144/144, frontend 89/89 + typecheck, Rust 227 pass/5 ignored. Route ratchet 191/191 là coverage inventory, không phải test count. Installed artifact, key thật, packaged respawn/body/fault-injection còn `[EXTERNAL]` nên chưa `RUNTIME`. |
| 12c | `W8-U04` | Vòng đời tiến trình con: đóng app / bấm Cập nhật / app crash → không còn tiến trình ngầm, trình cài ghi được file, app mở lại được | Đóng cửa sổ ⇒ cả `pdf-inspector.exe` và `pdf-inspector-backend.exe` biến mất kể cả khi display worker đang kẹt; đường updater dọn tiến trình con trước khi trình cài chạy; app chết bẩn ⇒ OS tự dọn cây con qua Job Object; không bao giờ tồn tại hai instance đầy đủ cùng lúc; cặp exe/sidecar lệch hash ⇒ thông điệp chỉ đúng cách sửa thay vì buộc gỡ cài; mọi nhánh từ chối khởi động đều để lại breadcrumb; Office/soffice mồ côi được dọn ở lần khởi động sau | `AUTO`: current-source đã bỏ startup `taskkill /IM`; force-kill chỉ còn cho PID generation do app pin bằng HANDLE + creation time, listener lạ làm startup fail-closed. Rust current snapshot 227 pass/5 ignored; artifact harness 32/32 là snapshot mục tiêu 2026-09-03. Chưa `RUNTIME`: cập nhật thật trên VM sạch, đóng app khi render nặng, crash/Job Object, stale listener và kill sidecar giữa job Office — xem `VONG_DOI_TIEN_TRINH_FIXES_2026-08-28.md`. |

Thứ tự trên là thứ tự điều tra, không phải severity. Có thể đổi khi xuất hiện lỗi người dùng mới hoặc finding P0/P1 đã xác nhận.

### 4.1. Delta workflow Bù xén — 2026-09-06

> `STALE` từ2026-09-07: người dùng đã yêu cầu quay về source trước hợp nhất `64129a6`.
> Hai hàng dưới là snapshot lịch sử, không còn là bằng chứng cho checkout hiện tại.
> Code và tài liệu hợp nhất lưu tại checkpoint `codex/backup-unification-2026-09-07`;
> xem `QUAY_LUI_HOP_NHAT_BU_XEN_2026-09-07.md`. Không thay trạng thái các delta ngoài phần4.1.

| Mã | Luồng / hợp đồng | Bằng chứng hiện hành | Khoảng trống |
|---|---|---|---|
| `W2-U03-WORKFLOW` | Tab PDF → tự động/chọn tem → tuning → preview → PDF; metadata layer không tự sửa nguồn, working PDF không mở ngược Viewer, nguồn thật đổi phải loại mask cũ | `AUTO`: 615test frontend/51suite +typecheck, 430test backend/9suite. Đã sửa WORKFLOW.REVISION/TUNING/CONTROLS/ACTION/SYNC/SOURCE/PREVIEW/RECIPE/RECIPE.GEOM. PDF CUT trang đầu đạt `ARTIFACT`: giữ tấm/tách tem/reorder bảo toàn artwork/bleed và ownership CUT. | Chưa thao tác native Tauri/installed; không nâng toàn bộ W2 hoặc các nhánh hình học cũ. |
| `W2-U03-FRAME-ARC` | Tem ellipse chạm frame → dựng lại cung có kiểm chứng → cùng canonical CUT/bleed/fingerprint → PDF mở lại | `ARTIFACT`: bản sao mask của `test bu xen.pdf` có3mép chạm, CUT45→8cubic, không cusp/join hở/đoạn ngắn, source/Alpha/RGBA/labels giữ nguyên; reject frame/shape không đủ bằng chứng. Kèm trong bộ430test cuối. | Chưa Tauri/máy bế vật lý; không tự phục hồi hình bất kỳ từ phần bị xén nặng. |

Báo cáo: `BAO_CAO_AUDIT_WORKFLOW_BU_XEN_2026-09-06.md`,
`BAO_CAO_AUDIT_BU_XEN_CUNG_SAT_MEP_2026-09-06.md`; log `WORKFLOW_BU_XEN_FIXES_2026-09-06.md`.
Đây là bổ sung riêng cho phản hồi hiện hành; giữ nguyên các delta nesting/security và snapshot lịch sử khác.

## 5. Baseline máy quét hợp đồng

Lệnh tái hiện trên cây mã hiện tại:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/audit_contracts.ps1 -SelfTest
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/audit_contracts.ps1 -IncludeUntracked -Format Json -OutputPath audit-contracts-baseline.tmp.json
```

Kết quả ngày 2026-08-04, HEAD `1fb8519`:

| Chỉ số | Giá trị |
|---|---:|
| File tracked/live được liệt kê trong scope | 1.265 |
| File đã quét | 1.157 |
| File loại theo vendor/generated/extension | 108 |
| Tracked file thiếu trên đĩa | 1 |
| Lỗi đọc file | 0 |
| Tổng ứng viên `[SUSPECTED]` | 879 |

| Rule | Số ứng viên |
|---|---:|
| `UNIT_INTEGER_ROUNDING` | 89 |
| `MAGIC_UNIT_CONVERSION` | 95 |
| `SWALLOWED_ERROR` | 516 |
| `STATIC_OR_TEST_SUPPRESSION` | 108 |
| `PDFIUM_THREAD_WITHOUT_GUARD` | 0 |
| `RESOURCE_CAP_WITHOUT_RAM_SIGNAL` | 3 |
| `SCATTERED_FEATURE_GATE` | 15 |
| `RELEASE_ONLY_BRANCH` | 53 |

Giới hạn diễn giải:

- 879 là **ứng viên**, không phải 879 bug. Đặc biệt `SWALLOWED_ERROR` gồm nhiều cleanup/fallback/cancel có thể là chủ đích.
- `PDFIUM_THREAD_WITHOUT_GUARD = 0` chỉ nói scanner không thấy callable **cùng file** vi phạm mẫu hiện hỗ trợ; nó chưa resolve wrapper xuyên file/decorator/handle truyền gián tiếp.
- Số lượng thay đổi khi source hoặc rule thay đổi. Không dùng baseline này làm ngưỡng release nếu chưa triage và duyệt riêng.
- Báo cáo JSON tạm không lưu vào repo; chạy lại cho từng wave và chỉ lưu Markdown khi phục vụ một báo cáo audit cụ thể.

## 6. Mẫu thẻ audit unit

Sao chép khối này vào báo cáo audit của wave:

```text
Mã / tên luồng:
Hành động người dùng → kết quả quan sát được:
Entry:
State/schema/transport:
Handler/route:
Engine TS/Python/Rust:
Writer:
Artifact thật:
Consumer/reopen:
Hợp đồng field/default/enum:
Đơn vị / hệ tọa độ / page box / rounding:
Gate entitlement:
Ma trận ca biên:
Test tự động:
Kiểm artifact:
Kiểm runtime:
Trạng thái + ngày + commit:
Khoảng trống / bước tiếp:
```

Nếu thiếu entry/reachability hoặc artifact/consumer thì không được nâng trạng thái bằng suy đoán.

## 7. Khi nào phải đánh dấu `STALE`

| Vùng thay đổi | Wave tối thiểu cần xem lại |
|---|---|
| `desktop/src/components`, store, `api.ts`, process handler | W1–W6 và wave nghiệp vụ tương ứng |
| `backend/app/api`, schemas, workers, writer | W1–W4, W7 và W8 nếu có gate |
| `native`, `imposition_core`, `print_engine` | W1, W2, W5, W7 |
| `desktop/src/lib/dieline`, `mockup3d`, sidecar bundle | W5 |
| feature catalog, entitlement, auth/license | W4, W6, W8 và mọi luồng bị gate |
| `build_production.ps1`, Tauri/Nuitka/maturin config, release script | W8 |
| Dependency PDF/PDFium/render | Mọi wave tạo/đọc artifact PDF |

Không đánh stale toàn bộ repo theo phản xạ. Chỉ đánh các audit unit có đường trace đi qua vùng đã đổi và ghi lý do.

## 8. Nhịp audit và chốt duyệt

1. Chọn 1–3 audit unit có cùng artifact/hợp đồng.
2. Lập baseline và trace dọc; scanner chỉ bổ sung `[SUSPECTED]`.
3. Xác minh finding, viết `docs/BAO_CAO_AUDIT_<CHUDE>_<YYYY-MM-DD>.md`.
4. **Dừng chờ user duyệt.**
5. Sửa theo lô tối đa 5 file, verify hẹp và artifact/runtime sau mỗi lô.
6. Cập nhật trạng thái, nguồn bằng chứng và ngày/commit trong ma trận này.

Không build/publish GitHub chỉ để nâng W8. Internal build/installed smoke là một audit unit riêng và chỉ chạy khi user duyệt đúng phạm vi.

## 9. Nhật ký cập nhật ma trận

| Ngày | Thay đổi | Bằng chứng |
|---|---|---|
| 2026-09-04 | Cập nhật current-source W8 sau các lô §SEC.15/.19/.21/.23/.24-R1–R7; giữ `STALE · HOLD`. §SEC.24 nay phủ executable/entrypoint, Rust content-addressed exact-set/provenance, canonical GitHub endpoint/destination, Git/GitHub metadata/config/credential/exact grammar + ASCII SemVer/tag route, Known Folder signing key/Notepad và BAT `%__APPDIR__%`. Không tự gán severity hoặc nâng `RUNTIME`/`Closed`. | Nền: backend 144/144, frontend 89/89 + typecheck, Edge 36/36, Rust 227 pass/5 ignored, Save As 23/23. Delta: secret-store 19 pass + 3 `[BLOCKED-ENV]`; artifact-runtime 55 pass + 1 `[BLOCKED-ENV]`; bốn ca môi trường đạt 4/4 ngoài sandbox; no-GS 49/49; native QA 1/1; grammar/wrapper/AST 3/3; parser PS5.1/pwsh 7 bốn script và py_compile hai test file đạt; independent YAML/line-ending/BAT verifier sạch. Checkout hiện tại bị guard chặn đúng thiết kế bởi worktreeConfig/info files. Còn `[EXTERNAL]`: production/key/secret, Authenticode artifact, TPM/no-TPM, clean artifact/VM, OAuth/Credential Manager/GitHub, `gh` tương lai, UI/NAS/non-NTFS và packaged double-click/hostile-`PATH`; BAT interpreter/file-association và Ring-3 là residual accepted risk. |
| 2026-09-03 | **Snapshot lịch sử, superseded bởi dòng 2026-09-04 ở trên.** Re-audit và hardening W8 §SEC.19/.21/.22/.23; giữ `STALE · HOLD`. Khi đó clock/anchor, raw query, sidecar ownership và closed payload exact-set đã Applied/Verified ở source tests; chưa nâng `Closed` hoặc `RUNTIME`. | Snapshot khi đó: backend security 128/128, artifact 32/32, DPAPI/ACL 2/2; frontend 64/64 + typecheck; Rust overlay 191 pass/5 ignored. Body digest và hardlink/Tesseract provenance còn mở **ở snapshot này** nhưng đã có control source/test ngày 2026-09-04; clean build/install/uninstall/fault/deploy proof vẫn mở. |
| 2026-09-02 | Audit read-only W8 chống crack kết hợp black-box bản cài và white-box hai repo; giữ `STALE · HOLD`, chưa sửa production. Xác nhận §SEC.16 P0 authority HWID; §SEC.17 và §SEC.23 release trust; §SEC.18–§SEC.22 còn điều kiện/proof gap. | `BAO_CAO_AUDIT_CHONG_CRACK_BLACKBOX_WHITEBOX_2026-09-02.md`; HEAD `pdfcompare=33143499…`, `printsolutions=d122d552…`; bản cài 1.0.0.900. Black-box: loopback/docs-off/header/origin/env gate và outer sidecar tamper pass; Authenticode `NotSigned`; sidecar shutdown còn artifact gap. Frontend 38 pass, Rust 167 pass/5 ignored, backend 151 pass/1 skip/1 lỗi ACL runner. Báo cáo chốt A0=Mitigated, chỉ A1 có server-verified TPM attestation mới đủ điều kiện Closed; thêm exact payload inventory §SEC.23. |
| 2026-09-02 | Hoàn tất Lô C–E4 hiệu năng preview/thực thi Tem bế/CNC; giữ `SOURCE + AUTO + PROBE · HOLD`. | C: trả job ID trước, prep executor RAM-gated, `preparing_manifest`, cancel-aware publication; 95 pass. D: deadline chung bao baseline/probe/search, cancel ưu tiên, publication/final validator giữ nguyên; Rust 92 pass/2 ignored. E3: quality-gate proof server-owned, `grid_capacity=0` không có ý kiến, S&R theo `design_index`, stale mới probe snapshot một lần; 246 pass. E/§06: telemetry embed/paint/page-build/base-save/report/duplex/merge/publish; 79 pass. E4: unified cut-inspect một descriptor/parse + SHA-256 pre/post, Pro gate, frontend abort/latest-only/no-CUT; backend 158 pass, frontend 8 pass + typecheck/lint. Rộng 422 pass/2 lỗi cũ; `git diff --check` đạt; không cập nhật golden. Còn packaged runtime, N≥20/P50/P95/RSS đa tier, contention/CNC duplex nặng và fingerprint inspect→export. |
| 2026-09-01 | Hoàn tất Lô B hiệu năng source/manifest theo B1/B2a/B2b/B2c; PERF-NEST-02 chuyển `FIXED-PARTIAL`, giữ `HOLD`. | Proof RAM chỉ sống trong transaction; SHA-256 + stable `TemporaryFile` + hậu kiểm tamper giữ nguyên; marker deterministic publish no-replace và resolve O(1); batch `load_many()` full-validate/order; pytest artifact root tạm. Preview hậu B `6,618–9,225 s` (4 lượt N=1), execution `5,890–6,200 s`, load `2,940–3,073 s`, 13 tờ/661 placement, solve lại 0. Rerun dưới root cô lập: lõi 254 pass, lease 12 pass; preview/session/capacity 110 pass và bộ rộng 235 pass/2 lỗi cũ từ lượt trước isolation; py_compile 6 module đạt, không cập nhật golden. 26 snapshot audit cũ còn 0; hai cụm regression 92 snapshot/88 marker đã thu hồi; smoke + rerun isolation không tăng inventory runtime. |
| 2026-09-01 | Kiểm chứng `W2-U01-CLIPOWN` trên đúng PDF nguồn người dùng; không sửa thêm production và không nâng `RUNTIME`. | `Adjusted page size 1.pdf`, SHA-256 `E7D9B7EC5849416CD5F6A77BF936387263F1397D21F0A5D63926B98E0AF2F691`: 8 trang đồng nhất khoảng 145×205 mm, Media/Crop origin khác 0, không Trim/Bleed/Art. Public `run_nup_engine()` ở mixed bleed 3 mm và sequential đúng route bleed 2 mm/gap 0 đều cho 4 tờ A4, 2 `Do`/tờ; clip sequential cả 4 tờ `[(15.5908,420.9455),(420.9455,826.3002)] pt`, raster tờ 2/3 giữ vùng cam `0/23` pixel trắng ở phía ownership nên không cross-paint. Source hash không đổi. Canonicalization thành công; `§CANONFAIL.1` độc lập vẫn P1 mở. |
| 2026-09-01 | Hoàn tất Lô 1 `W2-U01-CLIPOWN`: sửa dải trắng tại seam Bình cắt xén; giữ `§CANONFAIL.1` P1 mở/chưa sửa. | `BINH_CAT_XEN_LOI_TRANG_RANH_GIOI_FIXES_2026-09-01.md`; `compute_output_clips()` giải láng giềng toàn tờ bằng sweep/index `O(N log N)`, gate `not is_die_cut and not page_sheet_mode`; epsilon `0.01 pt` khóa tiny-overlap do làm tròn nhưng khe dương `0.005 pt` vẫn chia đúng. Regression A5 → A4 kiểm content+raster/outer bleed; unit kiểm gap/corner/tiny-overlap. Test hẹp 13/32 pass; compatibility 89 pass và CNC/sheet-plan 44 pass ngoài sandbox (có chồng lấp, không cộng; warning Pydantic cũ). Review parity brute-force 15.800 cấu hình; synthetic 10k≈0,34 s, 40k≈1,44 s; không cập nhật golden. |
| 2026-09-01 | Audit read-only lỗi trắng tại ranh giới Bình cắt xén; xác nhận 2 P1, chưa sửa production. | `BAO_CAO_AUDIT_BINH_CAT_XEN_LOI_TRANG_RANH_GIOI_2026-09-01.md`; artifact 2 trang `154×216 mm` (trim A5 `148×210`, bleed trắng 3 mm) trên A4: clip overlap `17,0078 pt = 6 mm`, raster mất đúng 3 mm trim. Negative control `sequential` sạch. Probe `/Rotate=45` vẫn xuất 935 byte thay vì fail-closed. 28 test hẹp pass/1 warning; ma trận backend liên quan 41 pass, frontend liên quan 99 pass; không cập nhật golden. |
| 2026-09-01 | Triển khai Lô A hiệu năng preview S&R; giữ `HOLD` cho Lô B–E. | Wave scheduler dùng chung + `plan_batch_hardware`, child subscriber/cancel family, durable batch latch cho mọi S&R nhiều mẫu, LRU fence và ordered reference. Rerun cuối exact-file/current native `62,840→25,925 s` (2,424×); sequential/wave A/B sau refactor `35,642→18,148 s`, 13 placed count/pose digest khớp. Targeted 110 pass + review độc lập 168 pass; bộ rộng 235 pass/2 lỗi cũ; không cập nhật golden. Đã xác minh và xóa đúng 26/26 snapshot audit (26.074.958 byte), remaining=0/bytes=0. |
| 2026-09-01 | Audit read-only tốc độ preview và thực thi True-shape nesting Tem bế/CNC; giữ `HOLD`, chưa sửa production. Rút ba P1 stale về quantity/CNC duplex/trần 200 tờ và các claim cũ về progress/cancel/single-core. | `BAO_CAO_AUDIT_TOC_DO_PREVIEW_VA_THUC_THI_NESTING_TEM_CNC_2026-09-01.md`; current native `3298c2f4…`, preview S&R 13 mẫu `62,840 s` = solve `33,349 s` + commit/verify `28,840 s`; wave probe `34,131→13,195 s` với pose digest giữ nguyên; execution từ reference `11,793 s`, không solve lại, load/verify `8,672 s`; xác nhận 4 P1 về preview tuần tự, verify lặp, handoff block event loop và deadline không bao baseline. Benchmark contract 22 pass; regression quantity/CNC duplex/maxSheets 4 pass; bộ rộng 227 pass/3 fail (1 sandbox named-pipe, 1 contract stale one-dao, 1 fixture PDF hỏng). Chờ duyệt Lô A–E, mỗi lô ≤5 file. |
| 2026-08-30 | Tái audit read-only Bình tem bế/CNC theo Esko/i-cut; supersede báo cáo cùng ngày trước đó, giữ `HOLD`, chưa sửa production code. | `BAO_CAO_AUDIT_LAI_NESTING_PRYNX_2026-08-30.md`; audit surface 127 file có manifest/digest; xác nhận 3 P1, 8 P2 và 9 capability gap. Rust 347 pass/2 ignored, backend 384 pass/1 skip, cut-export 41 pass; frontend final rerun 121/121 và GridPreview cô lập 34/34, còn lượt pre-digest 116/3 + 31/1 được giữ như lịch sử không tái hiện. Happy-path handoff đóng; failure-path, raw-log provenance, numeric SLO, artifact/runtime và tác động vật lý cutter còn mở. |
| 2026-08-28 | Audit + sửa `W8-U04` vòng đời tiến trình con (đóng app / cập nhật / crash). Xác nhận 11 finding: tên tiến trình sai và thứ tự diệt ngược trong NSIS hook, đường updater không bao giờ chạy chốt dọn, `shutdown_render_worker` chặn vô hạn, thiếu Job Object, lỗ single-instance, integrity fail-closed không có đường phục hồi, nhánh port 8321 không để lại breadcrumb, và tiến trình Office mồ côi. Đã sửa cả 6 lô. | `cargo check` 0 lỗi mới, `cargo test --lib` 167 pass/0 fail (thêm test `IsProcessInJob` và test escape hộp thoại), `makensis /V2` sạch, `tsc --noEmit` sạch, vitest 296 file/3132 pass/0 fail, pytest 4138 pass/0 fail, `py_compile` sạch. Chưa `RUNTIME`: 4 ca kiểm tay ghi trong `VONG_DOI_TIEN_TRINH_FIXES_2026-08-28.md`. |
| 2026-08-25 | Hoàn tất các lô B–H `W6-U03`: Edit barrier/cache/owner/Undo/Recovery/fail-closed, lease backend+frontend+Recipe và direct-path source fence. Giữ `AUTO-PARTIAL`, chưa tự nâng runtime. | `HOP_DONG_REVISION_CONG_CU_FIXES_2026-08-25.md`; frontend 107/107 + 123/123, backend 84/84, typecheck/ESLint/py_compile đạt; Tauri runtime còn mở. |
| 2026-08-25 | Audit mới `W6-U03` và triển khai Lô A: Working snapshot clone/freeze, edit generation, commit fence hai chốt và CAS riêng cho Recipe playback. Chưa sửa các lô B–H. | `BAO_CAO_AUDIT_HOP_DONG_REVISION_CONG_CU_2026-08-25.md`, `HOP_DONG_REVISION_CONG_CU_FIXES_2026-08-25.md`; typecheck đạt; 5 file/75 test + 4 file/43 test đạt. |
| 2026-08-17 | Delta re-audit `W6-U02` sau thay đổi S1 và parity Sticker; xác nhận cửa chặn nền tảng đúng nhưng callback outcome, object-edit lifecycle, Merge nhiều file và recipe sticker cũ vẫn chưa an toàn; chưa sửa production code. | `BAO_CAO_AUDIT_LAI_GHI_VA_PHAT_QUY_TRINH_2026-08-17.md`; 161 test Recipe/i18n + 14 test Sticker, typecheck đạt; chưa PDF artifact/Tauri runtime. |
| 2026-08-15 | Audit `W2-U04-XEPTEM` trên đúng `Xep Tem.pdf`; xác nhận UI ép AI toàn trang làm rơi 4/5 tem, warning/confidence không hiển thị và test khóa payload sai. Chưa sửa production code. | `BAO_CAO_AUDIT_TACH_TEM_PDF_XEP_TEM_2026-08-15.md`; probe venv: `ai=1`, `auto/vector/simple-bg=5`; artifact auto = ZIP 5 PNG + PDF 5 trang, 1 CutContour/trang; baseline backend 24/24 và frontend 11/11 xanh nhưng thiếu oracle ca này. |
| 2026-08-15 | Sửa `W8-U03 §SIG.01/.02`: signer ràng buộc token hiện tại, tự đăng ký lại cache stale; supervisor phục hồi sidecar bằng cùng secret, cờ thế hệ riêng và startup proof có giới hạn. | `BAO_CAO_AUDIT_AUTH_SIGNATURE_CROSS_FEATURE_2026-08-15.md`; `AUTH_SIGNATURE_FIXES_2026-08-15.md`; frontend 17, backend 91, Rust 2 + 7 debug + 7 release, typecheck/lint/cargo check đạt. Installed runtime còn mở. |
| 2026-08-15 | Hoàn tất `W6-U02` Lô R1: bảo toàn native working path/bytes qua từng Step, chuẩn hóa outcome Hủy/lỗi và chờ commit thật trước khi phát bước kế. | `RECIPE_FIXES_2026-08-15.md`; 128 test mục tiêu + typecheck đạt; `§PLAY.1/.2/.6/.7` lên `AUTO`, còn 2 P0 và chưa có PDF artifact/runtime Tauri. |
| 2026-08-15 | Re-audit `W6-U02` Ghi/Lưu/Phát Recipe; truy vết recorder, storage, playback, cancel và ownership nhiều tab; chưa sửa production code. | `BAO_CAO_AUDIT_GHI_VA_PHAT_QUY_TRINH_2026-08-15.md`; 93 test liên quan + typecheck đạt; toàn luồng chỉ `TRACED`, có 4 P0 và còn thiếu integration/PDF artifact/runtime Tauri. |
| 2026-08-13 | Hoàn tất Lô B `W7-U08`: tách font duy nhất khỏi occurrence, cache report theo identity, tự quét/gộp cảnh báo, ba trạng thái UI, progress/Hủy và nối client disconnect tới cooperative cancel. | `CHU_FONT_FIXES_2026-08-13.md`; backend 74 pass/17 skip; frontend 10 pass; typecheck/lint/diff-check đạt. Runtime Tauri còn mở. |
| 2026-08-04 | Khởi tạo 8 wave, thang bằng chứng, hàng đợi audit unit và baseline scanner. | Tài liệu audit/fixes hiện có + scanner self-test 17 ca + baseline 1.157 file/0 lỗi đọc. |
| 2026-08-04 | Audit W1/W2 trên baseline `1bf8621`; xác nhận PB1, PB2 và PA1; giữ PB3/PA2 ở mức nghi vấn. | Hai báo cáo W1/W2 + harness PageBox/UserUnit + test production `computeSpreadGrid`. |
| 2026-08-04 | Sửa `§W1.PB1`; ánh xạ crop rotate-aware và giữ kích thước theo hướng hiển thị. | `docs/PAGE_BOX_FIXES_2026-08-04.md`; 32 test frontend + 28 test backend + artifact raster bốn góc. |
| 2026-08-04 | Review PB1 xác nhận thêm `§W1.PB4` range/all sai vùng với mixed rotation; giữ PB5 ở mức nghi vấn. | Artifact hai trang 0°/90° có marker bốn màu; phụ lục báo cáo W1. |
| 2026-08-04 | Xác nhận và sửa PB2–PB6, gồm working PDF strict, canonical page space và `/UserUnit` xuyên backend/Viewer native; khép cache cùng path, peak RAM/parser và metadata >2.000 trang. | `PAGE_BOX_FIXES_2026-08-04.md`; frontend full 1.857 test + typecheck + lint budget; backend full 2.238 test; Rust 56 test + cargo check locked. |
| 2026-08-04 | Sửa PA1/PA2; preview Booklet nhận đủ lề dưới và mọi page edit fail-closed nếu không materialize được. | `PREVIEW_ARTIFACT_FIXES_2026-08-04.md`; fault-injection + Mixed page-count guard. |
| 2026-08-04 | Audit artifact đường cắt theo Alpha; xác nhận polyline 146–1.408 node/trang, tolerance 0,02 mm thấp hơn pixel 300 DPI và test thiếu oracle độ mượt. | `BAO_CAO_AUDIT_DUONG_CAT_ALPHA_2026-08-04.md`; parse PDF 72 trang + probe tolerance/Hausdorff/min-gap + 2 test baseline xanh. |
| 2026-08-04 | Sửa §ALPHA.1–3: lọc raster theo mm, fitted Bézier hai chiến lược có guard và fallback fail-safe; giữ §ALPHA.4 chờ smoke/quyết định sản phẩm. | `DUONG_CAT_ALPHA_FIXES_2026-08-04.md`; 89 test; artifact vòng hai 62/72 trang fitted, 72/72 trang Bézier, median 308 → 144,5 segment, box delta 0 pt, 36,52 s tuần tự. |
| 2026-08-05 | Sửa hồi quy hiệu năng Alpha §ALPHA.P1: bỏ hard-cap theo MB/số trang trên máy mạnh, CPU-1 worker, reduced-pool retry và guard Hausdorff bằng buffer hai chiều. | `DUONG_CAT_ALPHA_FIXES_2026-08-04.md`; 112 test; app baseline 86,257 s/1 worker, production auto benchmark 12,005 s/15 worker; CutContour parity 72/72, box delta 0 pt. |
| 2026-08-05 | Audit `W2-U02-OC` từ UI → payload → Sticker/Page Sheet/CNC writer → PDF mở lại; xác nhận §OC.1–§OC.3 và chưa sửa trước khi duyệt. | `BAO_CAO_AUDIT_OC_VA_DAT_TEN_OC_2026-08-05.md`; 78 test hiện có xanh; artifact 3 shape Sticker, separate cut, Unicode, CNC 1/2 mặt và input biên. |
| 2026-08-05 | Sửa §OC.1–§OC.3: PontConfig validation xuyên tầng, CNC giữ Graphtec/layer/group/item, payload CNC bỏ toggle Sticker và khóa Front + Cut. | `OC_VA_DAT_TEN_OC_FIXES_2026-08-05.md`; backend full 2.302 pass/4 skip + target final 20 pass; frontend 1.893 pass/2 skip; typecheck + lint budget + artifact CNC ba shape đạt. |
| 2026-08-05 | Audit lại W7 về startup, worker/RAM, UI/cache, disk và release artifact; xác nhận 6 P1 + 3 P2, chưa sửa trước chốt duyệt. | `BAO_CAO_AUDIT_HIEU_NANG_VA_THAN_THIEN_PHAN_CUNG_2026-08-05.md`; startup log release, artifact rc.3, scanner 1.188 file, backend 37 test, frontend 40 test + production build. |
| 2026-08-06 | Hoàn tất sửa W7 Lô 1–11; đóng §PERF.8 bằng benchmark/parity PPE và §PERF.9 bằng runtime minimize/restore. | `PERF_FIXES_2026-08-05.md`; frontend 1.926 pass/2 skip; print_engine 348 + 218 test; native/Python PPE 117 test; runtime minimize 0 ms CPU/5 giây. |
| 2026-08-07 | Audit đường cắt từ JPEG nền trắng cỡ lớn trên đúng file khách; xác nhận nhiễu >1 mm² tạo MultiPolygon, cấu hình mặc định vô hiệu auto và một component rác làm bỏ qua dựng hình. | `BAO_CAO_AUDIT_DUONG_CAT_ANH_LON_2026-08-07.md`; artifact 805 mm trước/sau, parse 7–12 CutContour, 123 test hiện có xanh nhưng thiếu oracle E2E ảnh thật. |
| 2026-08-07 | Đóng §NOODLE.1–15 sau audit topology mở rộng; fallback theo pixel nguồn, profile mực đậm end-to-end và giữ interior ring khi tắt lấp lỗ. | `DUONG_CAT_ANH_LON_FIXES_2026-08-07.md`; 111/111 artifact, 154 test liên quan, backend full 2.420 pass/21 skip; Poppler toàn hình + zoom 150 DPI. |
| 2026-08-07 | Audit `W2-U05` từ UI → freeform fitter → content stream `/CutContour`; tách “ít node” khỏi continuity/short-command và xác nhận §MOTION.1–6. | `BAO_CAO_AUDIT_DUONG_BE_FREEFORM_MAY_CAT_2026-08-07.md`; file khách auto-circle smooth, force-contour gãy 5,45°; hoa trơn có 16 join >10°; hourglass có 16 đoạn <0,25 mm; 22 test baseline pass nhưng thiếu motion oracle. |
| 2026-08-07 | Hoàn tất `W2-U05` Lô A: thêm metric/oracle line/cubic theo mm, không hard-cap và chưa đổi output engine. | `DUONG_BE_FREEFORM_MAY_CAT_FIXES_2026-08-07.md`; 23 test pass; 12.000 segment đo trong 0,0827 s; trạng thái vẫn `TRACED` tới khi tích hợp production. |
| 2026-08-07 | Audit `W7-U03` trên PDF Illustrator CMYK/DeviceN + transparency không có OutputIntent; tách raw PDFium, JPEG q90 và render color-managed. | `BAO_CAO_AUDIT_HIEN_THI_GRADIENT_VA_MAU_VIEWER_2026-08-07.md`; SHA-256 `95F38C…8184`; PDFium→Acrobat MAE 20,342, FOGRA39→sRGB 4,547, PPE 4,663; chờ duyệt sửa theo lô. |
| 2026-08-07 | Sửa `W7-U03`: detector màu, runtime identity, PNG lossless, accurate PPE/FOGRA39 tự bật theo trang, cache tách mode và cách ly asset sRGB gắn nhãn sai. | `HIENTHI_MAU_VIEWER_FIXES_2026-08-07.md`; Rust 86 pass/1 ignored; backend 121 pass/1 skip; frontend target 30 pass + typecheck; artifact khách MAE 4,6632; chờ smoke Tauri. |
| 2026-08-08 | Thêm `W7-U04` audit Render Engine từ mở file qua PDFium/PPE/cache tới installer; xác nhận 11 finding và bác bỏ nghi vấn print giữ khóa Viewer vì print đã out-of-process. | `BAO_CAO_AUDIT_PRYNX_RENDER_ENGINE_2026-08-08.md`; scanner 1.225 file/0 lỗi đọc; 46 frontend + 17 backend + 86 Rust; hai harness soundness/cancel. |
| 2026-08-08 | Thêm `W8-U02`: phân loại Ghostscript thành caller legacy, hợp đồng/API, build/setup, tripwire và công cụ đối chứng; xác nhận runtime 0 lần gọi GS nhưng source/payload marker chưa sạch. | `BAO_CAO_AUDIT_DON_SACH_GHOSTSCRIPT_2026-08-08.md`; 24 no-GS + 38 routing/policy test pass; 4 test stale bị skip; rc.4 không có binary GS. |
| 2026-08-08 | Hoàn tất sửa `W8-U02`: xóa caller Sticker cuối, config/discovery/telemetry/attic, hợp đồng API legacy, marker/payload và setup; giữ verifier, tripwire, hồ sơ pháp lý và golden dev có chủ đích. | `GHOSTSCRIPT_CLEANUP_FIXES_2026-08-08.md`; production caller scan 0 hit; policy/corpus 62 pass; backend 2.533 pass + 2 stale test đã sửa và rerun 3 pass; frontend typecheck + 37 pass; Rust check + 74 pass. |
| 2026-08-09 | Hoàn tất các lô code `W7-U04` tới PPE viewport/latest-only và hardening ownership/cache/profile; chưa đóng cổng UI/installer. | `PRYNX_RENDER_ENGINE_FIXES_2026-08-08.md`; frontend `94`, backend `91`, `print_engine` `575`, native check + maturin đạt; API artifact PDF khách đúng clip; viewport engine median nhanh hơn `25,8%`. |
| 2026-08-09 | Hotfix `§RENDER.F1` sau feedback ảnh đầu sai gradient/màu: bootstrap nhận diện màu trước mount, risky page accurate-only, unmount ảnh display cũ và khóa regression cold-open/cache-hit. | `PRYNX_RENDER_ENGINE_FIXES_2026-08-08.md`; frontend `98/98`, Rust check đạt; probe display worker trên PDF khách trả `colorRisk` trong 45 ms, 4/4 trang high-risk. |
| 2026-08-09 | Hotfix `§ZOOM.F2` sau feedback chữ mất nét khi giảm zoom: giữ bitmap viewport đã decode, chuẩn hóa identity theo DPI bucket và loại chuyển accurate viewport→full-page tại ngưỡng tiling. | `PRYNX_RENDER_ENGINE_FIXES_2026-08-08.md`; baseline `6,33→6,32` từng tạo full-page gần 28 MP, `8,0→7,9` từng bỏ tile dù cùng 768 DPI; frontend typecheck + `106/106` test đạt. |
| 2026-08-10 | Audit dọc bốn điều khiển CutContour live; xác nhận wiring/parity đúng nhưng §CUTSMOOTH.1–8 còn mở, gồm fallback bỏ qua motion oracle, `detail=0` nguy hiểm, vùng chết fidelity, tension yếu và UI ẩn reference. | `BAO_CAO_AUDIT_DIEU_KHIEN_LAM_MUOT_DUONG_BE_2026-08-10.md`; đúng ảnh 9 tem + 8 shape tại 72/300 DPI; parse 4 PDF thật; 10 test baseline pass nhưng thiếu regression tương ứng. |
| 2026-08-10 | Triển khai Lô A CutContour: final oracle sau mọi path, phân loại góc thật, fail-closed, metadata quality và cấm export tự refit khi thiếu override. | Sao 300 DPI: 10 cubic, min 17,542 mm; notch: 11 cubic, min 4,951 mm; ảnh khách mặc định giữ 915 cubic. Verify 15 + 76 + 129 test pass. |
| 2026-08-10 | Audit `W7-U05` Preflight Output Preview trên đúng PDF khách và hai screenshot PrynX/Acrobat; xác nhận full-page overlay swap, CSS multiply, màu spot MD5, profile FOGRA hard-code, metadata/inventory rơi khỏi contract. | `BAO_CAO_AUDIT_XEM_TRUOC_BAN_IN_PREFLIGHT_2026-08-10.md`; probe PPE 150 DPI; MAE CSS→Acrobat `12,856`, PPE+SWOP→Acrobat `2,485`; 68 backend + 10 frontend test baseline pass. |
| 2026-08-10 | Tái audit `W7-U05` sau khi người dùng xác nhận cold-open không còn reload; đối chiếu từng điều khiển trong ảnh Acrobat/PrynX và probe lại contract hiện tại. | Báo cáo W7-U05 phụ lục parity: SWOP `available=true`; trang 1 có 6 plate, trang 3 mới có `khuon be`; 4/4 trang khai Transparency/DeviceCMYK; PPE response thiếu metadata/coverage; Ink Manager chỉ trả C/M/Y/K. Xác nhận §OP.8–§OP.11 và kế hoạch Lô A–E. |
| 2026-08-10 | Triển khai Lô B `W7-U05`: đồng bộ intent qua Separations/ICC; đưa profile/intent vào WorkspaceContext theo tab; nối Viewer native fast path và accurate backend động; tách cache/generation theo Simulation; giữ bitmap hiện tại khi mở Output Preview chờ PPE. | `XEM_TRUOC_BAN_IN_PREFLIGHT_FIXES_2026-08-10.md`; backend `161 passed`, frontend liên quan `63 passed`, probe PDF khách SWOP + Perceptual trả PPE/3 spot/Transparency/DeviceCMYK; full frontend còn 4 failure ngoài phạm vi. Runtime Tauri còn chờ. |
| 2026-08-10 | Triển khai Lô C `W7-U05`: dùng composite PPE Overprint lossless làm ảnh chính; tách diff thành chẩn đoán; đọc Overprint từ `gs` được dùng thật và Form XObject; đồng bộ Simulation, latest-only và cleanup overlay. | `XEM_TRUOC_BAN_IN_PREFLIGHT_FIXES_2026-08-10.md`; backend tập trung `45 passed`, frontend tập trung `39 passed`, typecheck và diff check đạt; PDF khách trang 1 không dùng Overprint, fixture dương tính spot/Form đạt. Runtime Tauri còn chờ. |
| 2026-08-10 | Triển khai Lô D `W7-U05`: sắp panel theo workflow Acrobat; group Process/Spot; tách hành động sửa file; đăng ký route Ink Manager và thêm shortcut có guard tới Ink Manager/Set Page Boxes. | `XEM_TRUOC_BAN_IN_PREFLIGHT_FIXES_2026-08-10.md`; typecheck đạt, regression rộng 15 file `99 passed`; toàn frontend `2.161 passed, 2 skipped`, 3 failure StickerCutline ngoài phạm vi; diff/whitespace check đạt. §OP.11 đạt `AUTO`; runtime Tauri và inventory Ink Manager trên PDF khách còn chờ. |
| 2026-08-10 | Triển khai Lô E1 `W7-U05`: thay listener hover không có producer bằng store đúng tab; thêm point/average theo mm, DPI artifact và sửa chuẩn hóa tọa độ trang xoay. | Backend ICC/Separations `25 passed`; frontend sampling/Viewer/DOM/i18n `36 passed`; typecheck đạt. Sample Size đạt `AUTO`; runtime Tauri còn chờ. |
| 2026-08-10 | Triển khai Lô E2 `W7-U05`: thêm Warning Opacity theo tab; Gamut/TAC và diff Overprint dùng cùng opacity, trong khi composite Overprint và Soft-Proof luôn giữ 100%. | Typecheck đạt; regression Output Preview/Viewer/store/i18n `12 file / 57 passed`. Warning Opacity đạt `AUTO`; runtime Tauri còn chờ. |
| 2026-08-10 | Triển khai Lô E3 `W7-U05`: đọc PageBox thật theo trang nguồn; vẽ Art/Trim/Bleed đúng CropBox, `/Rotate`, frame và cờ khai báo; giữ Set Page Boxes là hành động riêng. | Probe PDF khách 4 trang đạt; backend PageBox/API `84 passed`; frontend geometry/store/DOM/Viewer/i18n `14 file / 78 passed`; typecheck đạt. Show/Preview filter deferred vì chưa có contract thật; runtime Tauri còn chờ. |
| 2026-08-10 | Sửa `§OP.12` và audit E4 `W7-U05`: thêm page identity riêng cho bitmap mô phỏng; trace Paper Color/Black Ink/Background Color tới PPE/ICC và không dựng toggle khi contract chưa tồn tại. | Typecheck + frontend rộng `14 file / 79 passed`; probe PDF khách Relative↔Absolute MAE `7,4115`, `105.094` pixel đổi, max `25`. E4 `TRACED + ARTIFACT · DEFERRED`; runtime Tauri còn chờ. |
| 2026-08-10 | Triển khai Lô F `W7-U05`: ghép tập kẽm từ plane u8 + LUT spot qua ICC, endpoint PNG nhị phân và frontend latest-only; bỏ consumer CSS multiply ở subset/solo. | PDF khách SWOP 150 DPI: `92,7–103,8 ms`/composite, endpoint `114,4 ms`, nhanh hơn raster Soft-Proof `895,4 ms`; all-on parity MAE `0,214767`, max `5`. `31` lõi mực + `85` backend + `65` frontend đạt; typecheck/cargo check đạt. `§OP.1 = AUTO + ARTIFACT`, Tauri runtime còn chờ. |
| 2026-08-10 | Triển khai Lô G `W7-U05`: chín Show filter tại sink PPE; hai Preview mode; tách Paper Color/Black Ink khỏi intent nội dung; hòa Background Color theo phản xạ giấy/mực; truyền contract qua UI/cache/session/FastAPI/facade/PyO3. | Typecheck đạt; frontend tập trung `15 file / 148 passed`; backend tập trung `9 file / 182 passed`; PPE Rust `7` test Show + `2` test E4 đạt. Native cũ thiếu capability fail-loud. |
| 2026-08-10 | Nghiệm thu cũ `W7-U04/W7-U05` trên Tauri dev được tái phân loại sau re-audit harness. | **`STALE` cho acceptance:** `42/42` dùng Background signature sai thời điểm và nuốt timeout; số cold/warm `1.909/1.944 ms` vừa ghi sai cách đọc đơn vị vừa chụp trang trắng. Chỉ giữ các ảnh/log này làm lịch sử chẩn đoán, không dùng đóng cổng runtime. |
| 2026-08-10 | Sửa finding cleanup phát sinh từ smoke: file `/upload/local` giữ mtime nguồn cũ có thể bị sweep dù DB còn sở hữu. | `backend/app/core/cleanup.py`; `test_storage_pressure_cleanup.py` `8/8`; path đăng ký trong DB được loại trước sweep tuổi/áp lực đĩa. |
| 2026-08-10 | Audit `W7-U06` phạm vi ảnh hưởng PPE ngoài Viewer/Output Preview; xác nhận năm consumer trực tiếp (hai consumer nằm hẳn ngoài họ Preflight), bác bỏ rò default và tách ảnh hưởng thuật toán khỏi coupling DLL/tài nguyên. | `BAO_CAO_AUDIT_PHAM_VI_ANH_HUONG_PPE_2026-08-10.md`; Rust `641 pass/4 ignored`; backend mục tiêu `279 pass`; shared-native `85 pass`; artifact PDF khách Export CMYK/fallback shape; PDF/X-1a transparency `7/7`. Chưa build theo chỉ đạo. |
| 2026-08-10 | Triển khai Lô A `W7-U06`: khóa E2E detect-shape qua PPE, PDF/X-1a transparency thật, default isolation và ABI/capability của wheel staging. | Lô code/test 5 file + lô hồ sơ 2 file; verify hẹp `105 pass`, backend mục tiêu `297 pass`, shared-native `85 pass`; PowerShell parse đạt; native hiện hành được gate nhận, artifact cũ 09/08 bị từ chối. Không build theo chỉ đạo. |
| 2026-08-10 | Nghiệm thu Lô B `W7-U06`: benchmark chạy riêng/chồng Viewer+Export, Output Preview+flatten, Outline+detect và hai đường cancel; 1 warm-up + 5 mẫu trên ba policy RAM, thêm production DPI. | High-tier production P95 slowdown tối đa `10,4%`, peak RSS `851,961 MiB`; không thêm scheduler cap. Session cancel drain P95 `56,027 ms`. Xác nhận `§PPE.SCOPE.8` low budget 384 MiB chặn Export 300 DPI và `§PPE.SCOPE.9` file-action chưa cooperative cancel. Low/medium vẫn chờ máy vật lý; không build. |
| 2026-08-10 | Audit `W2-U06` bảo toàn nhiều tờ qua N-Up, Sticker, Mixed Guillotine, cluster, Page Sheet, Booklet và CNC; xác nhận chỉ CNC Dàn nhiều mẫu còn bỏ tờ/mẫu sau tờ đầu. | `BAO_CAO_AUDIT_BAO_TOAN_NHIEU_TO_BINH_TRANG_2026-08-10.md`; helper + API preview + artifact CNC 5 mẫu đều tái hiện `§MSHEET.1` P0; user đã duyệt lô sửa. |
| 2026-08-10 | Sửa `W2-U06 §MSHEET.1`: layout CNC trả mọi tờ mẫu, preview dùng `sheets[]`, renderer xuất từng Front/[Back]/Cut và fail-closed nếu còn mẫu bắt buộc chưa đặt. | Artifact 5 mẫu một mặt = 10 trang, 3 cặp hai mặt = 9 trang; 116 backend + 8 frontend test, typecheck và diff check đạt. Chưa runtime app với file khách có đường bế/boong thật. |
| 2026-08-10 | Triển khai Lô B2 `W7-U06 §PPE.SCOPE.8`: low-tier lấy 25% RAM khả dụng/heavy slot trong `256–640 MiB`, fallback 384 khi không biết RAM; medium/high không đổi. | Baseline đỏ đúng `4`, sau sửa test tập trung `24/24`, backend liên quan `268/268`; TIFF CMYK 300 DPI `3117×2338` có ICC. Hai benchmark production policy mới giữ checksum/source/cleanup, peak tối đa `849,691 MiB`, slowdown bảo thủ `17,1%`. Máy `<8 GB` vật lý và build vẫn chưa chạy. |
| 2026-08-10 | Re-audit runtime Lô 6 thay DOM-ready bằng pixel compositor gate hai frame và sửa mọi đơn vị về `ms`. | Cold first-visible/sharp `4175/4670 ms`, warm `3971/4402 ms`, cùng pixel signature `5aee9a8d`, `0` bad HTTP/console. Đây là `RUNTIME-PARTIAL` trên phiên dev đang có nhiều process; lăn/zoom/pan/xoay và Output Preview current-source vẫn chờ chạy lại sau native rebuild. |
| 2026-08-10 | Triển khai Lô 7 provenance + corpus rollout gate, giữ default Viewer `current`. | Capability nhúng revision/dirty/timestamp/profile/provenance/identity; build staging đối chiếu wheel và manifest map SHA-256 `.pyd`. Corpus schema 2 yêu cầu 5 cặp/trang @96 DPI, MAE `≤5`, PPE P95 `≤650 ms`, 0 unsupported/error và artifact ổn định. Reporter self-test, PowerShell gate probe, native cargo check đạt; log shadow thật/installed vẫn `OPEN`. |
| 2026-08-11 | Sửa re-audit `W3-U04` theo 5 lô: identity/close-tab, path capability, ICC fallback, cooperative cancel + lease và release behavior smoke. | `UPSCALE_CROSS_TOOL_FIXES_2026-08-11.md`; frontend 37 test, backend 59 test, typecheck/lint/cargo/build-contract đạt; file khách Balanced ×4 và PPE 96 DPI đạt artifact/runtime hẹp. Tauri UI và installed release còn mở. |
| 2026-08-11 | Audit `W2-U07` đường Bế → CorelDRAW/Illustrator; xác nhận lựa chọn `.exe` thủ công được lưu nhưng bị path tự dò lấn quyền ưu tiên ở UI và payload native. | `BAO_CAO_AUDIT_DUONG_DAN_COREL_ILLUSTRATOR_2026-08-11.md`; harness forward-regression đỏ đúng `appPath`, suite modal hiện hữu 4/4 pass; chưa sửa trước chốt duyệt. |
| 2026-08-11 | Sửa `W2-U07 §OPENAPP.1–2`: path thủ công ưu tiên trước fallback tự dò và suite khóa payload cuối cho cả hai app. | `DUONG_DAN_COREL_ILLUSTRATOR_FIXES_2026-08-11.md`; baseline 3 regression đỏ, sau sửa modal 9/9, modal + OCG 29/29, typecheck pass, ESLint 0 error. Runtime app thật còn chờ. |
| 2026-08-11 | Audit `W7-U07` từ trang đang xem/thumbnail chọn → hộp In → preview → TypeScript IPC → worker → Rust/GDI; xác nhận danh sách rời rạc và current/selection bị rơi khỏi contract. | `BAO_CAO_AUDIT_IN_THEO_PHAM_VI_TRANG_2026-08-11.md`; harness tạm 3/3 tái hiện, baseline frontend 10/10 pass; Rust bị khóa tài nguyên dev (`os error 32`); chưa sửa trước chốt duyệt. |
| 2026-08-11 | Sửa `W7-U07 §PRINTRANGE.1–4`: thêm parser danh sách trang, current/selection theo tab và explicit list xuyên TypeScript/Tauri/worker/Rust; validate trước StartDocW. | `IN_THEO_PHAM_VI_TRANG_FIXES_2026-08-11.md`; targeted frontend 23/23, typecheck, ESLint hẹp, Rust helper 7/7 và cargo check target độc lập đạt. Full frontend 1 timeout contention ngoài phạm vi, isolated 1/1 đạt; runtime artifact còn mở. |
| 2026-08-12 | Audit `W2-U08` ca S&R preview 16 nhưng PDF ảnh có 17; trace `splitGap` xuyên UI/API/engine/worker và tạo PDF artifact để đếm placement thật. | `BAO_CAO_AUDIT_PREVIEW_BINH_CAT_XEN_SR_2026-08-12.md`; engine hiện tại khớp `17/17` ở 0 mm và `16/16` ở 2/16 mm; backend 19 pass, frontend 4 pass. Xác nhận `§SRPARITY.1` thiếu fingerprint/fail-closed; chờ duyệt, chưa sửa production. |
| 2026-08-12 | Sửa chẩn đoán `W2-U08 §SRPARITY.1`: thêm mã đối chiếu theo tab/request/job và log hình học + capacity/placement thật xuyên preview→PDF, không đổi solver. | `PREVIEW_BINH_CAT_XEN_SR_FIXES_2026-08-12.md`; backend 22/22, frontend 35/35, typecheck/py_compile/diff-check đạt; runtime harness log `17` ở 0 mm và `16` ở 2 mm. |
| 2026-08-13 | Re-audit `W3-U03` Phục hồi & Vector hóa Logo trên đường PrynX core; không sửa mã. | `BAO_CAO_AUDIT_LOGO_REBUILD_2026-08-13.md`; §LR3.* vẫn đóng; 2 P1 + 5 P2 mới (§LR4.01–§LR4.07); production HOLD; bằng chứng `TRACED`, chưa runtime Tauri. |
| 2026-08-13 | Kiểm chứng lại báo cáo `W3-U03` (sửa claim test sai, số 0,5625%, quy kết nguyên nhân §LR4.01) rồi sửa §LR4.01–§LR4.07 theo lô A–E. | `LOGO_REBUILD_FIXES_2026-08-13.md`; backend logo 66/66, vitest logo+i18n+routing 74/74, typecheck pass; regression engine thật dấu 3×3 (despeckle 0 giữ / 4 nuốt); đổi copy thành "Vector hóa Logo"; HOLD/NO-GO giữ nguyên, còn runtime Tauri + JPEG holdout. |
| 2026-08-13 | Audit `W7-U09` cửa sổ In hay “out”: overlay+Properties khuất, pdf.js full-file, PrintDlg HWND xuyên process, worker không COM, list rỗng kẹt UI, hủy xóa temp sớm. | `BAO_CAO_AUDIT_CUA_SO_PRINT_2026-08-13.md`; log máy dev mở dialog nhiều lần, 0 job `print_pdf_direct`; `§PRINT.01–08`/`§PRINTRANGE` không hồi quy trên code. Chờ duyệt, chưa sửa. |
| 2026-08-14 | Hoàn thiện Lô 0A `W7-U10` baseline PPE Native Viewer mà không build/chạy app: khóa scope trang chính + thumbnail đóng, checkpoint atomic, config đúng path, hình học request↔LiveTile/DPI/xoay/clip/phủ viewport và giới hạn request→pixel. | Node/Python self-test schema 2, `rustfmt --check`, Cargo metadata offline/locked, `git diff --check`, fingerprint Standee/FOGRA39/font đạt; runtime gate còn mở. |
| 2026-08-14 | Chạy nghiệm thu runtime Lô 0A `W7-U10` trên Standee thật và sửa request trùng `LiveTile` khi effect + IntersectionObserver cùng phát. | Core 30/30; WebView2 60/60 pixel gate đạt, `900/900` PPE fulfilled và 0 display/shadow/reject; cold FSP P50/P95 `1511/1798 ms`, warm `803/844 ms`; peak cây `2889,555 MiB`. Thumbnail smoke `587 ms` vẫn PDFium. 73/73 test + typecheck; không build/commit/push. |
| 2026-08-19 | Audit + sửa `W2-U03-COLOR` lỗi toàn trang đổi màu, gradient gãy và trang trắng sau bù xén trên đúng hai file CMNM2026, trang 1; feedback cuối loại hồi quy tự đổi Quỹ đạo thành kéo Form vector. | `BAO_CAO_AUDIT_MAU_HIEU_UNG_BU_XEN_2026-08-19.md`; rectangle giữ đúng `trajectory/inpaint` đã chọn, Image + SMask nằm dưới Form artwork gốc và metadata requested/applied khớp. Engine + quỹ đạo 162/162; file thật trang 1 trajectory/bleed 2 mm/bite 1 mm chạy `3,017 s`, không warning, vùng sâu 0% pixel lệch >10. Viewer không tự PPE→PDFium; worker vẫn trả PNG PPE khi chỉ geometry/font xấp xỉ, chỉ chặn `ink_unsound`. Frontend 72/72 + typecheck; Rust worker 31/31. |
| 2026-08-19 | Audit + sửa `W2-U03-EDGE` trên đúng `tải xuống.jpg`: màu viền nhạt, OOM 214 MiB và hồi quy đường bế răng cưa. | `BAO_CAO_AUDIT_BU_XEN_BE_TEM_MAU_VIEN_HIEU_NANG_2026-08-19.md`; deterministic chỉ lấy màu nền, AI giữ hình học. PDF cuối 1 CutContour/91 cubic, median bleed `RGB(85,10,6)`, raster 2000²; 260/260 test liên quan đạt. |
| 2026-08-20 | Audit `W3-U05` engine màu RGB→CMYK từ UI/API qua writer/PPE tới PDF/TIFF mở lại; chưa sửa source theo chốt duyệt. | `BAO_CAO_AUDIT_ENGINE_MAU_RGB_CMYK_2026-08-20.md`; xác nhận 3 P0 + 7 P1 + 3 P2, HTTP 500, artifact ảnh trắng/unfilterable, residual colorspace, ICC/OI/black-policy drift; Rust ICC 12 pass, các suite hẹp frontend/backend xanh nhưng thiếu postcondition. |
| 2026-08-20 | Sửa `W3-U05` Lô A+B: response log typed-list, Flate CMYK/Gray, parser hậu kiểm residual và cleanup route/ActionEngine. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; targeted backend/artifact tests đạt; runtime desktop và các lô profile/intent/Combine còn mở. |
| 2026-08-20 | Sửa `W3-U05` Lô C–E: sRGB asset thật, ICC nguồn nhúng, intent/BPC, black K-only, OutputIntent profile đích và HTTP smoke. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; 146 backend + 16 Rust test đạt; sidecar cô lập upload/convert/download/reopen/render đạt; PPE CalRGB/SMask và profile auto còn mở. |
| 2026-08-20 | Sửa `W3-U05` Lô F1/F2: PPE CalRGB/CalGray hạ soundness; SMask Matte khử preblend; writer fail-closed mask RGB chưa rewrite. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; print_engine `371 pass/2 ignored`, backend màu `147 pass`; CalRGB full calibration/profile auto còn backlog. |
| 2026-08-20 | Sửa `W3-U05` Lô G: profile/intent Convert Colors ↔ Output Preview theo tab; Relative+BPC công khai; request/profile fail-closed. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; typecheck + frontend `13 pass`, backend `104 pass`; smoke cổng `18323` upload/convert/download/reopen/postflight/OI/render đạt, ba input/profile lỗi trả 422. |
| 2026-08-20 | Sửa `W3-U05` Lô H: codify corpus brightness FOGRA39 Image XObject + PDF vector patch thành regression gate. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; `test_color_conversion_acceptance.py` 2/2, backend màu tổng `106 pass`; khóa byte BPC, ΔL*/TAC/K-only/OI/postflight. |
| 2026-08-20 | Sửa `W3-U05` Lô I: wide-gamut artifact, graphics color state, ICC identity và transparency fail-closed. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend màu `188 pass`; AdobeRGB/P3 byte CMM + ΔE00/ΔL*/TAC đạt, split/qQ không biến màu, ICC mismatch/alpha không publish; HTTP P3 download đạt. |
| 2026-08-20 | Sửa `W3-U05` Lô J: flatten có điều kiện DeviceRGB+SMask trước ICC và surface warning qua HTTP/ActionEngine. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend màu trước Lô K `191 pass`; ordinary/Matte isolated image khớp CMM, nền vector/Form reuse fail-closed; smoke cổng `18325` success/failure + no-artifact đạt. |
| 2026-08-20 | Sửa `W3-U05` Lô K: ICCBased Adobe RGB/Display P3 alpha đổi qua blend DeviceRGB trước composite, thêm oracle wide-gamut và smoke socket thật. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend màu `194 pass`; isolated Adobe alpha khớp byte CMM + OI/TAC, backdrop vector business failure không artifact, smoke cổng `18326` đạt. |
| 2026-08-20 | Sửa `W3-U05` Lô L: fail-closed `/Decode` tùy biến của RGB image/SMask trước writer/flatten. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend màu `197 pass`; HTTP contract `11/11`; smoke cổng `18327` trả blocker/no-output, mapping mặc định vẫn đạt. |
| 2026-08-20 | Sửa `W3-U05` Lô M: fail-closed ICCBased RGB `/Range` tùy biến/hỏng trước writer và alpha flatten. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend màu `209 pass`; HTTP contract `12/12`; smoke cổng `18328` xác nhận canonical CMYK/OI sạch và custom blocker/no-output. |
| 2026-08-20 | Sửa `W3-U05` Lô N: mở CalRGB matrix/gamma lane có calibration tự nhất quán, giữ alpha/shading/BlackPoint ngoài phạm vi fail-closed. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend màu `249 pass`; acceptance CalRGB `7/7`; smoke cổng `18329` download CMYK/OI/discriminator đạt. |
| 2026-08-20 | Sửa `W3-U05` Lô O: mở vector-alpha isolate lane có oracle cho đúng một fill DeviceRGB alpha trên nền trắng; nhiều paint/q-Q state/blend/soft-mask/group/backdrop tiếp tục fail-closed. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend ma trận màu `256 pass, 2 warnings`; native/HTTP regression + smoke cổng `18331` positive `/DeviceCMYK` không `/ExtGState`, negative business failure/no-artifact. |
| 2026-08-20 | Sửa `W3-U05` Lô P: harden detector/hậu kiểm Flatten Transparency cho inline mask, Type3, annotation AP và plate PPE sai kích thước; surface blocker qua ActionEngine. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend ma trận màu `261 pass, 2 warnings`; smoke cổng `18332` success cảnh báo mất vector + inline/AP failure no-artifact; baseline giữ complex raster ngoài Convert Colors vì thiếu OI/drift màu. |
| 2026-08-20 | Sửa `W3-U05` Lô Q: validate `x1a|x4`, thay PDF/X-1a compliance heuristic bằng parser thật và giữ blocker transparency/RGB qua export/route. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend ma trận `271 pass, 2 warnings`; socket `18333` CMYK X-4 success/OI sạch, complex alpha + invalid standard 422 no-artifact. |
| 2026-08-20 | Sửa `W3-U05` Lô R: UI Save PDF/X + Convert Colors kiểm warning/download, chỉ commit sau artifact hợp lệ; sửa copy X-4 và unknown compliance check thành manual. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; Windows UI `12 pass`, `tsc --noEmit` đạt; warning `MẤT VECTOR` hiện, download lỗi không gọi `onFileFixed`. |
| 2026-08-20 | Sửa `W3-U05`/`W6-U02` Lô S: Recipe prepress giữ `warnings` từ response PDF/X/Convert Colors (`warnings[]` + `log[]`) và `engine`, PlaybackRunner gom theo step/nhánh thoát sớm, ImpositionTab hiện toast sau commit. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; Vitest recipe `116 passed`, typecheck đạt; Tauri production click-smoke còn mở. |
| 2026-08-20 | Sửa `W3-U05` Lô T: Flatten Transparency kiểm provenance PPE/FOGRA39, plate C/M/Y/K, source OI `/N=4`/bytes và hậu kiểm artifact; mixed/no-OI cảnh báo không print-ready, Spot+transparency fail-closed. | `ENGINE_MAU_RGB_CMYK_FIXES_2026-08-20.md`; backend liên quan `326 passed, 2 warnings`, native PPE `78 passed`, frontend recipe/preprocess `126 passed`, typecheck đạt; smoke artifact all/mixed/Spot đạt, complex transparency drift vẫn HOLD, Tauri production click-smoke còn mở. |
| 2026-08-21 | Re-audit `W3-U05-R2` trước khi sửa: Relative+BPC tốt nhất trên `Desktop\rgb.pdf`, nhưng xác nhận §COLOR.28–.30 P1 (vibrance no-op, mixed relabel, bỏ DefaultRGB) và §COLOR.31–.32 P2; chờ duyệt. | `BAO_CAO_AUDIT_CHUYEN_HE_MAU_RGB_CMYK_GIU_SANG_2026-08-21.md`; đây là baseline lịch sử, đã được supersede bởi các dòng triển khai bên dưới. |
| 2026-08-21 | Chạy click-smoke `W3-U05` trên Tauri dev/source hiện tại và dọn runtime cô lập. | WebView2/CDP harness `23/23`, không console/page/network error; positive RGB+SWOP commit một Working File CMYK FOGRA39 có OI `/N=4`/K-only/no-residual, negative alpha+Multiply hiện `[LIVE_TRANSPARENCY_RGB]`, không output/không commit; manifest `audit/evidence/W3_U05_TAURI_CLICK_SMOKE_2026-08-20.json`. Packaged-installer production smoke còn mở. |
| 2026-08-21 | Sửa W3-U05 Lô A–C: Lab/vibrance canonical, reachable existing DeviceCMYK/OI policy và DefaultRGB resource scope. | `CHUYEN_HE_MAU_RGB_CMYK_GIU_SANG_FIXES_2026-08-21.md`; backend color regression/artifact gate đạt; mixed/profile/default invalid đều fail-closed. |
| 2026-08-21 | Sửa W3-U05 Lô D1–D2: preview PPE theo trang, metric ΔL*/ΔC*/ΔE00/clipping/TAC, balanced-v1 gate, UI preview-before-commit và copy scope trang. | `CHUYEN_HE_MAU_RGB_CMYK_GIU_SANG_FIXES_2026-08-21.md`; preview `16 pass`, frontend preprocess suite đạt; no-download temp cleanup và RAM gate đã kiểm. |
| 2026-08-21 | Sửa W3-U05 Lô D3: Spot dùng profile đích, Working PDF revision fingerprint, execute generation fence/abort, schema + UI trusted-recommendation fail-closed. | Backend color gate `134 passed, 2 warnings`; frontend `18 passed`; typecheck, ESLint và `git diff --check` xanh. Artifact probe `rgb.pdf`/HTTP đạt; Tauri exact-file, installer và press proof còn mở. |
| 2026-08-21 | Chạy click-smoke `W3-U05` trên Tauri dev/source hiện tại và dọn runtime cô lập. | WebView2/CDP harness `23/23`, không console/page/network error; positive RGB+SWOP commit một Working File CMYK FOGRA39 có OI `/N=4`/K-only/no-residual, negative alpha+Multiply hiện `[LIVE_TRANSPARENCY_RGB]`, không output/không commit; manifest `audit/evidence/W3_U05_TAURI_CLICK_SMOKE_2026-08-20.json`. Packaged-installer production smoke còn mở. |
| 2026-08-26 | Audit `W6-U04` vòng đời file và `W3-U06` hợp đồng ảnh; chưa sửa finding production. | `BAO_CAO_AUDIT_VONG_DOI_FILE_VA_HOP_DONG_ANH_2026-08-26.md`; 4 file/55 test xanh; harness TIFF expected 0,48 pt received 2 pt; scanner self-test 17/17. |
| 2026-08-26 | Khép F2b của `W6-U04` (§FILE.A4) ở mức `ARTIFACT`: chốt chặn Save As lên artifact tạm — tách plan/executor thuần cho bước ghi, nâng oracle native lên định danh file Windows có fallback so chuỗi, nối call site `handleSaveFile` và khoá i18n vi/en. Không tự nâng `RUNTIME`. | Baseline `b0bfa6aa6e3b2fe6aabb31871aa0a831ec951604`; spec `.kiro/specs/save-as-artifact-guard/`; báo cáo gốc `BAO_CAO_AUDIT_VONG_DOI_FILE_VA_HOP_DONG_ANH_2026-08-26.md`. TS phạm vi spec 6 file/64 test, full suite 287 file/2.849 pass/2 skip/0 fail, Rust `disk_copy_request_tests` 7/7 (hardlink, junction, fail-closed, artifact nguyên vẹn) + toàn crate 160 pass/5 ignored, `cargo check --lib`/typecheck/lint 0 error/lint:budget xanh; đỏ-trước bằng mutation tạm ở bốn tầng. Còn chuỗi 7 bước `run_dev.bat`, ổ mạng map so với UNC, thời điểm xoá của vòng dọn artifact backend và symlink. |
| 2026-08-26 | Cập nhật ma trận theo Lô A–F của `W6-U04`/`W3-U06`: cả 10 finding đã có bản sửa, hàng `8c` lên `AUTO`, hàng `5e` lên `AUTO + ARTIFACT-PARTIAL`; ghi kết luận 5 nghi vấn cần fixture/oracle (§FILE.S1/S2 và bullet browser fallback đã đóng; §IMG.S1/S2 còn mở đã xác nhận; §IMG.S3 vẫn `[SUSPECTED]`). | `BAO_CAO_AUDIT_VONG_DOI_FILE_VA_HOP_DONG_ANH_2026-08-26.md`; harness TIFF 300 DPI nay xanh `0,48 × 0,72 pt`; Rust `system_files_inbox_tests` 2/2; TS `SystemIntegrations` 14, `useIncomingFileDispatcher` 19, `tabNavigation` 21, `useRecentFiles.office` 3, `ImpositionTab.fileOpening` 8, `useTauriLifecycleListeners` 7, `imageNormalizer` 29, `combineTransport.integration` 22, `upscaleWorkingPage` 4. `ARTIFACT` chỉ đạt ở §IMG.B6 (JPEG CMYK reparse `/DCTDecode` + `/ICCBased` `/N 4` + `/Alternate /DeviceCMYK`). Còn mở: artifact TIFF thật, ICC/EXIF/Orientation ở Rust normalizer (§IMG.S1/S2/S3), các call site chỉ `TRACED`, `read_frame` không deadline, second-instance/root-reload/Alt+F4 và installed runtime; chưa build. |
| 2026-08-29 | Audit read-only `W2-U09/W7-U11` True-shape nesting Tem bế/CNC về correctness, sức tìm kiếm và hiệu năng; chưa sửa code theo chốt duyệt. | `BAO_CAO_AUDIT_HIEU_NANG_VA_SUC_MANH_NESTING_TEM_CNC_2026-08-29.md`; file thật 13 mẫu p50 23,854 s/p95 quan sát 25,239 s, 46 placement, baseline, `trialsRun=0`; instrumented CPU/Wall 0,923 trên máy 16 luồng/32 GB. Xác nhận preview lệch export, duplex/obstacle/fulfillment hở, solver tuần tự, NFP toàn blocker, search semantics yếu và generic tam giác 152→86 con. Release tiếp tục HOLD. |
| 2026-08-29 | Cập nhật artifact căn cụm `W2-U09/W7-U11`; đóng correctness §NEST-CENTER, không nâng tier/HOLD. | 44 tem; `dx/dy = 0,032611/0,073127 mm`; 43 pose 0° + 1 pose 90°; preview/export cùng manifest/fingerprint; QA Front/Cut + 4 ốc đạt. Cold preview 74,736 s còn mở. Xem `BAO_CAO_AUDIT_CANH_CUM_TRUE_SHAPE_NESTING_2026-08-29.md`. |
| 2026-08-29 | Re-audit source cuối `W2-U09/W7-U11`; đóng pin dedup/cache pivot, benchmark direct-difference và giữ HOLD. | Native `97b3f0e4…fec4`; Rust 334/334, backend focused 295/295; đúng input SHA `EFCDE4…57AB`: 44 tem, 1 pose 90°, native revalidation + preview/render identity + Front/Cut/4 ốc đạt. Cold/warm/render `23,778/0,101/2,707 s`; giảm 68,2% nhưng cold vẫn FAIL KPI. Xem `BAO_CAO_AUDIT_HIEU_NANG_VA_SUC_MANH_NESTING_TEM_CNC_2026-08-29.md`. |
| 2026-08-30 | Re-audit độc lập báo cáo song song/preview của `W2-U09/W7-U11`; chưa sửa production, giữ HOLD. | `BAO_CAO_RE_AUDIT_SONG_SONG_VA_PREVIEW_NESTING_2026-08-30.md`; log lịch sử xác nhận 45→44 khi session miss. Các phiên app sau và fresh-store regression đạt 44×0°/`solvedAgain=false`, nhưng persisted-store current-source tái hiện `ManifestConflictError` rồi export `solvedAgain=true`; parity vẫn OPEN. Artifact lề 3 mm nằm giữa là bằng chứng hình học riêng. Cold preview `15,519–16,640 s`; khác request/native với mốc 23,778 s nên không gọi là A/B speedup. Xác nhận mỗi solve một thread, hardware workers bị rơi, portfolio chưa có; hạ các claim CPU/Wall→20×, 324 ms/orientation, 44 placement/sweep, LRU 192 MB và “parallel trial chắc chắn lợi ích 0” về đúng mức bằng chứng. Backend 99 pass đã gồm exact-file case + rerun riêng 1 pass, Rust 33+8+1 pass. Lô tiếp theo chờ duyệt: D0 provenance + phase counters. |
| 2026-09-02 | Hoàn tất Lô A–F và chốt baseline current source/native cho audit tốc độ preview/thực thi `W2-U09/W7-U11`; bổ sung corpus 23/23, sửa provenance CNC writer và đóng oversubscription contention. | `BAO_CAO_AUDIT_TOC_DO_PREVIEW_VA_THUC_THI_NESTING_TEM_CNC_2026-09-01.md`; native `04f40087…b4d0a0`. N=20: preview cold/warm P50/P95 `9,313/10,758` và `2,265/3,458 giây`, execution `6,416/7,066 giây`, không solve lại/raster parity. CNC writer raw `CD5459DF…11FF9`: simplex `12,980/13,714 ms`, duplex `22,498/28,976 ms`, parity nhưng chỉ `quantity=2`/hai pose. Breadth Lô 0 N=1: 23/23 chạy, 14/22 không kém, 8/22 kém, 1 fail-closed; raw `0D1D7646…B5838`. PERF-NEST-12: grant `30→15`, deficit layout `50→16`, 47 focused + 253 production pass; absolute wall phẳng, strict parity còn một outlier. Verdict `SOURCE + AUTO + PROBE · HOLD`; còn packaged runtime, N=20 đủ 23 ca/hai tier RAM thấp, contention 3+ preview và fixture+harness CNC duplex nặng thật. |
| 2026-09-02 | Follow-up theo phản hồi đổi SL/metadata: tách invalidation layout khỏi report, tính số tờ cục bộ và gia cố marker batch spill. | Bình trang giữ preview khi đổi SL dương/mã đơn/tên sản phẩm/vật liệu/cán màng; đổi tập trang hoặc hình học vẫn tính lại; N-Up/CNC giữ semantics quantity. Preview→Execute giữ manifest/layout nhưng writer dùng report mới có `reportHash` + artifact fingerprint. UI giữ “Sức chứa” + “Cần in” cập nhật cục bộ, chỉ bỏ “Kiểu xếp”. Sửa lỗi batch 13 mẫu: durable reference không còn đòi session nóng; marker dựng all-or-nothing đủ hash nên không lỗi từ mẫu 2 sau LRU, hash lệch/spill lỗi vẫn fail-closed. Backend 258 + 505 pass trong sandbox + 2/2 multiprocessing ngoài sandbox, post-fix 111; frontend 90 pass + typecheck/lint, post-fix 40; runtime app thật còn mở. |
| 2026-09-05 | Audit độ thông minh/hiệu năng nesting `W2-U09/W7-U11`; không sửa production, chờ duyệt Lô A. | `BAO_CAO_AUDIT_NESTING_THONG_MINH_HIEU_NANG_2026-09-05.md` + JSON evidence. Mở §NEST26.1 P1 marks CNC rơi ở preview gây identity/reference miss; §NEST26.2 P2 Balanced core 12 interrupted, S20 vùng bao lớn hơn Fast 7,95%; §NEST26.3 P2 corpus thực grant 1 dù planner 15. S&R solve-only batch 10,088 s/600 vs serial 25,799 s/661, N=1, không quality gate UI/artifact. Backend 257 pass/1 lỗi môi trường, rerun riêng ngoài sandbox 1 pass; Rust lib 47 + gap 2, scanner 17. Native dirty `baeec051…`/identity `fbe222dd…572bf`; HEAD lập báo cáo `669b92db…`. Delta chỉ `TRACED + PROBE · HOLD`; không nâng `RUNTIME` hoặc phủ định bằng chứng artifact lịch sử. |
| 2026-09-05 (cập nhật sau duyệt) | Đã triển khai Lô A handoff CNC và quick-win cache baseline. | `cncDuplexMarks` đi qua cache key/payload/schema/mapper/job identity; contract tests true→4 obstacles, false→0 và invalidate pass. Baseline Rust tái dùng NFP cache ở reason-check và bỏ clone `RegionMm`; `mixed_nesting_baseline` 43 passed/2 ignored, GridPreview 42 passed, backend marks 2 passed. Chưa hứa speedup phần trăm; cần benchmark after cùng quality floor và artifact CNC duplex thật. |
| 2026-09-08 | Tái audit và sửa `W2-U02-OC / PONTLAYER-RE`: true-shape writer của **Bình Tem Bế (Sticker) lẫn CNC** nay ghi OCG Graphtec/layer/group + `/NM`; modal/save-plan nhận diện CUT chung cuối file; merge nhiều chunk remap OCG theo ref; hardening pontType/guide/path sentinel. Page Sheet legacy giữ OCG trong artifact đối chứng. | `BAO_CAO_AUDIT_REAUDIT_OC_LAYER_ILLUSTRATOR_2026-09-08.md`, `OC_LAYER_ILLUSTRATOR_FIXES_2026-09-08.md`; trước sửa route `true_shape=True`, artifact `ocprops=False`; backend full trước Lô C **5.358 passed/2 skipped**, sau Lô C focused **101 passed**, frontend focused **187 passed**, typecheck + Vite build + ESLint file sửa đạt. Tier `AUTO + ARTIFACT`; runtime Illustrator/Graphtec chưa xác minh. |
| 2026-09-08 (đính chính 09/09) | Sanitize basename PDF tạm chỉ bảo vệ đầu vào; chưa khắc phục Win32 `0x8007007b`, người dùng báo vẫn lỗi. | 11 test modal mock IPC + Vite build không chứng minh lời gọi Win32 thành công. Nguyên nhân buffer native và bản sửa nằm ở dòng 09/09 bên dưới. |
| 2026-09-09 | `W2-U02-OC / WIN32-7B`: tái hiện WinError 123 với tên hợp lệ khi `FILE_RENAME_INFO.FileName` thiếu NUL và không có padding. Sửa cấp/copy NUL riêng, `FileNameLength` không tính NUL; giữ canonicalization, identity, DELETE access, no-share WRITE/DELETE và handle sau publish. | `OC_LAYER_ILLUSTRATOR_FIXES_2026-09-08.md` Lô E; Rust regression đỏ trước/xanh sau, native file I/O 13 pass, toàn Tauri lib **242 pass/5 ignored**, cargo check + modal 11 pass. Quan sát debug EXE/process mới sau lúc sửa Rust; chưa nghiệm thu nút Bế → Illustrator/installer, không nâng RUNTIME GUI. |
