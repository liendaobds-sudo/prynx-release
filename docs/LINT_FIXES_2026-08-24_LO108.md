# LO108 — Tách API dọn tile cache khỏi LivePageFrame (2026-08-24)

## Phạm vi

- `desktop/src/components/workspace/LivePageFrame.tsx`
- `desktop/src/components/ImpositionTab.tsx`
- API cache đã có tại `desktop/src/lib/tileUrlCache.ts`

## Bằng chứng trước sửa

ESLint báo hai finding `react-refresh/only-export-components` tại export dòng 137 của `LivePageFrame.tsx`. Hai hàm chỉ thao tác cache thuần, không phụ thuộc React; việc re-export từ component khiến Fast Refresh coi file là module hỗn hợp.

## Thay đổi

- Xóa hai tên `clearTileUrlCache` và `clearTileUrlCacheForFile` khỏi import/re-export của `LivePageFrame.tsx`.
- `ImpositionTab.tsx` import trực tiếp từ `../lib/tileUrlCache`, là module sở hữu implementation và trạng thái cache.
- Không đổi implementation, namespace, owner lifecycle, điều kiện clear hoặc payload; đường gọi dọn cache theo file vẫn giữ nguyên.

## Verify

- ESLint targeted: `react-refresh/only-export-components` trên `LivePageFrame.tsx`, `ImpositionTab.tsx`, `tileUrlCache.ts` = `0`.
- `npx vitest run src/lib/tileUrlCache.test.ts src/components/workspace/LivePageFrame.liveTile.test.tsx`: `2` file, `33/33` test đạt.
- `npm run typecheck`: đạt.
- Các lỗi/warning ESLint khác trên component lớn là finding tồn tại ngoài phạm vi lô này.
- Chưa commit, push hoặc build release.
