# LINT FIXES 2026-08-24 LO131

## Phạm vi

- desktop/src/hooks/viewer/useViewerZoom.ts

## Việc đã làm

- Thay timer ref any bằng ReturnType<typeof setTimeout>.
- Kiểu hóa sự kiện kéo minimap từ Event/CustomEvent detail có guard số.
- Bổ sung dependency ref/setter ổn định cho các listener/effect; dùng updateViewportRectRef cho effect khai báo trước callback để tránh closure cũ và không đổi thứ tự hook.
- Giữ vòng đời ResizeObserver ổn định bằng mainWidthRef, không rebind observer mỗi lần width state đổi.
- Khoanh suppression có lý do cho effect auto-fit đồng bộ zoom/isReady với layout viewport; không thay đổi công thức zoom.

## Verify

- ESLint mục tiêu: đạt, 0 errors / 0 warnings.
- npm run typecheck -- --pretty false: đạt.
- npx vitest run src/hooks/viewer src/components/acrobat: 14 file, 149 passed.
- git diff --check: đạt.
- Không commit, push hoặc build release.