# LO107 — Reset draft Marks Settings theo phiên mở (2026-08-24)

## Phạm vi

- `desktop/src/components/imposition-tools/MarksSettingsDialog.tsx`

## Thay đổi

Tách content thành component keyed theo cấu hình marks. Draft được khởi tạo một lần khi content mount; thay đổi cấu hình từ parent tạo key mới và reset đúng như effect cũ. Listener Escape vẫn nằm trong effect vì đây là đăng ký sự kiện ngoài React, không phải setState đồng bộ.

## Verify

- ESLint mục tiêu: 0 lỗi, 0 cảnh báo.
- `marksConfig.test.ts`: 1/1 đạt.
- `npm run typecheck`: đạt.
- Chưa commit/push/build release.
