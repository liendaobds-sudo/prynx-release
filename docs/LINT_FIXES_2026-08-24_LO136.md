# LO136 — LivePageFrame

## Phạm vi

- File: `desktop/src/components/workspace/LivePageFrame.tsx`.
- Mục tiêu: sửa 4 lỗi `number | undefined` tại khởi tạo `startFields` và type hóa các boundary VDP rõ ràng, không đổi hành vi render/kéo/resize.

## Thay đổi

- Dùng giá trị mặc định `0` cho x/y/width/height khi tạo dữ liệu tương tác VDP, tránh truyền `undefined` vào `startFields`.
- Dùng `VdpToolField` cho callback `onVdpFieldsChange`, các thao tác duplicate/group/rotate và overlay VDP.
- Thu hẹp type cho VDP auto-fit, dynamic font và xử lý text/null; giữ nguyên các fallback hiển thị hiện tại.
- Không thay đổi payload, tọa độ, render policy, hook dependency hay backend.

## Verify

- `npm run typecheck -- --pretty false`: đạt; không còn lỗi TypeScript của `LivePageFrame.tsx`.
- `npx eslint src/components/workspace/LivePageFrame.tsx --format stylish`: còn 30 lỗi và 14 cảnh báo debt cũ ở các boundary khác; giảm từ baseline 50 lỗi và 14 cảnh báo của lô này.
- `npx vitest run src/components/workspace/LivePageFrame.liveTile.test.tsx src/components/workspace/LivePageFrame.renderPolicy.test.ts`: 2 file, 45 test đạt.

## Chưa xử lý trong LO136

- Các `any` còn lại ở Tile/SelectableText/Edit preview và các cảnh báo exhaustive-deps có vòng đời nhạy cảm; cần lô riêng với boundary/test tương ứng, không sửa hàng loạt trong lô này.
