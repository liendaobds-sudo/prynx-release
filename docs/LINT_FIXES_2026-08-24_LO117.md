# LINT FIXES 2026-08-24 LO117

## Phạm vi

- desktop/src/components/imposition-tools/sections/AdvancedSettingsSection.tsx

## Baseline

- File còn các lỗi type phát sinh sau khi bỏ ts-nocheck ở các lô trước: setter nhận string, preset JSON là any, kiểu thứ tự report quá rộng, selector thiếu dữ liệu theo trang và kiểu grouping optional không khớp setter.
- Lint budget trước lô: 588 errors / 93 warnings (theo baseline chiến dịch).

## Việc đã làm

- Giữ nguyên payload và hành vi UI; chỉ bổ sung type import/union cast tại các select.
- Kiểu hóa preset Pont từ localStorage bằng unknown + kiểm tra mảng/name, tránh truyền dữ liệu JSON không hợp lệ vào store.
- Thu hẹp kiểu orderedReportControls và vị trí report; bổ sung targetQuantitiesByPage vào selector.
- Dùng chỉ mục số đúng với hợp đồng Record<number, number> cho preview số lượng theo trang.

## Verify

- npx eslint src/components/imposition-tools/sections/AdvancedSettingsSection.tsx: đạt, 0 errors / 0 warnings.
- npm run typecheck -- --pretty false: đạt.
- npm run lint:budget: đạt, 581 errors / 90 warnings.
- Không commit, push hoặc build release.

## Rủi ro còn lại

- File vẫn còn các finding no-explicit-any/hooks ở những vùng chưa thuộc lô này; tiếp tục xử lý theo lô tối đa 5 file và verify riêng.
