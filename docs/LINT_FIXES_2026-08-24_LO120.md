# LINT FIXES 2026-08-24 LO120

## Phạm vi

- `desktop/src/components/imposition-tools/ImposerDashboard.tsx`

## Kết luận

- Chưa gỡ `// @ts-nocheck`.
- File này vẫn có cụm debt lớn quanh `any` và cast runtime, nên bỏ suppression lúc này không an toàn nếu giữ nguyên payload/logic.

## Bằng chứng

- `npx eslint src/components/imposition-tools/ImposerDashboard.tsx`
  - Báo lỗi ngay tại dòng 1 vì `@ts-nocheck`.
  - Báo thêm nhiều `@typescript-eslint/no-explicit-any` rải khắp file.
  - Tổng cộng: 47 vấn đề, trong đó 28 lỗi và 19 cảnh báo.
- `npx tsc --noEmit --pretty false --project tsconfig.app.json`
  - Lọc theo `ImposerDashboard` không còn lỗi từ file này.
  - `EXIT=0` cho phần typecheck được lọc, nhưng điều đó không đủ để bỏ suppression vì lint vẫn đỏ.

## Ghi chú

- Không sửa code file này trong lô LO120.
- Giữ nguyên logic và payload theo yêu cầu.
