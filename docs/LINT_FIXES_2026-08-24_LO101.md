# Lint fixes 2026-08-24 — Lô 101

## Phạm vi

- `desktop/src/components/imposition-tools/sections/GridPreview.tsx`
- `desktop/src/components/imposition-tools/sections/gridPreviewHelpers.ts`
- `desktop/src/components/imposition-tools/sections/GridPreview.trapezoid.test.ts`
- Mục tiêu: loại finding `react-refresh/only-export-components` khỏi component preview mà không đổi logic bình bản.

## Bằng chứng trước lô

- ESLint mục tiêu trên `GridPreview.tsx` ghi nhận 3 finding `react-refresh/only-export-components`: `shouldAutoSwitchToMixedGuillotine`, `resolveCellDirectionDegrees` và `resolveTrapezoidPreviewRatios`.
- Ba hàm đều là helper thuần, không render React; test trapezoid import trực tiếp helper từ `GridPreview.tsx`.

## Thay đổi

- Tách ba helper thuần và type `CellDirectionDegrees` sang `gridPreviewHelpers.ts`.
- Component chỉ import helper; không re-export lại từ file TSX, để Fast Refresh chỉ theo dõi component.
- Cập nhật test trapezoid sang module helper mới.
- Giữ nguyên điều kiện 422, phép tính hướng xoay và công thức tỷ lệ trapezoid; không đổi API/backend hoặc dữ liệu preview.

## Kết quả

- Finding `react-refresh/only-export-components` của `GridPreview.tsx`: 3 -> 0.
- ESLint mục tiêu sau lô: helper và test 0 lỗi/0 cảnh báo; `GridPreview.tsx` còn 38 lỗi `no-explicit-any` và 1 cảnh báo `exhaustive-deps` thuộc các lô khác.
- Các lỗi còn lại không được che bằng disable; giữ nguyên để xử lý theo lô type boundary riêng.

## Xác nhận

- `npx eslint src/components/imposition-tools/sections/GridPreview.tsx src/components/imposition-tools/sections/gridPreviewHelpers.ts src/components/imposition-tools/sections/GridPreview.trapezoid.test.ts`: Fast Refresh 0.
- `npm run typecheck`: đạt.
- `npx vitest run src/components/imposition-tools/sections/GridPreview.trapezoid.test.ts`: 3/3 đạt.
- `npx vitest run src/components/imposition-tools/sections`: 46/46 đạt.
- `git diff --check`: đạt (chỉ có cảnh báo chuẩn hoá CRLF của Git trên file đang sửa).
- Chưa commit, push hoặc build release.

## Bất biến đã giữ

- Không đổi layout, thuật toán xếp ô, đường bế, payload API, state store hay backend.
- Không sửa các finding `any`/hook ngoài phạm vi LO101.
