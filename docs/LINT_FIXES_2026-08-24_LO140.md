# LINT fixes — 2026-08-24 — LO140

## Phạm vi

Xử lý baseline ESLint/TypeScript còn lại sau các lô audit trước. Không thay đổi payload backend, hình học, format xuất hoặc luồng nghiệp vụ.

## Các lô đã xử lý

| Lô | File/phạm vi | Thay đổi | Verify |
|---|---|---|---|
| A | `desktop/test_impose.ts` | Thay `settings as any` bằng boundary cast `unknown as ProcessingSettings`, giữ fixture legacy rõ contract. | ESLint file đạt; typecheck đạt |
| B | `desktop/src/components/CombineTab.tsx`, `flipbook/FlipbookDialog.tsx`, `imposition-tools/OpenInDesignModal.tsx`, `imposition-tools/PaperSettingsUI.tsx`, `imposition-tools/cut-export/CutExportModal.tsx` | Bổ sung dependency thực sự cần; giữ trigger hẹp ở các effect nạp PDF/nhóm trang bằng suppression có lý do truy vết. | ESLint mục tiêu 0/0; typecheck đạt |
| C | `desktop/src/components/preprocess-tools/NumberingTool.tsx`, `useClassicCutlinePreview.ts`, `desktop/src/hooks/useVdpTool.ts` | Memo hóa helper sinh dãy và callback VDP; bổ sung dependency preview; giữ đồng bộ field theo số lượng có chủ đích. | ESLint mục tiêu 0/0; typecheck đạt |
| D | `desktop/src/components/preprocess-tools/OfficeConvertTool.tsx` | Giữ nguyên function declaration/lifecycle của staging; ghi suppression có lý do cho hai dependency trigger-scope để không kích hoạt staging lại mỗi render. | ESLint mục tiêu 0/0; OfficeConvert 12/12 |
| E | `desktop/src/i18n/locales/vi.json`, `en.json` | Bổ sung khóa login dùng tĩnh nhưng thiếu trong cả hai locale. | i18nCatalog 5/5 |

## Kết quả gate

- `npm run lint -- --max-warnings=0`: đạt, 0 lỗi / 0 cảnh báo.
- `npm run lint:budget`: đạt.
- `npm run typecheck -- --pretty false`: đạt.
- Regression phạm vi liên quan: 8 file viewer/imposition đạt **94/94**; `FlipbookDialog`, `OpenInDesignModal`, `useClassicCutlinePreview` đạt **21/21** khi chạy cùng lô.
- Bổ sung khóa `misc.login:loi_khong_xac_dinh` cho cả VI/EN sau khi test i18n phát hiện thiếu; test i18n đạt **5/5**.
- `OfficeConvertTool.test.tsx`: đạt **12/12** sau khi bỏ refactor lifecycle và giữ suppression trigger-scope.
- `npm run test -- --run`: đạt **269/269 file**, **2644 passed / 2 skipped** (2646 test tổng); không có test fail.

## Ghi chú rủi ro

- Working tree có nhiều thay đổi từ các chiến dịch trước; không stage/commit trong lô này.
- `git diff --check` còn báo whitespace/blank-line ở một số file ngoài phạm vi lô; chưa tự dọn để tránh trộn thay đổi.
- Các suppression hook đều có lý do trigger-scope ngay cạnh dependency array; cần kiểm tay các ca mở PDF/ghép file nếu đóng gói release.
