# LO110 — Lazy load preset giấy (2026-08-24)

## Phạm vi

- `desktop/src/components/imposition-tools/usePaperPresets.ts`

## Bằng chứng

Effect mount đọc localStorage rồi gọi `setSavedForms` đồng bộ, tạo finding `react-hooks/set-state-in-effect`.

## Thay đổi

Đưa việc đọc localStorage vào lazy initializer của `useState`. Imposer hiện truyền storage key cố định theo workspace; danh sách vẫn được nạp ngay ở lần mount đầu, dữ liệu hỏng vẫn rơi về mảng rỗng như trước. Các handler save/update/delete và persistence không đổi.

## Verify

- ESLint mục tiêu: 0 lỗi, 0 cảnh báo.
- `usePaperPresets.test.tsx`: 1/1 đạt.
- Chưa commit/push/build release.
