# Lô LO100 — Tách cấu hình mặc định MergeTool

## Phạm vi

- Finding: `react-refresh/only-export-components` tại `MergeTool.tsx:33`.
- Nguyên nhân: component file đồng thời export component và hằng số `defaultMergeSettings` được consumer ngoài UI dùng.
- Mục tiêu: đưa hằng số ra module riêng, giữ nguyên kiểu, giá trị và luồng khởi tạo settings.

## Thay đổi

- Thêm `desktop/src/components/preprocess-tools/mergeDefaults.ts` chứa `defaultMergeSettings`.
- `ImposerDashboard.tsx` dùng import type cho `MergeSettings` và import runtime từ helper.
- `MergeTool.tsx` không còn export hằng số runtime; các type/component và hành vi form giữ nguyên.

Không đổi payload merge, giá trị mặc định, routing hay backend.

## Verify

Baseline trước lô: `errors=636, warnings=95` (`npm run lint:budget`).

Sau lô:

- `npm run lint:budget`: `errors=635, warnings=95`, budget đạt.
- `npx eslint src/components/preprocess-tools/MergeTool.tsx src/components/preprocess-tools/mergeDefaults.ts`: đạt.
- `npm run typecheck`: đạt.
- `npx vitest run src/lib/preprocessEngine/preprocessEngine.test.ts`: `26/26` đạt.

Runtime desktop chưa chạy trong lô này; cần smoke test mở Imposition → Merge và xác nhận settings mặc định khi kiểm tay.
