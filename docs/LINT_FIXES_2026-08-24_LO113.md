# LO113 — Bỏ suppression ở section cài đặt imposition (2026-08-24)

## Phạm vi

- `desktop/src/components/imposition-tools/sections/NupSettingsSection.tsx`
- `desktop/src/components/imposition-tools/sections/OutputSettingsSection.tsx`

## Bằng chứng trước sửa

Cả hai section đều bắt đầu bằng `@ts-nocheck`, khiến TypeScript không kiểm tra hợp đồng UI với settings store. Nup có cast `as any` cho `layoutType`; Output có cast `as any` cho `cutType`. Import `CutType` trong Output đang nằm nhầm trong JSDoc nên không tạo tác dụng khi bật kiểm tra kiểu.

## Thay đổi

- Xóa `@ts-nocheck` ở cả hai section (giới hạn đúng hai file nhỏ trong lô).
- Nup dùng `NupSettings[layoutType]` cho setter `setLayoutType`, khớp kiểu store và giữ nguyên các giá trị UI hiện có.
- Output import `CutType` từ generated contract ở vị trí module hợp lệ và dùng kiểu đó cho `setCutType`.
- Không đổi điều kiện hiển thị, payload, giá trị mặc định hoặc hành vi setter.

## Verify

- ESLint targeted cho hai file: 0 lỗi, 0 cảnh báo.
- Test imposition: `npx vitest run src/components/imposition-tools/useImposerSettingsStore.characterization.test.ts src/components/imposition-tools/sections/GridSettingsSection.mixedGuillotine.test.tsx` — 2 file, 51/51 test đạt.
- `npm run lint:budget`: errors=605, warnings=93; gate đạt tại thời điểm chạy (có các lô khác đang đồng thời thay đổi workspace).
- `npm run typecheck`: chưa đạt do lỗi cú pháp có sẵn ngoài phạm vi tại `desktop/src/App.tsx:1056-1058` (unterminated string literal); không có lỗi nào trỏ vào hai section LO113 khi lọc output.
- Chưa commit, push hoặc build release.

## Rủi ro còn lại

Typecheck toàn repo cần chạy lại sau khi lỗi cú pháp ở App được lô sở hữu sửa. Hai section đã được ESLint và test store/UI liên quan kiểm tra độc lập; chưa chạy kiểm tay trong Tauri.