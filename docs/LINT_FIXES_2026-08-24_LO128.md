# LO128 — Auth store

## Phạm vi

- `desktop/src/stores/useAuthStore.ts`

## Thay đổi

- Thay hai truy cập runtime `window as any` bằng kiểu mở rộng `globalThis` an toàn.
- Khai báo kiểu hẹp cho response `license-verify`, gồm status, token, plan, feature và metadata.
- Thay các ép kiểu `any` ở nhánh xác thực hợp lệ bằng optional access và type guard cho danh sách feature.
- Thêm guard kiểu cho status thu hồi; không đổi payload, trạng thái license hoặc luồng fallback offline.

## Verify

- ESLint file mục tiêu: đạt, 0 lỗi, 0 cảnh báo.
- Typecheck frontend: còn 4 lỗi có sẵn ở `LivePageFrame.tsx:5601`, không thuộc lô này.
- Chưa commit/push/build.
