# LO112 — Derived preview Shuffle (2026-08-24)

## Phạm vi

- `desktop/src/components/preprocess-tools/ShuffleTool.tsx`

## Thay đổi

Chuyển chuỗi preview từ state cập nhật trong effect sang `useMemo`; tab hiện tại cũng được suy ra trực tiếp từ `settings.presetId`, để parent là nguồn sự thật duy nhất. Công thức mô phỏng, thông báo lỗi và chuỗi hiển thị giữ nguyên.

## Verify

- ESLint mục tiêu: 0 lỗi, 0 cảnh báo.
- `npm run typecheck`: đạt.
- Repo không có test UI Shuffle riêng; engine test được giữ nguyên, cần kiểm tay thao tác đổi tab/preset khi chạy app.
- Chưa commit/push/build release.
