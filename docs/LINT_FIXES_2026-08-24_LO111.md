# LO111 — Mutation imperative Three.js và callback minimap (2026-08-24)

## Phạm vi

- `desktop/src/components/dieline-tool/MockupCanvas.tsx`
- `desktop/src/components/dieline-tool/ShadowFloor.tsx`
- `desktop/src/hooks/viewer/useViewerZoom.ts`

## Bằng chứng

React Compiler đánh dấu các phép gán vào `WebGLRenderer`/`Scene` của Three.js và việc callback minimap được gọi trong callback ResizeObserver trước vị trí khai báo lexical. Đây đều là API imperative có chủ đích; thay đổi sang state React sẽ làm sai vòng đời renderer/viewport.

## Thay đổi

Thêm suppression có lý do ngay tại mutation Three.js và callback hoisted. Không thay đổi phép tính tone mapping, nền scene, zoom hoặc vị trí minimap.

## Verify

- ESLint mục tiêu: các finding immutability đã được suppress có lý do; các any/dependency vốn có giữ nguyên cho lô riêng.
- Dieline render wiring + no-network: **29/29 đạt**.
- `npm run typecheck`: đạt.
- Chưa commit/push/build release.
