# Lint fixes 2026-08-24 — Lô 106

## Phạm vi

- `desktop/src/components/ImpositionTab.tsx`
- `desktop/src/lib/impositionPathPolicy.ts`
- `desktop/src/lib/impositionPathPolicy.test.ts`

Mục tiêu là loại một finding Fast Refresh của helper thuần, giữ nguyên chính sách nhận diện artifact backend. Không chạm backend, PDFium, payload lưu file hoặc build release.

## Bằng chứng trước sửa

- ESLint trên `ImpositionTab.tsx`: 1 finding `react-refresh/only-export-components` tại `isEphemeralBackendPath`.
- Helper được gọi tại hai điểm: quyết định có buộc Save As khi commit và bỏ qua snapshot recovery trỏ vào path tạm.

## Thay đổi

- Tách `isEphemeralBackendPath` sang `desktop/src/lib/impositionPathPolicy.ts`.
- Cập nhật `ImpositionTab.tsx` import helper; xóa bản định nghĩa/export trùng trong component.
- Thêm test regression cho:
  - thư mục `uploads`, `results`, `temp` trên path Windows/POSIX;
  - tên artifact 32 ký tự hex `.pdf`;
  - path lưu thật, thư mục gần tên nhưng không khớp, và giá trị rỗng.
- Giữ nguyên regex, chuẩn hóa slash/chữ thường và kết quả boolean.

## Kết quả

- Fast Refresh finding của `ImpositionTab.tsx`: 1 -> 0.
- ESLint helper/test: 0 lỗi, 0 cảnh báo.
- ESLint `ImpositionTab.tsx` còn các lỗi `any`/hook tồn tại từ trước, không sửa ngoài phạm vi.
- Không thay đổi hành vi lưu file hoặc recovery.

## Xác nhận

- `npx eslint src/lib/impositionPathPolicy.ts src/lib/impositionPathPolicy.test.ts src/components/ImpositionTab.tsx`: Fast Refresh = 0.
- `npx vitest run src/lib/impositionPathPolicy.test.ts`: 3/3 test đạt.
- `npm run typecheck`: đạt.
- `git diff --check`: không có lỗi nội dung mới; working tree có cảnh báo line ending/blank line vốn liên quan thay đổi đồng thời ở ImpositionTab.
- Chưa commit, push hoặc build release.
