# LINT FIXES 2026-08-24 LO118

## Phạm vi

- `desktop/src/components/imposition-tools/sections/AutoCatalogSection.tsx`

## Baseline

- ESLint mục tiêu: 3 errors và 1 warning.
- Errors gồm `@ts-nocheck` và hai ép kiểu `any`; warning là dependency của `useEffect`.

## Việc đã làm

- Gỡ `// @ts-nocheck` khỏi section.
- Dùng `ImposerSettingsState` để giữ đúng union type cho `catalogMasterSigOverride` và `catalogRemainderPlacement`, không đổi giá trị payload.
- Destructure giá trị/phương thức Zustand dùng trong effect để dependency array phản ánh đúng các giá trị effect đọc.
- Không thay đổi điều kiện hiển thị, text i18n, callback nghiệp vụ hoặc cấu trúc payload.

## Verify

- `npx eslint src/components/imposition-tools/sections/AutoCatalogSection.tsx`: đạt, 0 errors / 0 warnings.
- `npm run typecheck`: toàn project còn lỗi ngoài phạm vi tại `AdvancedSettingsSection.tsx:977` (`TS7015`); không có lỗi được báo từ `AutoCatalogSection.tsx`.
- Chưa chạy Vitest vì thay đổi chỉ là type/lint trong component, không đổi logic engine.

## Rủi ro còn lại

- Runtime chưa được chạy thủ công trong lượt này; cần kiểm tra nhanh toggle catalog khi có booklet offset sau khi các lô typecheck toàn project hoàn tất.
- Không commit, push hoặc build release trong LO118.
