# LO134 — Type hóa `useWorkspaceStore`

Ngày: 2026-08-24
Phạm vi: `desktop/src/stores/useWorkspaceStore.ts`

## Mục tiêu

Loại bỏ 15 khai báo `any` ở workspace state/setter mà không đổi tên field, giá trị mặc định hoặc hành vi setter.

## Thay đổi

- Dùng `CachedPdfObject[]` cho danh sách object edit.
- Thêm type cây OCG dùng chung cho layer panel.
- Dùng `WorkspacePreflightIssue` cho finding được highlight trên viewer.
- Dùng `VdpToolField[]`; setter vẫn nhận dữ liệu template/recovery legacy ở boundary và cast đúng tại điểm nhận.
- Dùng `Record<number, Record<string, unknown>>` cho tham số shape theo trang.
- Thêm type cụ thể cho watermark preview và stamp/number preview.
- Khai báo updater `bleedView` đúng cấu trúc `{ show, mm }`.

Không thay đổi logic khởi tạo, reset hoặc cập nhật state.

## Verify

- `npx eslint src/stores/useWorkspaceStore.ts --format stylish`: đạt, 0 lỗi.
- Unit store: 6 file, 18 test đạt.
- `npm run typecheck -- --pretty false`: không còn lỗi trong `useWorkspaceStore.ts`; còn lỗi nền ở `DataMergeTool.tsx` và `LivePageFrame.tsx` thuộc các lô khác (không phải LO134).

## Rủi ro còn lại

Runtime cần tiếp tục giữ dữ liệu VDP template/recovery cũ; setter có boundary type riêng để không thay đổi tương thích đó. Chưa build release.