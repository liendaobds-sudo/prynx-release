# PrynX — Claude Code

@AGENTS.md

Bộ skill dự án: `.claude/skills/` (bản gốc ở `.agents/skills/` — sau khi sửa bản gốc, chạy `powershell scripts/sync_ai_skills.ps1` để đồng bộ). Lệnh nhanh: `/audit <chủ đề>`, `/add-boxtype <tên hộp>`, `/verify`.
